\documentclass[10pt]{article} % For LaTeX2e

\PassOptionsToPackage{numbers, sort&compress}{natbib}
\usepackage{tmlr}

\usepackage[colorlinks=true,citecolor=BlueViolet,linkcolor=Blue]{hyperref} % For hyperlinks in the PDF like citations
\usepackage{mpemath}
\RequirePackage[capitalise]{cleveref}
\usepackage{graphicx} % Required for inserting images
\usepackage[dvipsnames]{xcolor} 
\usepackage{appendix}

\SetKwInOut{Input}{Input}
\SetKwInOut{Output}{Output}

\usepackage{booktabs}

\newcommand{\gersi}[1]{\textcolor{red}{#1}}
\newcommand{\palash}[1]{\textcolor{green}{#1}}
\newcommand{\mm}[1]{\textcolor{magenta}{[#1]}}
\newcommand{\maeve}[1]{\textcolor{blue}{[#1]}}

\bibliographystyle{abbrvnat}

\newcommand{\Nats}{\mathbb{N}}

\DeclareMathOperator{\kl}{KL}
\DeclareMathOperator{\ER}{DR}
\DeclareMathOperator{\ess}{ess}
\DeclareMathOperator{\KL}{KL}
\DeclareMathOperator{\ext}{ext}
\DeclareMathOperator{\conv}{conv}
\DeclareMathOperator{\qvar}{QuickVaR}
\DeclareMathOperator{\ews}{EWS}
\newcommand{\quant}{{\mathfrak{q}}}

\renewcommand{\cite}{\citep}

\IncMargin{2em}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% THEOREMS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\title{Linear-time Algorithms for Risk Measures: \\
  VaR, CVaR, and TVaR}
%\author{Palash Agrawal, Gersi Doko, Maeve Burwell, Marek Petrik}
%\date{}

\begin{document}

\maketitle

\begin{abstract}
Monetary risk measures have gained popularity in expressing decision-makers' preferences for risk aversion. Value-at-Risk (VaR) and Conditional-Value-at-Risk (CVaR) in particular are used commonly for this purpose. This paper proposes new efficient algorithms for computing these risk measures for discrete random variable in expected linear time in the size of the discrete random variable. First, we propose a QuickVaR algorithm that computes the VaR of a discrete random variable. Then, we leverage QuickVaR to propose QuickDivergence, an algorithm that can compute a certain class of $\varphi$-divergence risk measures, such as the popular CVaR risk measure. The QuickVaR algorithm adapts the well-known Quickselect algorithm, while QuickDivergence builds on polymatroid optimization algorithms. Numerical results show that our new algorithms offer an order of magnitude speed up for large domains, and a library implementation of the algorithms is available at \todo{REDACTED for anonymity}.%\url{https://github.com/RiskAverseRL/RiskMeasures.jl}.     
\end{abstract}

\section{Introduction}\label{sec:intro}

Monetary measures of risk generalize the expectation operator to be able to represent the risk aversion of decision makers in domains that range from robotics~\cite{akella2024risk,majumdar2020should,benrabah2024review} to finance~\cite{Follmer2016} to infrastructure maintenance~\cite{Inzunza2016}, and disaster response~\cite{Barahona2013}. Value-at-Risk~(VaR) and Conditional-Value-at-Risk~(CVaR) are the most popular risk measures and have made inroads in reinforcement learning, machine learning, and decision making~\cite{Petrik2024}. Machine learning and reinforcement learning algorithms typically must compute the risk measures quickly for large discrete random variables. For example, in risk-averse reinforcement learning, one must evaluate the risk measure in every iteration of the algorithm which can represent a significant computational bottleneck~\cite{Ho2021,Ho2018,Ho2022c,Hau2023a,Hau2025}. 

In this paper, we propose linear time algorithms for computing risk measures of discrete random variables. First, we develop \emph{QuickVaR}, an algorithm that computes VaR in linear time and generalizes the well-known quickselect algorithm. Second, we develop \emph{QuickDivergence}, an algorithm that computes, in linear time, $\varphi$-divergence risk measures~\cite{Ahmadi-Javid2012} that satisfy a specific piecewise linearity property. This algorithm reduces $\varphi$-divergence risk measures to linear minimization problems over polymatroids~\cite{Follmer2016}. We illustrate this algorithm for well-known CVaR risk measure and a related TVaR risk measure. Both of this risk measures are in the $\varphi$-divergence family and satisfy the required linearity property.  

Our algorithms' time complexity improves on the state of the art by a logarithmic factor. Virtually all implementations of VaR and CVaR algorithms require sorting the input arrays. The sort step requires $\Omega(n \log n)$ operations where $n$ is the size of the probability space~\cite{Follmer2016}. Our numerical results show that the logarithmic acceleration can lead to an order of magnitude speed-up for realistic large problems without incurring any penalty for small problems. In addition, our linear-time algorithms have simple implementation with minimal added complexity over standard algorithms.

% Coherent risk measures, like CVaR, can be evaluated by solving convex optimization problems~\cite{Follmer2016, rockefeller1996variational}. Generic convex optimization algorithms, such as the interior point linear program solvers, are no faster than $O(n^3)$~\cite{Ho2021}. The $\Omega(n \log  n)$ time complexity of the sort dominates the time complexity and prevents linear time computation. In this paper, we circumvent the complexity of sorting by developing no-sort algorithms for these risk measures and others. 

Linear time algorithms that resemble our QuickVaR and QuickDivergence have been studies in many domains and we summarize some of the most relevant ones. Some example of such structured optimization problems are the projection onto the simplex~\cite{adam2019projectionscanonicalsimplexadditional, Wang2013Simplex}, projection on k-capped simplex~\cite{kcapped_ang,Lim2016}, linear optimization on the intersection of $L_p$ or $\varphi$-divergence balls and the probability simplex in robust optimizations~\cite{Ho2022}, optimization for $\varphi$-divergence risk measures~\cite{Ahmadi-Javid2012}, and on polymatroids~\cite{Korte2012}. While some of these papers discuss the possibility of designing linear-time algorithms, they usually do not study them with a few exceptions~\cite{Condat2016,Lim2016}. Finally, QuickVaR is closely related to the computation weighted quantile but returns the same value only when the quantile is unique~\cite{Korte2012,cormen2001introduction}.

The remainder of the paper is organized as follows. \Cref{sec:preliminaries} describes the necessary background including risk measures, $\varphi$-divergence, and polymatroid optimization. Then, \Cref{sec:var-linear} proposes \emph{QuickVaR} to compute VaR in linear time. \cref{sec:phi-divergence} builds on QuickVaR to develop \emph{QuickDivergence} that computes certain $\varphi$-divergence risk measures in linear time. We evaluate the algorithms numerically on synthetic and realistic problems in \cref{sec:experiments}.
%A tested implementation of the algorithms in Julia is available at \url{https://github.com/RiskAverseRL/RiskMeasures.jl}.

\section{Preliminaries}\label{sec:preliminaries}

\emph{Notation}: In this paper, we restrict our attention to random variables defined on finite probability spaces $(\Omega, 2^{\Omega}, \bm{p})$ where $\Omega := \left\{ 1, \dots , n \right\}$, $\bm{p} \in \Delta_n$, and $\Delta_n$ is the $n$-dimensional probability simplex. We use a tilde to indicate a random variable, such as $\tilde{x}\colon \Omega \to \Real$. Vectors are denoted with a lower-case bold font, such as $\bm{x} \in \Real^n$ and indexed as $\bm{x}_i \text{ for } i = 1, \dots , n$. We use the function notation $\tilde{x}$ and vector notation $\bm{x}$ interchangeably, such that $x_\omega = x(\omega), \forall \,  \omega \in \Omega$. Sets are denoted with calligraphic letters, $\bar{\Real} := \Real \cup \left\{ -\infty, \infty \right\}$ denotes the extended real line, and $\Real_{\ge 0}$ are non-negative reals. The symbol $\Nats$ denotes all 0-based natural numbers and $a{:}b := a, a+1, \dots , b$ for $a < b \in \mathbb{N}$. We use $\bm{x}_{a{:}b}$ to denote a subset of elements of $\bm{x}$ and comparisons such as $\bm{x}_{a{:}b} < y$ are interpreted element-wise. 

\emph{Monetary risk measures} $\rho\colon \Delta_n \times \Real^n \to \bar{\Real}$ are functions of probability measure and a random variable that are monotone and translation invariant~\cite{Follmer2016}. Risk measures generalize the expectation operator to account for different the uncertainty in the distribution of a random variable for a specific distribution. We apply risk measures to random variables representing rewards and suppose that higher risk values are preferable. For the sake of streamlining the notation, we omit the probability measure when it is obvious from the context and only write $\rho[\tilde{x}]$ to compute the risk value.

\emph{Value-at-Risk}~(VaR) is a popular risk measure and is defined for $\alpha\in [0,1)$ and $\tilde{x}\in \mathbb{R}^n$ as~\cite{Follmer2016,Shapiro2014}
\begin{equation}
  \label{eq:var-definition}
  \var{\alpha}{ \tilde{x} }
  \;:=\;
    \max\; \left\{\tau \in \bar{\Real} \mid \P{\tilde{x} < \tau}  \le \alpha\right\} =
    \max\; \left\{\tau \in \bar{\Real} \mid \P{\tilde{x} \ge  \tau}  \ge 1 - \alpha\right\},
\end{equation}
where the maximum exists because $\tau \mapsto \P{\tilde{x} < \tau}$ is lower semi-continuous. One can readily see that $\varo_0[\tilde{x}] = \ess \inf [\tilde{x}]$, and typically the risk measure is extended as $\varo_{1}[ \tilde{x} ] = \infty$.

\begin{table}
    \caption{Parameters for selected $\varphi$-divergence risk measures for $\alpha\in (0,1)$.} \label{tab:phi-divergence-risk}
    \centering
    \label{tab:divergences}
    \begin{tabular}{l|c|c|c}
         \toprule
         Risk measure & $\varphi(y)$  & $\varphi\opt(x)$ & $\beta$ \\
         \midrule        
         $\mathbb{E}$           & $\begin{cases} 0 &\text{if } y = 1 \\ \infty & \text{otherwise}  \end{cases}$ & $x$ & $0$  \\
      \midrule
      $\cvaro_{\alpha}$     & $\begin{cases} 0 &\text{if } y \in [0, \frac{1}{\alpha}] \\ \infty & \text{otherwise}  \end{cases}$ & $\frac{1}{\alpha} \cdot \max \left\{ 0,  x \right\} $& $0$ \\
      \midrule
      $\evaro_{\alpha}$     & $\begin{cases} y \log y &\text{if } y > 0 \\ 0 &\text{if } y =  0 \\  \infty &\text{if } y < 0 \end{cases}$ & $\exp{ x - 1 }$ & $\log \frac{1}{\alpha}$ \\
      \midrule
         $\tvaro_{\alpha}$     & $|y - 1|$ & $ \begin{cases} x &\text{if } |x| \le 1\\ \infty &\text{otherwise} \end{cases} $& $\sqrt{ 2 \log 2 \cdot \log \frac{1}{\alpha} }$ \\
         \bottomrule
    \end{tabular}
\end{table}

A \emph{$\varphi$-divergence risk measure} $\ER_{\varphi, \beta }\colon \mathbb{R}^n \to  \bar{\Real}$ is defined for $\varphi\colon \bar{\Real} \to \bar{\Real}$ and $\beta\in \Real_{\ge 0}$ as~\cite{Ahmadi-Javid2012}
\begin{align} \label{eq:fdiv-primal}
  \ER_{\varphi, \beta}[ \tilde{x} ]
  &:= \inf \left\{\bm{q}\tr \bm{x} \mid \bm{q}\in \Delta_n, H_{\varphi}(\bm{q} \| \bm{p}) \le \beta
    %, \bm{q} \ll \bm{p}
    \right\} \\
  \label{eq:fdiv-dual}
  &= \sup \left\{  t \cdot v - t\cdot \E { \varphi\opt \left(- \frac{\tilde{x}}{t} + v + \beta \right)} \mid  v \in \Real, \,  t> 0 \right\},
\end{align}
where $\varphi\opt(x)$ is the convex conjugate of $\varphi(y)$, $\varphi$ is a closed, convex function that satisfies $\varphi(1) = 0$, and $H_{\varphi}$ is a $\varphi$-divergence measure~\cite{Bayraksan2015}:
\[
  H_\varphi(\bm{q} \| \bm{p}) := \sum_{\omega \in \Omega} p_{\omega} \cdot \varphi\left(\frac{q_{\omega}}{p_{\omega}}\right).
\]
We adopt the common convention that $1 / 0 = \infty$ and $0 \cdot \infty = 0$~\cite{rockefeller1996variational} which makes it possible to drop the absolute continuity restriction used in some definitions of $\ER$. It is important to note that $\varphi$-divergence risk measures are coherent, which means they are convex and positively homogeneous. Any coherent risk measure $\rho\colon \mathbb{R}^n\to \bar{\Real}$ satisfies for some closed convex non-empty set $\mathcal{Q} \subseteq \Delta_n$ \citep[Prop. 2.84]{Follmer2016}. 

\Cref{tab:phi-divergence-risk} summarizes several common $\varphi$-divergence risk measures. We focus on the \emph{Conditional-Value-at-Risk}~(CVaR), which is a $\varphi$-divergence risk measure and defined equivalently for $\tilde{x} \in \mathbb{R}^n$ and $\alpha \in (0,1]$ as~\cite{Follmer2016}
\begin{equation} \label{eq:primal_cvar}
    \cvaro_\alpha(\tilde{x})
  := \max_{z \in \Real} \left(z - \frac{1}{\alpha} \cdot \E{[z -\bm{x}]_+}\right),
\end{equation}
and extended continuously to $\cvaro_0[\tilde{x}] = \ess\inf[\tilde{x}]$. We also study another $\varphi$-divergence risk measure, which we call \emph{Total-Value-at-Risk}~(TVaR). The importance of TVaR is that it bounds the common Entropic-Value-at-Risk~(EVaR)~\cite{Ahmadi-Javid2012,Hau2023a,Su2025} from below as we show next. 
\begin{proposition}
For each $\alpha\in [0,1]$ and $\tilde{x} \in \mathbb{R}^n$:
\[
  \var{\alpha}{\tilde{x}}
  \; \ge\; 
  \cvar{\alpha}{\tilde{x}}
  \; \ge\; 
  \evar{\alpha}{\tilde{x}}
  \; \ge\; 
  \tvar{\alpha}{\tilde{x}}.
\]
\end{proposition}
\begin{proof}
  \mm{We need a proof for TVaR, the blurb here is not a proof.
  }The proof follows directly from Chapter 2 of \cite{CoverThomas1991Elements} and \cite{Ahmadi-Javid2012}. Note the proof in \cite{Ahmadi-Javid2011} is in terms of costs while our risk measures are in terms of rewards.
\end{proof}

% In the remainder of the paper, we first propose a linear-time algorithm for computing VaR then we build on it to propose a linear time algorithm for certain $\varphi$-divergence risk measures. 

\section{Computing VaR in Linear Time} \label{sec:var-linear}

We now describe the QuickVaR algorithm, show that it computes VaR, and show that it runs in average linear time. 

The standard VaR algorithm for a discrete random variable $\bm{x}\in \Real^n$ with distribution $\bm{p}\in \Delta_n$ and risk level $\alpha$ proceeds as follows~(e.g.,~listing~6.25 in \cite{privault2026notes}). It sorts the entries of $X$ to get a permutation $\sigma \colon 1{:}n \to 1{:}n$ such that $i \mapsto x_{\sigma(i)}$ is monotone. Then, it finds an index $i \in 1{:}n$ such that 
\[
  \sum_{j = 1}^{i-1} p_{\sigma(j)} 
  \; <\;
  \alpha
  \; \le\;
  \sum_{j = 1}^i p_{\sigma(j)}.
\]
The value $x_{\sigma(i)}$ is the optimal solution in~\eqref{eq:var-definition} by  \cref{thm:quantile-in-finite-omega} below. The computational complexity of this algorithm is $\Omega(n \log  n)$ because its runtime is dominated by the need to sort the entries of $\bm{x}$. For the sake of completeness, we give a formal definition of the standard algorithm in \cref{alg:slow-quantile} in the appendix.

Our \emph{QuickVaR} algorithm, described in \cref{alg:quick-quantile}, computes the VaR in $O(n)$ time by avoiding the need to sort the elements of the input random variable $\bm{x}$. In particular, QuickVaR partially sorts the indexes of $\bm{x}$, analogous to the Quickselect algorithm~\cite{horowitz1998computer}. 

\Cref{alg:quick-quantile} is a recursive algorithm that works for a random variable $\bm{x}$ with a distribution $\bm{p}\in \Delta_n$ as follows. In addition to the random variable, the input is $f$ (front) and $b$ (back), which delimit the indices of $\bm{x}$ it manipulates.  The algorithm  first picks a pivot element $v$ randomly between $b$ and $e$. Choosing the pivot randomly is essential to achieve an average linear time complexity. Given a pivot, we use $\operatorname{partition}$ to partially sort the entries of $\bm{x}$ into three groups: 1. less than $v$: $\bm{x}_{b{:}(l-1)} < v$, 2. equal to $v$: $\bm{x}_{l{:}g} = v$, and 3. greater than $v$: $\bm{x}_{(g+1){:}e} > v$. Then, the algorithm checks for the conditions of \cref{thm:quantile-in-finite-omega} to determine which of the three groups contains the VaR value. Finally, if the VaR value is in group 1 or 3, the algorithm calls itself recursively with adjusted $b$, $ e$, and $\alpha$.

The $\operatorname{partition}$ procedure in \cref{alg:quick-quantile} uses the Dutch national flag algorithm to partition the input array into the three groups. The algorithm runs in linear time, and the properties of its output are given in \cref{lem:hoare-partition} below. For the sake of completeness, we provide the reader with the algorithm in \cref{alg:partition} in \cref{sec:stand-var-algor}. 

We present \cref{alg:quick-quantile} recursively to simplify analysis. However, a loop-based, non-recursive implementation typically performs better in imperative languages. For such an equivalent version, see \cref{alg:quickvar-nonrecursive} in \cref{sec:stand-var-algor}.

\begin{algorithm}
  \caption{\texttt{QuickVaR}: Recursive VaR algorithm} \label{alg:quick-quantile}
  \Input{$\bm{x} \in \Real^n$, $\bm{p} \in \Delta_n$, $\alpha \in [0,1)$, $f, b \in 1{:}n$ such that $f \le  b$ and $\sum_{j=f}^b p_j > \alpha$}
  \Output{$\varo_{\alpha}[\tilde{x}]$}
  $v \gets x_{\operatorname{rand}(f{:}b)}$ \tcp*{Pivot}
  $l, g, \bm{x}', \bm{p}' \gets \operatorname{partition}(\bm{x}, \bm{p}, v, f, b)$
  \tcp*{~\cref{alg:partition}}
  \label{ln:t}
  $t \gets \sum_{j=f}^{l-1} p'_j$ \tcp*{$t = \P{\tilde{x} < v} - \P{\tilde{x} < x'_f}$}
  \label{ln:e}
  $e \gets \sum_{j=l}^{g} p'_j$  \tcp*{$e = \P{\tilde{x} = v}$}
  \label{ln:firstif}
  \If(\tcp*[f]{$\alpha + \P{\tilde{x} < x'_f} < \P{\tilde{x} < v}$}){$\alpha < t$}{ 
    \KwRet{$\qvar(\bm{x}', \bm{p}', \alpha, f, l-1)$}
  }
  \label{ln:secondif}
  \ElseIf(\tcp*[f]{$\P{\tilde{x} \le v} \le \alpha + \P{\tilde{x} < x'_f}$}){$t + e \le \alpha$}{
    \KwRet{$\qvar(\bm{x}', \bm{p}', \alpha - t - e, g+1, b)$}
  }
  \label{ln:thirdif}
  \lElse{
    \KwRet{$v$}
  }
\end{algorithm}


The following theorem states the correctness of QuickVaR.
\begin{theorem} \label{thm:var-correct}
\Cref{alg:quick-quantile} executed with $b =
1$ and $e = n$ and $\alpha\in [0,1)$ returns $\varo_{\alpha}[ \tilde{x} ]$.
\end{theorem}

Before proving \cref{thm:var-correct}, we need to show several important lemmas. The following lemma shows that the VaR value is the probability atom that satisfies specific probability properties. 
\begin{lemma}\label{thm:quantile-in-finite-omega}
Assuming $\alpha\in [0,1)$ and a discrete $\tilde{x}$, there exists $\omega\in\Omega$ with $\var{\alpha }{\tilde{x}} = x_{\omega}$ and
\begin{equation} \label{eq:var-property-prob}
  \P{\tilde{x} < v} \le \alpha < \P{\tilde{x} \le v}. 
\end{equation}
\end{lemma}
\begin{proof}
First, we show that any $v$ that satisfies \eqref{eq:var-property-prob} solves \eqref{eq:var-definition} optimally. The implication $\P{\tilde{x} < v} \le \alpha \implies v \in \left\{ t \in \Real \mid  \P{ \tilde{x} < t } \le \alpha \right\}$ follows immediately. Second, we prove $\alpha < \P{ \tilde{x} \le v } \implies \forall t \in \left\{ t \in \Real \mid  \P{ \tilde{x} < t } \le \alpha \right\}, v \ge t$ by contradiction. Suppose that $\exists t \in \left\{ t \in \Real \mid  \P{ \tilde{x} < t } \le \alpha \right\}, v < t$. Then, by the monotonicity of the probability operator, we get a contradiction as
 \[
   \alpha
   < \P {  \tilde{x} \le v  }
   \le \P {  \tilde{x} < t  }
   \le \alpha.
 \]
Then, the existence of $\omega$ follows from \cref{lem:prob-lumpy} in \cref{sec:prob-discr-rand} because \eqref{eq:var-property-prob} implies that 
  \[
    \P{\tilde{x} =
      \var{\alpha }{\tilde{x}}} = \P{\tilde{x} \le \var{\alpha }{\tilde{x}}}  - \P{\tilde{x} < \var{\alpha }{\tilde{x}}} > \alpha - \alpha = 0.
  \]
\end{proof}

The following lemma proves the properties of the $\operatorname{partition}$ operator defined in \cref{alg:partition}. 
\begin{lemma} \label{lem:hoare-partition}
If $(l, g, \bm{x}', \bm{p}') = \operatorname{partition}(\bm{x'}, \bm{p'}, v, f, b)$ for $f < b$, then:
\begin{align} \label{eq:hoare-correspond}
  \bm{x}_{f{:}(l-1)} < v, \qquad
  \bm{x}_{l{:}g} = v,  \qquad
  \bm{x}_{(g+1){:}b} > v.
\end{align}
Moreover, $ \bm{x}_{f{:}b} = \bm{x'}_{\sigma(f{:}b)}, \bm{p} = \bm{p'}_{\sigma(f{:}b)}$ for some permutation $\sigma\colon f{:}b \to f{:}b$, and $\bm{x}_{1{:}(b-1)} = \bm{x'}_{1{:}(b-1)}, \bm{p}_{1{:}(b-1)} = \bm{p'}_{1{:}(b-1)}$, and $\bm{x}_{(e+1):n} = \bm{x'}_{(e+1):n}, \bm{p}_{(e+1):n} = \bm{p'}_{(e+1):n}$. The algorithm runs in $O(b - f + 1)$ time.
\end{lemma}
\Cref{lem:hoare-partition} is a standard property of the Dutch national flag algorithm, and we outline its proof in \cref{sec:stand-var-algor} for the sake of completeness.

The following lemma analyzes the recursive property of QuickVaR and is the main building block of \cref{thm:var-correct}.
\begin{lemma} \label{lem:quickvar-correct}
If input conditions of \cref{alg:quick-quantile} hold and $\bm{x} \in \Real^n$ satisfies that
 \begin{equation} \label{eq:qvar-precondition}
  \mathbf{x}_{1{:}(f-1)} < x_f, \qquad
  x_f \le \bm{x}_{f{:}b} \le x_b,  \qquad
  x_b < \bm{x}_{(b+1){:}n},
\end{equation} 
then \cref{alg:quick-quantile} returns $x_{\omega}, \omega\in f{:}b$ such that
\begin{equation} \label{eq:qvar-postcondition}
  \P{\tilde{x} < x_{\omega}}
  \quad \le \quad
  \alpha + \P{\tilde{x} < x_f}
  \quad < \quad \P{\tilde{x} \le x_{\omega}}. 
\end{equation}
\end{lemma}
\begin{proof}
From \eqref{eq:qvar-precondition} and \cref{lem:hoare-partition}, $t$ and $e$ on lines~\ref{ln:t} and \ref{ln:e} satisfy that
\begin{equation} \label{eq:te-equivalence}
  t = \P{\tilde{x} < v} - \P{\tilde{x} < x'_f}, \qquad e = \P{\tilde{x} = v}
\end{equation}
We proceed by induction on $k = b - f$. \emph{Base case}: $k = 0$. Since $b = f$, we have that $v = x_b$ and $l = g = b$, $t = 0$, $e = p_b$, and $\alpha  \ge t$. The input condition implies that $t + e = p_b > \alpha$. Because $t \le \alpha < p_b$, the algorithm returns $v$ on line~\ref{ln:thirdif} that satisfies
\[
 \P{\tilde{x} < v} \le \alpha + \P{\tilde{x} < x_f} < \P{\tilde{x} \le v}. 
\]
Above, the first inequality holds from $x = x_f$ and $0 \le \alpha$. The second inequality holds from the algorithm's input condition.  

\emph{Inductive step}. Suppose that $k>0$ and that the lemma holds for all $k' < k$. We now analyze the following three cases.

(i) If $\alpha < t$, the algorithm executes the first if statement on line~\ref{ln:firstif}. Because $0 \le \alpha  < t$, the recursive call satisfies the required input conditions: $f \le l-1$ and $\sum_{j=f}^{l-1} p_j = t > l$. The recursive call also satisfies \eqref{eq:qvar-precondition} by \cref{lem:hoare-partition}, and we can conclude that \eqref{eq:qvar-postcondition} holds from the inductive hypothesis because $l-1 - f \le b-1 -f \le k$.

(ii) If $t + e \le \alpha$, the algorithm executes the second if statement on line~\ref{ln:secondif}. We show that the input conditions for the recursive call are met. Because $t + e = \sum_{j = f}^g p_j \le \alpha < \sum_{j=f}^b p_j$  (the second inequality is the input condition), it must be that $g < b$ and therefore $g+1 \le b$. Also, by the algorithm's input condition and algebraic manipulation, we get that
\[
 \sum_{j=g+1}^b p_j = \sum_{j=f}^b - t -e > \alpha  -t -e,  
\]
and the input conditions hold.  Because \eqref{eq:qvar-precondition} holds from \cref{lem:hoare-partition} and $b - g - 1 < b-f \le  k$, the inductive hypothesis implies that there exists $x_{\omega}$ such that
\[
  \P{\tilde{x} < x_{\omega}}
  \quad \le \quad
  (\alpha - t - e) + \P{\tilde{x} \le v}
  \quad < \quad \P{\tilde{x} \le x_{\omega}}. 
\]
Then, \eqref{eq:qvar-postcondition} follows by algebraic manipulation after substituting $t,e$ from \eqref{eq:te-equivalence}.

(iii) If $t \le \alpha < t + e$, the algorithm executes the else statement on line~\ref{ln:thirdif}, and the inequality \eqref{eq:qvar-postcondition} holds immediately by substituting $t,e$ from \eqref{eq:te-equivalence}.
\end{proof}

We are now ready to prove the correctness of QuickVaR.
\begin{proof}[Proof of \cref{thm:var-correct}]
  With $b = 1$ and $e = n$, the condition in \eqref{eq:qvar-precondition} is satisfied since $\alpha\in [0,1)$. Thefore, \cref{lem:quickvar-correct} shows that QuickVaR returns $x_{\omega}$ that satisfies
  \[
  \P{\tilde{x} < x_{\omega}}
  \quad \le \quad
  \alpha 
  \quad < \quad \P{\tilde{x} \le x_{\omega}}, 
\]
which satisfies \eqref{eq:var-property-prob} and therefore equals to the VaR value by 
  \cref{thm:quantile-in-finite-omega}. 
\end{proof}

With the correctness established above, we are ready to analyze the computational complexity of QuickVaR. 
\begin{theorem} \label{prop:time-complexity}
\Cref{alg:quick-quantile} runs in expected $O(n)$ time.
\end{theorem}
\begin{proof}
The proof is analogous to the standard quickselect algorithm, as in \citep[theorem~3.3]{horowitz1998computer}, except that we need to keep track of the risk level $\alpha$. Fix some $n \in \mathbb{N}$, $\bm{x}$, and $\bm{p}$ and we study the properties. Let $T\colon 1{:}n \to [0,\infty]$ be the \emph{expected} runtime of \cref{alg:quick-quantile} such $T(k)$ is the expected runtime when executed with $k := b - f + 1$. To simplify the notation, we assume without loss of generality in the remainder of the proof that $f = 1$ and $e = k$. Then, let
\[
  l := \left| \left\{ i \in 1{:}k \mid  x_i < \varo_{\alpha}[\tilde{x}]\right\}   \right|
\]
Note that by the assumption $\sum_{j = f}^b p_j > \alpha$, we have that $k \ge 1$ and $\varo_{\alpha}[\tilde{x}] \le \max_{i\in f{:}b} x_i$. Therefore, $l \in 0{:}(k-1)$. The algorithm terminates in constant time when $k = 1$, and we upper-bound the runtime for the worst-case choice of $l$ for $k > 1$ as
\begin{align}
\nonumber
  T(k)
  &\le d\cdot k + \max_{l\in 0{:}(k-1)} \frac{1}{k} \sum_{i=0}^{k-1} \left(   T(i) \cdot 1_{l < i} + T(k - i - 1) \cdot 1_{l > i}  \right) \\
\nonumber
  &= d\cdot k + \max_{l\in 0{:}(k-1)} \frac{1}{k}  \left(  \sum_{i=l+1}^{k-1} T(i)  + \sum_{i=0}^{l-1} T(k - i - 1)   \right) \\
\label{eq:quickvar-complexity}
  &= d\cdot k + \max_{l\in 0{:}(k-1)} \frac{1}{k}  \left(  \sum_{i=l+1}^{k-1} T(i)  + \sum_{i=l}^{k-1} T(i)   \right).
\end{align}
where $d\cdot k$ is the local runtime of the algorithm for some $d > 0$ and $T(i)$ and $T(k - i - 1)$ are the average runtimes of the recursive calls. The average over $i$ corresponds to the uniformly-random selection of the pivot with $i = \left| \left\{ j \in 1{:}k \mid x_i < v \right\} \right|$ in the first line of the inequality. The last two equalities follow by algebraic manipulation. Then, an argument identical to \cite[theorem~3.3]{horowitz1998computer}, since \eqref{eq:quickvar-complexity} is the same as equation (3.8) in \cite{horowitz1998computer}, shows that \(T(k)\le 4\cdot d\cdot k\) for all \(k\ge 1\).
\end{proof}

\begin{remark}
  An attentive reader may notice that QuickVaR resembles the algorithm for computing the weighted median~\cite[section~17]{Korte2012}. However, the weighted median algorithm does not, in general, compute VaR. Define the \emph{quantile} $\quant_{\alpha},\alpha \in (0,1)$ for $\tilde{x}$ as the closed set
\[
 \quant_{\alpha}[\tilde{x}] := \left\{ z\in \Real \mid  \P{ \tilde{x} \le z } \ge \alpha , \P{ \tilde{x} \ge z } \ge 1-\alpha  \right\},
\]
with bounds $\quant^+_{\alpha}[\tilde{x}] := \max \quant_{\alpha}[\tilde{x}]$ and $\quant^-_{\alpha}[\tilde{x}] := \min \quant_{\alpha}[\tilde{x}]$. One can readily show that $\varo_{\alpha }[\tilde{x}] = \quant^+_{\alpha}[\tilde{x}]$. Although the precise definition of a weight median varies, it is typically a value in $\quant_{\frac{1}{2}}[\tilde{x}]$. Because $q^+_{\alpha}[\tilde{x}] \neq q^{-}_{\alpha}[\tilde{x}]$, in general, VaR also does not equal the median. A careful analysis shows that the weighted median algorithm in~\citet[section~17]{Korte2012} computes $\quant^-_{\frac{1}{2}}[\tilde{x}]$, it does not equal to $\varo_{\frac{1}{2}}[\tilde{x}]$.
\end{remark}

\section{Computing \texorpdfstring{$\varphi$}{phi}-divergence Risk Measures in Linear Time} \label{sec:phi-divergence}

In this section, we describe and analyze QuickDivergence, a new linear-time framework for computing a certain class of $\varphi$-divergence risk measures, such as CVaR and TVaR. QuickDivergence treats risk evaluation as a linear optimization over a polymatroid. Standard polymatroid optimization algorithms must sort their input which requires $\Omega(n \log  n)$ runtime. We identify a structural condition of risk measure polymatroids that makes it possible to avoid sorting and instead use the linear-time QuickVaR algorithm described in \cref{sec:var-linear}.

The remainder of the section is organized as follows. \Cref{sec:quickdivergence} describes a general linear-time algorithm suitable for $\varphi$-divergence risk measures that satisfy a EWS property. Then \cref{sec:cvar-tvar-are} shows that both CVaR and TVaR satisfy the EWS property and can be computed in linear time. 

\subsection{QuickDivergence} \label{sec:quickdivergence}
We first recall the definition of a submodular function~\cite[section~2.1]{Korte2012} and a polymatroid~\cite[definition~14.9]{Korte2012}.
\begin{definition} \label{def:polymatroid}
A function $f \colon 2^{1{:}n} \to \Real_{\ge 0}$ is \emph{submodular} if
\[ 
  f(\mathcal{A}) + f(\mathcal{B}) \geq f(\mathcal{A} \cup \mathcal{\mathcal{B}}) + f(\mathcal{A} \cap \mathcal{B}),
  \quad
  \forall \mathcal{A}, \mathcal{B} \subseteq 1{:}n.
\]
For a submodular $f \colon 2^{1{:}n} \to \Real_{\ge 0}$, a \emph{polymatroid} $\mathcal{Q}_f$ is a polytope defined as
  \[
    \mathcal{Q}_f
    \;:=\;
    \left\{ \bm{q} \in \Real_{\ge 0}^n  \mid   \bm{1}\tr \bm{q}_{\mathcal{U}} \leq f(\mathcal{U}), \, \forall \,  \mathcal{U} \subseteq 1{:}n \right\},
\]
and a \emph{normalized polymatroid} is the intersection with the probability simplex: $\widehat{\mathcal{Q}}_f := \mathcal{Q}_f \cap \Delta_n $.
\end{definition}

Polymatroids are useful in our setting because any coherent, comotonic monetary risk measure can be formulated as linear optimization over a polymatroids~\cite[corollary~4.89]{Follmer2016}. A risk measure $\rho$ is \emph{coherent} if it is concave and positively homogeneous.  A risk measure $\rho$ is \emph{comotonic} is for any comonotone $\tilde{x}, \tilde{y}$: $\rho[ \tilde{x} + \tilde{y} ] = \rho[ \tilde{x} ] + \rho[ \tilde{y} ]$, where the random variables $\tilde{x}, \tilde{y} \in \Real^{1{:}n}$ are \emph{comonotone} if $\tilde{x}(i) \ge \tilde{x}(j) \Leftrightarrow \tilde{y}(i) \ge \tilde{y}(j)$ for each $i,j\in 1{:}n$.

\begin{proposition}[corollary~4.8a in \citet{Follmer2016}] \label{prop:coherent-polymatroid}
Suppose that $\rho\colon \Delta_n \times  \Real^n \to \Real$ is a coherent comotonic risk measure. Then
\begin{equation}\label{eq:comonotonic-optimization}
  \rho[\tilde{x}] = \min_{\bm{q}\in \widehat{\mathcal{Q}}_c} \bm{q}\tr  \bm{x},
  \qquad
  \text{where}
  \qquad
  c(\mathcal{A}) := -\rho[ -\tilde{1}_{\mathcal{A}} ], \quad \forall  \mathcal{A} \subseteq 1{:}n,
\end{equation}
where $\tilde{1}$ is an indicator random variable. Moreover, if the risk measure is law-invariant then there exists $g\colon \Real \to \Real$ such that $c(\mathcal{A}) = g(\bm{1}\tr \bm{p}_{\mathcal{A}})$ for each $\mathcal{A} \subseteq 1{:}n$.
\end{proposition}

The standard solution to the polymatroid optimization problem in \eqref{eq:comonotonic-optimization} is to use a greedy algorithm, such as \cref{alg:greedy-polymatroid}. This algorithm adapts the standard polymatroid optimization, such as \citet[section~14.9]{Korte2012}, to the normalized polymatroid $\widehat{\mathcal{Q}}_f$. See \cref{prop:standard-polymatroid-works} for the proof of correctness. Note that the greedy approach in \cref{alg:greedy-polymatroid} requires sorting and, therefore, runs in $\Omega(n \log  n)$ time.

\begin{algorithm}
    \caption{Polymatroid greedy minimization.} \label{alg:greedy-polymatroid}
    \Input{submodular monotone function $f \colon  2^{1{:}n} \to \Real$, and an objective $\bm{x}\in \Real^n_{\ge 0}$}
    \Output{$\bm{q}\opt \in \arg\min_{\bm{q}\in \widehat{\mathcal{Q}}_f} \bm{x}\tr \bm{q}$}
    Use sort to get
    $\sigma \colon 1{:}n \to 1{:}n$ such that
    $i > j \implies x_{\sigma(i)} \ge  x_{\sigma(j)}, \; \forall i, j \in 1{:}n$\;
     \For{$i\in 1{:}n$}{
       $q\opt_{\sigma(i)} \gets f\left(1{:}i\right) - f\left(1{:}(i-1)\right) $
    }
    \Return{$\bm{q}\opt$}
\end{algorithm}

We now define a special class of submodular functions, which we call EWS, which make sorting is unnecessary when optimizing a linear function over a polymatroid.
\begin{definition} \label{asm:submodular-function}
A function $f\colon 2^{1{:}n} \to \Real_{\ge 0}$ is $\ews(\bm{\mu} ,m, c)$, or \emph{Element-Wise-Separable}, for $c \in \Real_{\ge 0}$,  $m\in \Real_{>0}$, such that $c + m \ge 1$, and  $\bm{\mu}\in \Real^n_{\ge 0}$ if $f(\mathcal{O}) = g(\bm{1}\tr \bm{\mu}_{\mathcal{O}}), \forall \mathcal{O} \subseteq \Omega$ where for each $x \in [0,1]$:
\[
    g(x) :=
    \begin{cases}        
    \min \{c + m  \cdot x, 1\} &\text{if } x > 0 \\
    0 &\text{otherwise}.
    \end{cases}
\]
\end{definition}

\begin{proposition}
Any EWS function $f\colon 2^{\Omega} \to  \Real_{\ge 0}$ is submodular, monotone, $f(\emptyset) = 0$, and $f(\Omega) = 1$.   
\end{proposition}
\begin{proof} 
  From \cref{lem:submodular-concave}, it is sufficient to show that $g$ is concave to show that  $f$ is submodular. The function $g$ is concave on the interval $(0, 1]$ because it is the minimum of two linear functions~\cite{boyd2004convex}. The concavity of $g$ on $[0, 1]$ follows from the definition. Since $g(0) = 0$, for each $\lambda \in (0,1]$ and $z \in ( 0, 1]$ we get that
\begin{align*}
  (1 - \lambda)g(0) + \lambda \cdot g(b)
  &= \lambda \cdot g(b)
  = \min\left\{\lambda  \cdot c + m\cdot \lambda\cdot b, \lambda\right\} \\
  &\leq \min \left\{ c + m \cdot \lambda \cdot b, 1 \right\}
  = g(\lambda \cdot  b) = g\left( (1 - \lambda)\cdot 0 + \lambda\cdot b\right).
\end{align*}
Because the inequality holds also for $\lambda = 0$, $g$ is concave by the definition. The monotonicity, non-negativity and the boundary value conditions for $f$ follow immediately by algebraic manipulation from the assumptions on $c$ and
$m$. 
\end{proof}

\begin{algorithm}
  \caption{\texttt{QuickDivergence}: Algorithm for optimizing linear function on polymatroids} \label{alg:maximum-weight-independent}
  \Input{$\bm{x} \in \Real^n$, $\bm{p} \in \Delta_n$, $\ews(\bm{p}, m, c)$ function $f \colon 2^{1{:}n} \to \Real_{\ge 0}$}
  \Output{$\min \left\{ \bm{q}\in \Real^n  \mid  \bm{q} \in \mathcal{Q}_f, \bm{1}\tr \bm{q} = 1\right\}$}
  Choose $k_{\min} \in \arg\min_{i \in  1{:}n} x_i$ breaking ties by choosing the minimal $i$\;
  $v \gets \operatorname{QuickVaR}(\bm{p}, \bm{x}, \alpha = (1 - c)/m)$ \;
  $\mathcal{K}_{< v} \gets \{ i \in  1{:}n \mid x_i < v \}$ ; ~
  $\mathcal{K}_{= v} \gets \{ i = 1{:}n \mid x_i = v \}$ \;
  $\theta \gets \left( 1 - c + m\cdot \sum_{k = \mathcal{K}_{< v}}  p_k\right)  $ \;
  \For{$i \in 1{:}n$}{
   \lIf{$i = k_{\min}$}{$q\opt_i \gets m\cdot p_i + c$}
   \lElseIf{$ i \in \mathcal{K}_{< v} \wedge i \ne k_{\min} $} {$q\opt_i \gets m \cdot p_i$}
   \lElseIf{$ i \in \mathcal{K}_{=v} \wedge i \neq k_{\min}$} {$q\opt_i \gets \min \left\{  \theta, m \cdot p_i \right\} $; $\theta\gets \theta - q\opt_i$}
   \lElse{$q\opt_i \gets 0$}
  }
  \Return{$\bm{q}\opt$}
 \end{algorithm}

\cref{alg:maximum-weight-independent} describes the \emph{QuickDivergence} approach to polymatroid optimization. The main difference from the standard linear polymatroid algorithm in \cref{alg:greedy-polymatroid} is that it does not require sorting the input vector. Instead, the algorithm is able to use QuickVaR to only sort the input vector partially. The following theorem states the correctness and runtime of \cref{alg:maximum-weight-independent}.
\begin{theorem}
\Cref{alg:maximum-weight-independent} solves~\eqref{eq:comonotonic-optimization} in expected $O(n)$ runtime. 
\end{theorem}
\begin{proof}
  First, to prove the correctness of \cref{alg:maximum-weight-independent}, assume that it returns $\bm{q}\opt$. We now show that $\bm{q}\opt$ coincides, up to arbitrarily breaking of ties, with $\hat{\bm{q}}$ computed by the standard polymatroid optimization in \cref{alg:greedy-polymatroid}. Suppose that \cref{alg:greedy-polymatroid} chooses a permutation $\sigma\colon  1{:}n \to 1{:}n$ is such that $i \le j \Rightarrow x_{\sigma(i)} \le x_{\sigma(j)}$ and $x_{\sigma(i)} = x_{\sigma(j)} \wedge i \le j \Rightarrow \sigma(i) \le \sigma(j)$ for each $i,j\in 1{:}n$. Intuitively, the conditions on $\sigma$ states that it was generated by a stable sort algorithm. We emphasize that the stability assumption is only needed for our proof technique and the actual implementation does not require it.

We now prove that $q\opt_i = \hat{q}_i$ for each $i\in 1{:}n$ by separately analyzing the following three cases.
  \emph{(i)} Suppose that $i = k_{\min}$. Then the equality holds immediately from the construction: $q\opt_i = f($ \mm{resume here}
  \emph{(ii)} Suppose that $i \in \mathcal{K}_{<v} \wedge i \neq k_{\min}$.
  \emph{(iii)} Suppose that $i \in \mathcal{K}_{=v} \wedge i \neq k_{\min}$.
  
The optimal solution will always be $q_{i+1} = f(\mathcal{O}_{i+1}) - f(\mathcal{O}_{i})$ \cite[Remark 4.86]{Follmer2016}, where $\mathcal{O}_i$ is the set of all elements that have only values less than or equal to the i-th largest element:
  \[
   \mathcal{O}_i := \left\{1, \dots ,i  \right\}, \quad \forall  i = 1, \dots, n,
 \]
 and $\mathcal{O}_{0 } := \emptyset$. For $f(\mathcal{O}_{i+1}) \leq 1$ and $f(\mathcal{O}_i) < 1$ and $\mathcal{O}_i \neq \emptyset$ \mm{???}
\begin{align*}
    f(\mathcal{O}_{i + 1}) - f(\mathcal{O}_i) &= c  +m \cdot \sum_{k = 1}^{i + 1}p_{k} - (c  +m \cdot \sum_{k = 1}^{i}p_{i})\\
    &= m \cdot p_{i+1}.
\end{align*}
We know from \eqref{eq:var-definition} that $v$ is the largest $\tau$ such that $P[x < \tau] \leq \frac{1 - c}{m}$. 
Thus, for any $i$ such that $x_i < z$ and $i \neq j$, $(c  +\sum_{j = 1}^{i}m \cdot p_j) \leq 1$
and thus $q_i$ must equal $m*p_i$.

  




For $f(\mathcal{O}_{i+1}) \leq 1$ and $f(\mathcal{O}_i) = 0$


\begin{align*}
    f(\mathcal{O}_{i + 1}) - f(\mathcal{O}_i) &= c  +\sum_{k = 1}^{i + 1}m\cdot p_{k}
\end{align*}

Thus, for any $i$ such that $x_i < z$ and $i = j$, $q_i$ must equal $m*p_i + c$.




For all other $i$ such that $x_i> z$, we note that $P[x < \tau] > \frac{1 - c}{m}$ indicates $f(\mathcal{O}_{i}) - f(\mathcal{O}_{i - 1}) = 1 - 1 = 0$, and thus $q_i = 0$.

For all other $i$ such that $x_i=z$, since all $q_i$ must sum to 1, it can be seen for all such $i$, $\sum_{k : x_k =  v}p_k$ must equal $y$. The solution of the LP is not affected by which $i$ this sum is distributed, so we set $x_z$ to $y$ and everything else to 0. 

The quick quantile call runs in expected linear time complexity as shown in \cref{prop:time-complexity}. After the call to QuickQuantile, the number of operations is bounded by the number of elements in $x_0$ and $p_0$ and thus is linear time as well. 
\end{proof}

\subsection{CVaR and TVaR are EWS} \label{sec:cvar-tvar-are} 
We now prove that CVaR can be represented as a polymatroid with a submodular function that satisfies Element-Wise-Seperability as defined above. Therefore we can provably compute CVaR in expected linear time.

\begin{proposition}
Let $f\colon 2^{\Omega} \to \Real_{\ge 0}$ be defined as
\[
  f(\mathcal{O})
  :=
  \min\left\{\frac{1}{\alpha} \cdot \P{\mathcal{O}}, 1\right\},
  \quad \forall \mathcal{O} \subseteq \Omega .
\]
Then
\begin{equation} \label{eq:cvar-correct}
\cvar{\alpha }{\tilde{x} } = \max \left\{\bm{x}\tr \bm{q} \mid  \bm{q} \in \mathcal{Q}_f\right\}.
\end{equation}
and $f$ is submodular and satisfies Element-Wise-Separabaility with $m = 1/\alpha$ and $c = 0$. 
\end{proposition}
  \mm{Make sure to define $\tilde{1}$ and $\bm{1}$}
\begin{proof}
To prove \eqref{eq:cvar-correct}, it is sufficient by \citet[theorem~4.88]{Follmer2016} and \citet[remark~4.96]{Follmer2016} to show
\begin{align}
  \nonumber
  f(\mathcal{O}) 
  &= -\cvar{\alpha }{- \tilde{1}_{\mathcal{O}}} ,\quad \forall \mathcal{O} \subseteq \Omega\\
  \label{eq:cvar-eq-submodular}
  \min\left\{ \frac{1}{\alpha} \bm{1}_{\mathcal{O}}\tr \bm{p},\; 1 \right\}
&= \max \left\{ \bm{1}_{\mathcal{O}}\tr \bm{q} \mid 
\bm{q} \ge 0, \,
\bm{1}\tr \bm{q} = 1, \,
\bm{q} \le \frac{1}{\alpha} \bm{p}
\right\}, \quad \forall \mathcal{O} \subseteq \Omega.
\end{align}
Note that the maximum in the right hand side of~\eqref{eq:cvar-eq-submodular} always exists because it equals to CVaR which is well defined.

We prove the equality in \eqref{eq:cvar-eq-submodular} as two inequalities. To prove that 
\[
  \min\left\{ \frac{1}{\alpha} \bm{1}_{\mathcal{O}}\tr \bm{p},\; 1 \right\}
\ge \max \left\{ \bm{1}_{\mathcal{O}}\tr \bm{q} \mid 
\bm{q} \ge 0, \,
\bm{1}\tr \bm{q} = 1, \,
\bm{q} \le \frac{1}{\alpha} \bm{p}
\right\}, \quad \forall \mathcal{O} \subseteq \Omega,
\]
observe that the constraints imply that for each $\bm{q} \in \mathcal{Q}_f$
\[
  \bm{1}_{\mathcal{O}}\tr \bm{q} \le \bm{1}\tr \bm{q} = 1,
  \qquad
  \bm{1}_{\mathcal{O}}\tr \bm{q} \le \frac{1}{\alpha} \bm{1}_{\mathcal{O}}\tr \bm{p},
\]
and hence the inequality holds. 

To prove that 
\[
 \min\left\{ \frac{1}{\alpha} \bm{1}_{\mathcal{O}}\tr \bm{p},\; 1 \right\}
\le \max \left\{ \bm{1}_{\mathcal{O}}\tr \bm{q} \mid 
\bm{q} \ge 0, \,
\bm{1}\tr \bm{q} = 1, \,
\bm{q} \le \frac{1}{\alpha} \bm{p}
\right\}, \quad \forall \mathcal{O} \subseteq \Omega,
\]
we construct a feasible $\bm{q} \in \mathcal{Q}_f$ for which the inequality holds.
We now analyze the following two cases separately for each $\mathcal{O} \subseteq \Omega$.
\begin{enumerate}
\item Suppose that $\bm{1}_{\mathcal{O}}\tr  \bm{p} \ge \alpha$. Then we construct $\bm{q}$ as
\[
\bm{q} \;:=\;  (\bm{1}_{\mathcal{O}}\tr \bm{p})^{-1} \cdot \bm{p} \otimes \bm{1}_{\mathcal{O}},
\]
where $\otimes$ is the Hadamard vector product. It is easy to verify that $\bm{q} \in \mathcal{Q}_f$ and $\bm{1}_{\mathcal{O}}\tr \bm{q} \le 1$. 
\item Suppose that $\bm{1}_{\mathcal{O}}\tr  \bm{p} < \alpha$. Then we construct $\bm{q}$ as
\[
\bm{q} \;:=\; \frac{1}{\alpha} \cdot \bm{p} \otimes \bm{1}_{\mathcal{O}}
+  d \cdot \bm{p} \otimes (\bm{1} - \bm{1}_{\mathcal{O}}),
\]
where $\otimes$ is the Hadamard vector product. Here, $d\in \Real $ solves
\[
d \cdot \bm{1}\tr \bm{p} \otimes (\bm{1} - \bm{1}_{\mathcal{O}}) = 1 - \frac{1}{\alpha } \bm{1}_{\mathcal{O}} \bm{p},
\]
and $d$ always exists because
\[
\bm{1}\tr \bm{p} \otimes (\bm{1} - \bm{1}_{\mathcal{O}}) = 1 - \bm{1}_{\mathcal{O}}\tr \bm{p}  > 1 - \alpha \ge 0.
\]
It is easy to verify that $\bm{q} \ge \bm{0}$ and $\bm{1}\tr \bm{q} = 1$. The condition $\bm{q} \le \alpha^{-1} \bm{p}$ holds because $d \le \alpha^{-1}$ is satisfied by multiplying both sides of the inequality by $\bm{1}\tr \bm{p} \otimes (\bm{1} - \bm{1}_{\mathcal{O}})$:
\[
  d \cdot \bm{1}\tr \bm{p} \otimes (\bm{1} - \bm{1}_{\mathcal{O}}) \le
  1 - \frac{1}{\alpha } \bm{1}_{\mathcal{O}} \bm{p}
  \le\frac{1}{\alpha } \cdot \bm{1}\tr \bm{p} \otimes (\bm{1} - \bm{1}_{\mathcal{O}}).
\]
\end{enumerate}
\end{proof}


(TVaR)~\cite{Kruse2021}

We remind the reader that in this work we define TVaR as the $\varphi$-divergence risk that uses the $L_1$ norm divergence. This may not seem very interpretable, however we can show that we can set the divergence constraint, or $\beta$, such that it can be a provable lower bound for a given EVaR with $\alpha$ value between 0 and 1.
From previous work , we know that the KL divergence is $\leq \frac{1}{2 \log(2)} \, \|p - q\|_1^2$ \cite{Iyengar2005}. Thus, by algebraic manipulation it can be seen that the coherent risk measure TVaR with the divergence constraint $\|p - q\|_1 \leq \sqrt{2 \log(2) *-\log(\alpha)}$  must be $\leq EVaR_\alpha[\tilde{x}]$. 

We now prove the submodular function for TVaR and show that it also satisfies ElementWiseSeperability as defined above.
\begin{proposition}
    

For $\mathcal{O} \subseteq \Omega$, define $f_{\beta}\colon 2^{\Omega} \to \Real_{\ge 0}$:
  \[
    f_{\beta}(\mathcal{O}) = \min\left\{\P{\mathcal{O}} + \frac{\beta}{2}, 1\right\}.
  \]
If $\mathcal{O} \neq \emptyset$, and $0$ otherwise, where $\beta = \sqrt{ 2 \log 2 \cdot \log \frac{1}{\alpha}}$ for $\alpha\in (0,1)$ represent the divergence constraint. Then
\[
  \tvar{\alpha }{\tilde{x} } = \max \left\{\bm{x}\tr \bm{q} \mid  \bm{q} \in \mathcal{Q}_{f_{\beta}}\right\}.
  \]
$f_{\beta}$ also satisfy ElementWiseSeparability with $m = 1$ and $c = \beta/2$.
\end{proposition}
\begin{proof}
 To prove \eqref{eq:cvar-correct}, it is sufficient by \citet[theorem~4.88]{Follmer2016} and \citet[remark~4.96]{Follmer2016} to show
\begin{align}
  \nonumber
  f(\mathcal{O}) 
  &= -\tvar{\alpha }{- \tilde{1}_{\mathcal{O}}} ,\quad \forall \mathcal{O} \subseteq \Omega\\
  \label{eq:tvar-eq-submodular}
  \min\left\{ \bm{1}_{\mathcal{O}}\tr \bm{p} + \frac{\beta}{2} ,\; 1 \right\}
  &=
    \max \left\{ \bm{1}_{\mathcal{O}}\tr \bm{q} \mid 
\bm{q} \ge 0, \,
\bm{1}\tr \bm{q} = 1, \,
\bm{\|p -q\|_1} \le \beta 
\right\}, \quad \forall \mathcal{O} \subseteq \Omega.
\end{align}
Note that the maximum in the right hand side of~\eqref{eq:tvar-eq-submodular} always exists because it equals to TVaR which is well defined.

We prove the equality in \eqref{eq:tvar-eq-submodular} as two inequalities. To prove that 
 
\[
  \min\left\{ \bm{1}_{\mathcal{O}}\tr \bm{p} + \frac{\beta}{2} ,\; 1 \right\}
\ge \max \left\{ \bm{1}_{\mathcal{O}}\tr \bm{q} \mid 
\bm{q} \ge 0, \,
\bm{1}\tr \bm{q} = 1, \,
\|\bm{p} -\bm{q}\|_1 \le \beta 
\right\}, \quad \forall \mathcal{O} \subseteq \Omega\]
observe that the constraints imply that for each $\bm{q} \in \mathcal{Q}_f$ that
\[
  \|\bm{p} - \bm{q}\|_1
  =
  \bm{1}_{\mathcal{O}}\tr \bm{|p - q|} + (\bm{1} - \bm{1}_{\mathcal{O}})\tr |\bm{p} - \bm{q}| \leq \beta.
\]
Since by the simplex constraint, \mm{this needs a bit more rigor}
\[
\sum_{i \in \mathcal{O}} (p_i - q_i)
+
\sum_{i \in \mathcal{O}^c} (p_i - q_i)
= 0, 
\sum_{i \in \mathcal{O}^C} (p_i - q_i)
=
-
\sum_{i \in \mathcal{O}} (p_i - q_i).
  \bm{1}_{\mathcal{O}}\tr \bm{|p - q|} = (\bm{1} - \bm{1}_{\mathcal{O}})\tr |\bm{p} - \bm{q}| \leq \beta/2,
\]
and 
  \[
  \bm{1}_{\mathcal{O}}\tr \bm{q} \leq 1\tr \bm{q}  = 1,
\]
the first inequality holds.

To prove that 
\[
  \min\left\{ \bm{1}_{\mathcal{O}}\tr \bm{p} + \frac{\beta}{2} ,\; 1 \right\}
\le \max \left\{ \bm{1}_{\mathcal{O}}\tr \bm{q} \mid 
\bm{q} \ge 0, \,
\bm{1}\tr \bm{q} = 1, \,
\|\bm{p} -\bm{q}\|_{1} \le \beta 
\right\}, \quad \forall \mathcal{O} \subseteq \Omega,
\]
we construct a feasible $\bm{q} \in \mathcal{Q}_f$ for which the inequality holds.
We now analyze the following two cases separately for each $\mathcal{O} \subseteq \Omega$.
\begin{enumerate}
\item Suppose that $\bm{1}_{\mathcal{O}}\tr  \bm{p} + \beta/2 \ge 1$. Then we construct $\bm{q}$ as
\[
\bm{q} \;:=\;  (\bm{1}_{\mathcal{O}}\tr \bm{p})^{-1} \cdot \bm{p} \otimes \bm{1}_{\mathcal{O}},
\]
where $\otimes$ is the Hadamard vector product. It is easy to verify that $\bm{q} \in \mathcal{Q}_f$ and $\bm{1}_{\mathcal{O}}\tr \bm{q} \le 1$. 
\item Suppose that $\bm{1}_{\mathcal{O}}\tr  \bm{p}  + \beta/2 < 1$. Then we construct $\bm{q}$ as
\[
\bm{q} \;:=\; \frac{\bm{1}_{\mathcal{O}} \tr {p} + \beta/2}{\bm{1}_{\mathcal{O}} \tr {p}} \cdot \bm{p} \otimes \bm{1}_{\mathcal{O}}
+  \frac{(\bm{1} - \bm{1}_{\mathcal{O}}) \tr {p} - \beta/2 )}{(\bm{1} - \bm{1}_{\mathcal{O}}) \tr {p}}\cdot \bm{p} \otimes (\bm{1} - \bm{1}_{\mathcal{O}}),
\]
It is easy to verify that $\bm{q} \in \mathcal{Q}_f$ and $\bm{1}_{\mathcal{O}}\tr \bm{q} \le 1$. 
\end{enumerate}


 % We first note that $\mathcal{O} = \emptyset$, then $Q(\mathcal{O}) = 0$,   Due to the set of constraints, we know that for all $ \mathcal{O} \text{ such that } \mathcal{O} \neq \emptyset,  q$ must be such that $Q({\mathcal{O}}) \leq {\P{\mathcal{O}}} + \frac{\beta}{2}$ as if $Q(\mathcal{O}) \geq \frac{\beta}{2} +  \P{\mathcal{O}}$ , then $Q(\mathcal{O'})$ must be $< \P{\mathcal{O}} - \frac{\beta}{2}$ in order for the sum constraint of the simplex to equal 1, thus violating the l1 Norm constraint.
  
 % Furthermore, as we show below, we can construct $q$ such that  for any $\mathcal{O} \neq \emptyset$,   
%  \[f(\mathcal{O}) = \min\left\{\P{\mathcal{O}} + \frac{\beta}{2}, 1\right\}
    .
%  \].
  
%\item[Case 1]: $\P{\mathcal{O}} + \frac{\beta}{2} \geq 1$. In this scenario, we pick an element in $\mathcal{O}$ and add $1 - \P{\mathcal{O}}$ to it. We set all elements not in $\mathcal{O}$ to 0.

%\item[Case 2]: $\P{\mathcal{O}} + \frac{\beta}{2}< 1$. In this scenario, we pick an element in $\mathcal{O}$ and add $\frac{\beta}{2}$ to it. We set all elements not in $\mathcal{O}$ to 0.

%We have now shown that $Q(\Omega) \leq f(\mathcal{O}) = \min\left\{\frac{1}{\alpha} \cdot \P{\mathcal{O}}, 1\right\}$ and it is possible to construct $Q(\Omega)$ such that $Q(\Omega) = \min\left\{\frac{1}{\alpha} \cdot \P{\mathcal{O}}, 1\right\}$. Thus by Remark 4.90 in \cite{Follmer2016}, $f(\mathcal{O}) = \min\left\{\frac{1}{\alpha} \cdot \P{\mathcal{O}}, 1\right\}$
\end{proof}

\section{Experiments}\label{sec:experiments}

To evaluate the algorithms developed in \cref{alg:quick-quantile} we numerically compare their speed up against their sorting based counterparts. For the remainder of the paper we prefix our implementation of the algorithms with a ``Q'' to denote the quick algorithms. 
The results obtained in this work were all generated on a computer with the following specifications: AMD Ryzen Threadripper 3970X 32-Core (64) @ 4.55 GHz with 256 GB RAM running Julia 1.11.5. We also manually disable the Julia garbage collector to reduce noise in the timing data.

Our experimental design allows us to verify that our implementation performs well in practice rather than only theoretically. \cref{fig:all_uniform} shows the results of our experiments. The random variables were generated from a uniform distribution with $N$ samples, where $N$ is varied from $10^2$ to $10^6$. The algorithms were run for each value of $N$ 10 times, and the average runtime was recorded. For consistency, we used a risk level of $0.95$, however the results are similar for other risk levels save for $\alpha = 0 \text{ or } 1$.

The results in \cref{fig:all_uniform} represent a 95\% confidence interval band computed over 10 random runs, each with separately generated random variable. The confidence bands are tight due to the little variance in the measured runtimes between runs. Note that the figure shows the timing results in both linear and log scales.

The results confirm that our quick algorithms are significantly faster than their sorting based counterparts. Especially in the case of CVaR and VaR which are extensively used in the industries aforementioned in \cref{sec:intro}. As expected, the speedup is logarithmic in the runtime of the quick algorithms which is consistent with the theoretical complexity of the algorithms, see \cref{sec:appendix_experiments}. This log speed up coincides with a noticeable speed-up in runtime across the tested random variable sizes. 

The computation of risk measures is often done over large random variables where the runtime and sensitivity of the algorithms to the size of the random variable is a concern. The results presented in this section are chiefly concerned with demonstrating our ability to compute risk measures quickly, fulfilling the practical need.

\begin{figure}
  \includegraphics[width=0.5\textwidth]{./code/plots/all_slow_vs_fast_uniform_linear.pdf}
  \includegraphics[width=0.5\textwidth]{./code/plots/all_slow_vs_fast_uniform_log.pdf}
  \caption{Comparison of runtime of the algorithms for samples from a \emph{uniform} random variable with linear (left) and logarithmic (right) view.}
  \label{fig:all_uniform}
\end{figure}

\begin{table}
\centering
\caption{Runtime comparison for the Stock Market scenario.}
\begin{tabular}{lrr}
\toprule
Method & Mean (ms) & St. dev. (ms) \\
\midrule 
CVaR & 0.3912 & 0.0382 \\ 
\textbf{QuickCVaR} & \textbf{0.1426} & \textbf{0.0307} \\ 
\midrule 
VaR & 0.3733 & 0.0321 \\ 
\textbf{QVar} & \textbf{0.1199} & \textbf{0.0287} \\ 
\midrule 
TVaR & 0.3993 & 0.0414 \\ 
\textbf{QuickTVaR} & \textbf{0.1786} & \textbf{0.0356} \\ 
\midrule 
Expectation & 0.0144 & 0.0124 \\ 
\bottomrule
\end{tabular}
\end{table}

\section{Conclusion}

We have formulated and demonstrated the correctness of a new algorithm for computing the maximum weight independent set for polymatroids without sorting which allows us to solve many common risk measures such as CVaR, VaR, and TVaR in $O(N)$ time.
This speed up is significant in the context of risk measures as they are often used in large scale applications such as finance, healthcare and robotics.

\bibliography{fastcvar.bib}

\appendix
\crefalias{section}{appendix}
\crefalias{Section}{Appendix}

\section{Properties of Discrete Random Variables} \label{sec:prob-discr-rand}
In this section, we summarize some useful properties of discrete random variables which we use in our proofs. 

\begin{lemma} \label{lem:prob-lumpy}
  For each $t\in \Real$:
  \[
    \P{\tilde{x} = t} > 0
    \implies
    \exists \omega \in \Omega, x(\omega) = t.
  \]
\end{lemma}
\begin{proof}
  It follows by induction on the size $|\Omega|$ after generalization that drop the constraint $\bm{1}\tr \bm{p} = 1$.
\end{proof}

\section{Supplemental VaR Algorithms and Results} \label{sec:stand-var-algor}

\begin{algorithm}
\caption{VaR: Standard VaR algorithm}
\label{alg:slow-quantile}
\Input{$\bm{x}\in \Real^n$, $\bm{p}\in \Delta_n$, and $\alpha \in [0,1)$}
\Output{$\varo_{\alpha}[\tilde{x}]$}
Sort $\bm{x}$ indexes to get $\sigma \colon 1{:}n \to 1{:}n$ such that $i > j \implies x_{\sigma(i)} \ge  x_{\sigma(j)}, \; \forall i, j \in 1, \dots , n$\;
$q_0 \gets 0$
\tcp*{$q_i = \P{\tilde{x} \ge x_{\sigma (i)}}$ if $x_{\sigma(i)}$ unique}
\For{$i\in 1{:}n$} {
    $q_i \gets q_{i-1} + p_{\sigma(i)}$ \;
    \lIf{$q_i > \alpha$} {\KwRet{$x_{\sigma(i)}$}}
}
\end{algorithm}

\begin{proposition}
For each $\tilde{x} \in \mathbb{R}^n$, $\bm{p}\in \Delta_n$, and $\alpha\in [0,1)$, \cref{alg:slow-quantile} returns $\var{\alpha}{\tilde{x}}$.
\end{proposition}
\begin{proof}
First, we show by induction on $i$ from $n$ to $1$ that
\begin{equation} \label{eq:proof-q-j}
  q_j = \P{\tilde{x} \ge x_{k_i}},
  \quad\text{where}\quad
  j = \min \left\{ j = 1, \dots , i \mid x_{k_j} = x_{k_i} \right\}.
\end{equation}
The base case is immediate. The inductive case for $i-1$ from $i$ follows by algebraic manipulation from the fact that $x_{k_i}$ is sorted increasingly:
\begin{align*}
\P{\tilde{x} \ge x_{k_{i-1}}} 
 &= \P{\tilde{x} = x_{k_{i-1}}} + \P{\tilde{x} > x_{k_{i-1}}}
 = \P{\tilde{x} = x_{k_{i-1}}} + \P{\tilde{x} \ge x_{k_{i}}} \\
 &= \P{\tilde{x} = x_{k_{i-1}}} + q_i = \sum_{l=j}^{i-1} q_{k_j} + q_{k_i} = q_{k_{i-1}}.
\end{align*}
Now, suppose that \cref{alg:slow-quantile} terminates with value $q\opt$. Then, if the algorithm terminates in iteration $i\opt$ then from \eqref{eq:proof-q-j}, we have that $\P{\tilde{x} \ge q\opt} = q_{j\opt} \ge 1-\alpha$ where $j\opt$ is defined as in \eqref{eq:proof-q-j}. Therefore, $q\opt \le \var{\alpha}{\tilde{x}}$. To prove the converse inequality, suppose that $q\opt > \var{\alpha}{\tilde{x}}$, and therefore $\P{\tilde{x} \ge q\opt} < 1-\alpha$. Then, this is a violation since \eqref{eq:proof-q-j} combined with the termination criterion implies that $\P{\tilde{x} \ge q\opt} < 1-\alpha$.
\end{proof}

\begin{algorithm}
  \caption{\texttt{partition}: Dutch national flag algorithm~\cite[chapter 14]{Dijkstra1976}}
  \label{alg:partition}
  \Input{$\bm{x} \in \Real^n$, $\bm{p} \in \Delta_n$, pivot $v\in \Real$, and front and back $f, b \in 1{:}n$, $f \le b$}
  \Output{$l, g \in f{:}b$, $\bm{x} \in \Real^n$, $\bm{p} \in \Delta_n$ such that \cref{lem:hoare-partition} holds}
  $l_0 \gets e_0 \gets f$; $g_0 \gets b$; $k \gets 0$ \;
  \While{$e_k \leq g_k$}
  {
  \label{line:partition-while}
    \If{$x_{e_k} < v$}{
      swap: $x_{e_k} \leftrightarrow x_{l_k}$ and $p_{e_k} \leftrightarrow p_{l_k}$\; \label{line:swap1}
      $l_{k+1} \gets l_k + 1$ ;
      $e_{k+1} \gets e_k + 1$\; \label{line:partition-e-1-1}
    }
    \ElseIf{$x_{e_k} > v$}{
      swap: $x_{e_k}\leftrightarrow x_{g_k}$ and $p_{e_k} \leftrightarrow p_{g_k}$\; \label{line:swap2}
      $g_{k+1} \gets g_{k} - 1$\; \label{line:parition-g-1}
    }
    \lElse {$e_{k+1} \gets e_{k} + 1$} \label{line:partition-e-1-2}
    $k \gets k + 1$
  }
  \KwRet{$(l_k, g_k, \bm{x}, \bm{p})$}\; 
\end{algorithm}


\begin{algorithm}
  \caption{\texttt{QuickVaR}: Non-recursive adaptation of \cref{alg:quick-quantile}} \label{alg:quickvar-nonrecursive}
  \Input{vector $\bm{x}^0$, pmf $\bm{p}^0$ and $\alpha^0 \in [0,1)$}
  \Output{$\varo_{\alpha}[\tilde{x}]$}
  $f_0 \gets 1$;~~~~$b_0 \gets n$;~~~~$k=1$ \;
  \While{$b_{k-1} > f_{k-1}$} {
    $v \gets x_{\operatorname{rand}(f{:}b)}$ \tcp*{Pivot}
     $l_k, g_k, \bm{x}^k, \bm{p}^k \gets \operatorname{partition}(\bm{x}^{k-1}, \bm{p}^{k-1}, v_k, f_{k-1}, b_{k-1})$
    \tcp*{Alg.~\ref{alg:partition}, \cref{lem:hoare-partition}} \label{line:partion_line}    
    $t_k \gets \sum_{j=f_{k-1}}^{l_k-1} p^k_j$  \tcp*{$\P{\tilde{x}^k < v_k}$}
    $e_k \gets \sum_{j = l_k}^{g_k} p^{k}_j $   \tcp*{$\P{\tilde{x}^k = v_k}$}
    \lIf{$\alpha_{k-1} < t_k$\label{line:determine_left}}
    {
      $f_k \gets f_{k-1}$;~$b_k \gets l_k-1$;~
      $\alpha_k \gets \alpha_{k-1}$
    }
    \lElseIf{$t_k + e_k \le  \alpha_{k-1} \label{line:determine_right}$} 
    {
      $f_k \gets g_k+1$;~$b_k \gets b_{k-1}$;~$\alpha_k \gets \alpha_{k-1} - t_k - e_k$
    }
    \lElse{
      $f_k \gets g_k$;~~~$b_k \gets g_k$
    }
    $k \gets k + 1$\; 
  }
  \KwRet{$x^k_{b_k}$}\;    
\end{algorithm}  

\begin{proof}[Proof of \cref{lem:hoare-partition}]
Let $v$ be an input to \cref{alg:partition} and consider the following invariants.
\begin{align}
\label{eq:ho-inv-less}   x_j &< v, & j\in f_k{:}(l_k-1), \\
\label{eq:ho-inv-equal}  x_j &= v, & j \in l_k{:}(e_k-1), \\
\label{eq:ho-inv-more}   x_j &> v, & j \in (g_k+1){:}b_k.
\end{align}
  
\emph{Initialization}. For $k=0$, conditions \eqref{eq:ho-inv-less}, \eqref{eq:ho-inv-equal}, \eqref{eq:ho-inv-more} are true because the intervals are empty. 
  
\emph{Maintenance}. At each iteration of the loop that starts on line \ref{line:partition-while} of \cref{alg:partition} there are 3 possible branches determined by the conditional logic. $x_{e_k} < v$, $x_{e_k} > v$ and $x_{e_k} = v$. Since $v, x_{e_k} \in \Real$ one of these branches must be executed for each iterate $k$.

  Each time the $x_{e_k} < v$ or $x_{e_k} > v$ branches are executed conditions \eqref{eq:ho-inv-less} and \eqref{eq:ho-inv-more} remain true respectively since in each case we are swapping neccecary elements to preserve each condition. Notice also that $e_k$ is incremented alongside $l_k$ to maintain $e_k - l_k = e_{k+1} - l_{k+1}$ as is necessary for condition \eqref{eq:ho-inv-equal}.

  When $x_{e_k} = v$, $e_k$ is incremented, since $e_0 = l_0$ this creates a difference between $e_{k+1}$ and $l_{k+1}$, the algorithm terminates when $g_t = e_t$, $g_t - l_t = e_t - l_t > 0$ therefore \eqref{eq:ho-inv-equal} remains true.   

  Condition \eqref{eq:hoare-correspond} remains true for each mutation that may occur as both the $x$ values and $p$ values are swapped at the same indices, see lines \ref{line:swap1} and \ref{line:swap2}.
  
  \emph{Termination}. In the loop on line \ref{line:partition-while} there are 3 possible paths of execution determined by the conditional logic $x_{e_k} < v$, $x_{e_k} > v$ and $x_{e_k} = v$. Each of these paths of execution decreases $g_k - e_k$ by 1. This occurs on lines \ref{line:partition-e-1-1} and \ref{line:partition-e-1-2} by increasing $e_k$ by 1 and keeping $g_k$ the same. Or on line \ref{line:parition-g-1} by decreasing $g_k$ by 1 and keeping $e_k$ the same.
  
Observe that  $g_0 - e_0 = b - f$ is positive since $f<b$ by assumption. $g_k-e_k$ decreases by 1 at each iteration, and each iteration requires constant time to complete. The function returns when $g_t - e_t = 0$, therefore this algorithm completes in $O(b-f)$ time. 

  Notice on the last iteration $g_{t} = e_{t}$, therefore by the same reasoning as above $g_{t+1} - e_{t+1} = -1$ and 
  condition \eqref{eq:ho-inv-equal} holds for $g_{t+1} = e_{t+1} - 1$.
\end{proof}

\section{Supplemental $\varphi$-Divergence Algorithms and Properties}

The following proposition states the correctness of the polymatroid optimization algorithm. 
\begin{proposition} \label{prop:standard-polymatroid-works}
Suppose that $f\colon  2^{1{:}n} \to \Real$ is submodular, monotone, $f(\emptyset) = 0$ and $f(1{:}n) = 1$, and for $\bm{x}\in \Real^n_{\ge 0}$. Then, \cref{alg:greedy-polymatroid} computes $\bm{q}\opt$ such that
  \begin{equation} \label{eq:normalized-poly-opt}
    \bm{q}\opt  \in  \arg\min_{\bm{q}\in \widehat{\mathcal{Q}}_f}  \bm{x}\tr \bm{q}.
\end{equation}
\end{proposition}
\begin{proof}
First, we show that $\bm{q}\opt \in \widehat{\mathcal{Q}}_f$. Note that the construction of $\bm{q}\opt $ in \cref{alg:greedy-polymatroid} matches the construction in proposition~14.10 in \citet{Korte2012} since $f(\emptyset) = 0$ and $q\opt_{\sigma(1)} = f(\left\{ 1 \right\}) - f(\emptyset) = f(\left\{ 1 \right\})$. Therefore, proposition~14.10 in \citet{Korte2012} shows that $\bm{q}\opt  \in \mathcal{Q}_f$. Moreover, by the construction of $\bm{q}\opt$ :
  \[
   \sum_{i=1}^n q\opt_i =  
   \sum_{i=1}^n q\opt_{\sigma (i)} = f(1{:}n) - f(\emptyset) = 1, 
  \]
which implies that $\bm{q}\opt \in \widehat{\mathcal{Q}}_f\Delta_n \cap \mathcal{Q}_f$, $\widehat{\mathcal{Q}}_f \neq \emptyset$, and the minimum \eqref{eq:normalized-poly-opt} exists.

To show, that $\bm{q}\opt $ is optimal, we reduce the optimization problem in \eqref{eq:normalized-poly-opt} to the standard linear polymatroid optimization problem with non-negative weights as follows:
\begin{align*}
  \arg\min_{\bm{q}\in \widehat{\mathcal{Q}}_f}  \bm{x}\tr \bm{q}
  &= \arg\min_{\bm{q}\in \widehat{\mathcal{Q}}_f}  (\bm{x} - q_{\min}\cdot \bm{1}) \tr \bm{q} \\
  &= \arg\max_{\bm{q}\in \widehat{\mathcal{Q}}_f}  (q_{\min}\cdot \bm{1} - \bm{x}) \tr \bm{q} \\
  &= \arg\max_{\bm{q}\in \mathcal{Q}_f}  (q_{\min}\cdot \bm{1} - \bm{x}) \tr \bm{q} \\
  &\ni \bm{q}\opt. 
\end{align*}
\end{proof}


The following lemma is folklore in submodular optimization~\cite[theorem~4.5.1]{Bilmes18}. We state and prove it for completeness.
\begin{lemma} \label{lem:submodular-concave}
  Suppose that $\bm{1}\tr\bm{\mu}\in \Real^n_{\ge 0}$ and $g \colon \Real_{\ge 0} \to \Real$ is concave and monotone. Then $f(\mathcal{A}) := g(\bm{1}\tr\bm{\mu}_{\mathcal{A}})$ is a submodular monotone function.
\end{lemma}
\begin{proof}
  The fact that $f$ is monotone follows immediately from the composition of two monotone function. To prove that $f$ is submodular, we use the following properties that hold for each $\mathcal{A}, \mathcal{B} \subseteq 1{:}n$:
  \begin{align}
    \label{eq:sets-bounds-nneg}
    \bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}}
    &\le \bm{1}\tr\bm{\mu}_{\mathcal{A}} \le \bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}},
    &
    \bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}}
    &\le \bm{1}\tr\bm{\mu}_{\mathcal{B}} \le \bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}}, \\
    \label{eq:mu-modular}
    \bm{1}\tr\bm{\mu}_{\mathcal{A}} + \bm{1}\tr\bm{\mu}_{\mathcal{B}}
    &= \bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}} + \bm{1}\tr\bm{\mu}_{\mathcal{A} \cap \mathcal{B}}.
  \end{align}
  The inequalities in \eqref{eq:sets-bounds-nneg} holds because $\bm{\mu} \ge  \bm{0}$ and the equality in \eqref{eq:mu-modular} holds because $\mathcal{A} \mapsto \bm{1}\tr\bm{\mu}_{\mathcal{A}}$ is modular. Then, writing $\bm{1}\tr\bm{\mu}_{\mathcal{A}}$ and $\bm{1}\tr\bm{\mu}_{\mathcal{B}}$ as a convex combination of $\bm{1}\tr\bm{\mu}_{\mathcal{A}\cap \mathcal{B}}$ and $\bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}}$ using \eqref{eq:sets-bounds-nneg} we get that
  \begin{align}
  \label{eq:ga-bound}
    g(\bm{1}\tr\bm{\mu}_{\mathcal{A}})
    &\ge
      \frac{\bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}} - \bm{1}\tr\bm{\mu}_{\mathcal{A}}}{\bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}} - \bm{1}\tr\bm{\mu}_{\mathcal{A} \cap \mathcal{B}}} g(\bm{1}\tr\bm{\mu}_{\mathcal{A} \cap \mathcal{B}}) + 
      \frac{\bm{1}\tr\bm{\mu}_{\mathcal{A}} - \bm{1}\tr\bm{\mu}_{\mathcal{A} \cap \mathcal{B}}}{\bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}} - \bm{1}\tr\bm{\mu}_{\mathcal{A} \cap \mathcal{B}}} g(\bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}}), \\
  \label{eq:gb-bound}
    g(\bm{1}\tr\bm{\mu}_{\mathcal{B}})
    &\ge
      \frac{\bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}} - \bm{1}\tr\bm{\mu}_{\mathcal{B}}}{\bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}} - \bm{1}\tr\bm{\mu}_{\mathcal{A} \cap \mathcal{B}}} g(\bm{1}\tr\bm{\mu}_{\mathcal{A} \cap \mathcal{B}}) + 
      \frac{\bm{1}\tr\bm{\mu}_{\mathcal{B}} - \bm{1}\tr\bm{\mu}_{\mathcal{A} \cap \mathcal{B}}}{\bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}} - \bm{1}\tr\bm{\mu}_{\mathcal{A} \cap \mathcal{B}}} g(\bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}}).
  \end{align}
  The inequalities in \eqref{eq:ga-bound} and \eqref{eq:gb-bound} follow from the concavity of $g$. Then summing \eqref{eq:ga-bound} and \eqref{eq:gb-bound} and algebraic manipulation yields that 
  \[
    f(\mathcal{A}) + f(\mathcal{B})
    = g(\bm{1}\tr\bm{\mu}_{\mathcal{A}}) + g(\bm{1}\tr\bm{\mu}_{\mathcal{B}})
    \ge
      g(\bm{1}\tr\bm{\mu}_{\mathcal{A} \cap \mathcal{B}}) + 
      g(\bm{1}\tr\bm{\mu}_{\mathcal{A} \cup \mathcal{B}})
     = 
      f(\mathcal{A} \cap \mathcal{B}) + 
      f(\mathcal{A} \cup \mathcal{B}),
    \]
    which proves that $f$ is submodular. 
\end{proof}

\section{Experiments}\label{sec:appendix_experiments}
\begin{figure}
  \includegraphics[width=0.5\textwidth]{./code/plots/all_slow_vs_fast_sparse_linear.pdf}
  \includegraphics[width=0.5\textwidth]{./code/plots/all_slow_vs_fast_sparse_log.pdf}
  \caption{Comparison of runtime of the algorithms for samples from a \emph{sparse} random variable with linear (left) and logarithmic (right) view.}
  \label{fig:all_sparse}
\end{figure}
Code and results for these experiments as well as the implementations
of all contained risk measures can be found at \todo{REDACTED for anonymity}.


\begin{table} 
\begin{tabular}{lccccccc}
\toprule
 Size of Prob. & CVaR & QuickCVaR & VaR & QuickVaR & TVaR & QuickTVaR & Expectation \\
Space (n) &  &  &  &  &  &  &  \\
\midrule
1000 & 0.03 ± 0.01 & \textbf{0.01 ± 0.01} & 0.03 ± 0.01 & \textbf{0.01 ± 0.01} & 0.03 ± 0.01 & \textbf{0.02 ± 0.01} & 0.00 ± 0.00 \\
2000 & 0.07 ± 0.01 & \textbf{0.03 ± 0.02} & 0.06 ± 0.01 & \textbf{0.02 ± 0.01} & 0.06 ± 0.01 & \textbf{0.03 ± 0.01} & 0.00 ± 0.00 \\
3000 & 0.10 ± 0.01 & \textbf{0.04 ± 0.02} & 0.09 ± 0.02 & \textbf{0.03 ± 0.02} & 0.10 ± 0.01 & \textbf{0.05 ± 0.01} & 0.00 ± 0.00 \\
4000 & 0.14 ± 0.01 & \textbf{0.05 ± 0.03} & 0.13 ± 0.03 & \textbf{0.04 ± 0.03} & 0.13 ± 0.01 & \textbf{0.07 ± 0.02} & 0.00 ± 0.00 \\
5000 & 0.18 ± 0.02 & \textbf{0.06 ± 0.03} & 0.17 ± 0.01 & \textbf{0.04 ± 0.03} & 0.17 ± 0.02 & \textbf{0.08 ± 0.02} & 0.00 ± 0.00 \\
6000 & 0.21 ± 0.02 & \textbf{0.07 ± 0.04} & 0.20 ± 0.02 & \textbf{0.06 ± 0.04} & 0.21 ± 0.02 & \textbf{0.10 ± 0.03} & 0.00 ± 0.01 \\
7000 & 0.25 ± 0.01 & \textbf{0.09 ± 0.05} & 0.24 ± 0.02 & \textbf{0.06 ± 0.04} & 0.24 ± 0.02 & \textbf{0.12 ± 0.03} & 0.01 ± 0.00 \\
8000 & 0.29 ± 0.02 & \textbf{0.09 ± 0.05} & 0.27 ± 0.02 & \textbf{0.07 ± 0.05} & 0.28 ± 0.02 & \textbf{0.13 ± 0.04} & 0.01 ± 0.00 \\
9000 & 0.33 ± 0.02 & \textbf{0.11 ± 0.06} & 0.31 ± 0.02 & \textbf{0.08 ± 0.06} & 0.32 ± 0.02 & \textbf{0.14 ± 0.04} & 0.01 ± 0.00 \\
10000 & 0.37 ± 0.02 & \textbf{0.11 ± 0.06} & 0.34 ± 0.02 & \textbf{0.09 ± 0.06} & 0.35 ± 0.03 & \textbf{0.16 ± 0.05} & 0.01 ± 0.00 \\
\bottomrule
\end{tabular}
\caption{Mean time (ms) ± 95\% confidence interval of each method.}
\label{tab:small_results}
\end{table}

To assure the reader that our algorithm is indeed faster than the sorting based methods, we present the results of our experiments on small scale problems in \cref{tab:small_results}. 
We test our algorithm on small scale problems where the difference in runtimes is not as significant, however these problems occur frequently in practice. \cref{tab:small_results} is consistent with the experimental results obtained in \cref{sec:experiments}, namely our no-sort methods outperform the sorting based methods in runtime.' This assures that the algorithms developed here provide improvements in runtime for inputs of varying sizes.

The results obtained in \cref{tab:small_results} were generated using a similar process to that of \cref{sec:experiments} where instead of 10 trails per experiment, we used 50 trails per experiment. \cref{tab:small_results} shows the mean time in ms $\pm$ the 95\% confidence interval for each method.

\cref{tab:small_results} shows that even for smaller settings our method is faster than the sorting based methods. The results are consistent with the theoretical complexity of the algorithms, which is $O(n \log n)$ for the sorting based methods and $O(n)$ for the QuickSelect based methods.


% The activity of other users on the system is minimized to account for scheduling conflicts. The presence of multithreading in our language of choice (Julia) is not used in this work, as while the runtime of the experiments would be shorter, we observed individual threads ``thrashing'', where they would suddenly take twice or three times as long on one particular iteration. We conjecture that this is due to the garbage collector. For this reason, we manually disable the Julia garbage collector, even when running synchronously.

\end{document}

%%% Local Variables:
%%% mode: LaTeX
%%% TeX-master: t
%%% End:
