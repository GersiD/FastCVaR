\documentclass{article}
\usepackage{graphicx} % Required for inserting images
\usepackage{fullpage}
\usepackage{mpemath}
\usepackage{hyperref} % For hyperlinks in the PDF like citations
\usepackage{dsfont}
\usepackage{amsmath}
\usepackage{mathtools}
\usepackage[capitalise,noabbrev]{cleveref}
\usepackage{algorithm,algorithmicx,algpseudocode}

\newcommand{\gersi}[1]{\textcolor{red}{#1}}
\newcommand{\palash}[1]{\textcolor{green}{#1}}
\newcommand{\mm}[1]{\textcolor{magenta}{[#1]}}
\newcommand{\maeve}[1]{\textcolor{blue}{[#1]}}
\bibliographystyle{abbrvnat}

\DeclareMathOperator{\kl}{KL}
\DeclareMathOperator{\ess}{ess}
\DeclareMathOperator{\KL}{KL}
\DeclareMathOperator{\ext}{ext}
\DeclareMathOperator{\conv}{conv}
\newcommand{\quant}{{\mathfrak{q}}}

%\DeclarePairedDelimiter\abs{\lvert}{\rvert}%
%\DeclarePairedDelimiter\norm{\lVert}{\rVert}%
\DeclarePairedDelimiter{\spannorm}{\lVert}{\rVert_s}

\usepackage{tikz}
\usetikzlibrary{arrows.meta,automata}


\title{CVaR in Linear Time}
\author{Gersi Doko, Palash Agrawal, Maeve Burwell, Marek Petrik}
\date{}

\begin{document}

\maketitle

\section{Introduction}
% https://www.researchgate.net/publication/2611689_A_One-Pass_Space-Efficient_Algorithm_for_Finding_Quantiles
Approximate quantile algorithm but probability distribution is not known unlike our setting (really each element in the given dataset has an equal probability)


% https://arxiv.org/pdf/2310.07224
(Top-k projection - note this is DIFFERENT than k-capped simplex because it does not have constraint that elements should be between 0 and 1 and it may not necessarily be looking at the sum of all elements, just the top k elements). The paper claims that this can be trivially extended to the superquantile (CVar), however this mainly seems to be the case other then if you are sampling from an unknown continuous distribuiton. That being said, they have a Rockefellar-Ursalyev formulation (well known in Cvar literature) formulation to the top-k operator in their prior work. They also have a quickselect formulation to solve this problem

% https://arxiv.org/pdf/1503.01002
A sorting and partitioning based approach to the k-capped simplex problem (with the constraint being the sum of the vector has to add up to k). Simple proof done with the KKT conditions.

% https://hal.science/hal-01056171v2/document
Excellent paper covering the k-capped simplex problem in two forms (when the sum can be less than or equal to k or when it can be equal to k). Covers lots of prior algorithms used to solve the problem (I need to read this in more depth). This paper has a lemma that will come helpful in equating the inequality constraint in k-capped simplex problem to the eqauiltity of constraint of the sum of probability vector equal to 1 in Cvar case.

% https://bayen.berkeley.edu/sites/default/files/efficient_bregman_projections.pdf
This paper seems to describe an algorithm for projection onto the unit simplex, NOT k-capped using a quickselect based approach, although this is not the primary contribution. 



\section{Preliminaries}

Before describing CVaR and its solution we define the basic notation we use in the paper. 
We use calligraphic letters to denote sets and a tilde to denote random variables. 
We also adopt the standard convention that $\mathcal{A}^{\mathcal{B}}$ represents the set of all functions from a set $\mathcal{B}$ to a set $\mathcal{A}$ and 
treat vectors as a function from indexes to real numbers. $\mathbf{1}$ denotes the vector of ones and $\mathds{1}(bool)$ denotes the indicator function. We use the convention that comparison operators are defined on vector spaces element-wise, that is for $x, y \in \Real^n$ then $x \leq y \iff x_i \leq y_i \quad \; \forall \; i \in 1\dots n$.
Finally, $\Delta^n$ denotes the standard $n$-simplex while $\Delta_k^n$ denotes the k-capped simplex~\cite{kcapped_ang}.

\paragraph{Value-at-Risk and Quantiles}

Quantile of a random variable $\tilde{x} \in \mathbb{X}$ at level $\alpha \in [0,1]$ is any value $\tau\in \Real$ such that $\P{\tilde{x} \le \tau} \ge \alpha$ and $\P{\tilde{x} \ge \tau} \ge 1-\alpha$. The quantile of a random variable may not be unique and lies in the interval $[\quant^-_{\alpha}(\tilde{x}), \quant^+_{\alpha}(\tilde{x})]$ where
%\begin{subequations} 
\begin{align} \label{eq:quantile-definition}
%\label{eq:upper-quantile-definition}
  \quant_{\alpha}^+(\tilde{x})
  &\;:=\;
    \max\; \left\{\tau \in \bar{\Real} \mid \P{ \tilde{x} < \tau } \le \alpha\right\}, 
      %\label{eq:low-quantile-definition}
  &\quant_{\alpha}^-(\tilde{x})
  &\;:=\;
    \min\; \left\{\tau \in \bar{\Real} \mid \P{ \tilde{x}  \le \tau } \ge \alpha\right\}.
\end{align}
%\end{subequations}
The maximum in \eqref{eq:quantile-definition} exists because $\tau \mapsto \P{\tilde{x} < \tau}$ is lower semi-continuous and the minimum in \eqref{eq:quantile-definition} exists because $\tau \mapsto \P{\tilde{x} \le \tau}$ is upper semi-continuous. Note that $\quant^+_1(\tilde{x}) = \infty$, $\quant^-_0(\tilde{x}) = -\infty$ and the quantiles are bounded otherwise for all $\alpha \in [0,1]$.

Monetary risk measures generalize the expectation operator to account for the uncertainty in the distribution. \emph{Value-at-Risk}~(VaR) is one of the oldest, simplest, and most common risk measures~\cite{Follmer2016, Shapiro2014}. Value-at-Risk $\varo \colon \mathbb{X} \to \bar{\Real}$ at a risk level $\alpha \in [0,1]$ of a random variable $\tilde{x}\in \mathbb{X}$ is defined as the largest $1-\alpha$ confidence lower bound on the value of the random variable:
\begin{equation}
  \label{eq:var-definition}
\var{\alpha}{\tilde{x}} \;:=\;  \quant^+_{ \alpha }(\tilde{x}).  
\end{equation}

\section{Formal Model}

\palash{Palash. Also while CVar and k-capped problem might be related, I think we need a formal lemma to show how one problem can be formulated as an instance of the other problem.} 
\palash{TODO reference where the linear programming formulation for CVar is derived.}

\begin{equation}
  \label{eq:primal_cvar}
  CVaR_\alpha(\tilde{x}) = 
  \begin{mprog}
    \minimize{\xi \in \Real^n} \xi\tr \tilde{x} 
    \stc \xi \leq \frac{1}{1-\alpha} p
    \cs \xi \in \Delta^n
  \end{mprog}
  =
  \begin{mprog}
    \minimize{\xi \in \Real^n} \xi\tr \tilde{x} 
    \stc \xi \leq \frac{1}{1-\alpha} p
    \cs \xi\tr \mathbf{1} = 1
    \cs \xi \geq 0
  \end{mprog}
\end{equation}

\gersi{TODO write dual of CVaR}


\begin{thm}
\Cref{alg:quick_cvar} is correct and runs in linear time.
\end{thm}


\begin{lem}
\Cref{alg:quick_quantile} computes $\quant^+$.
\end{lem}
\begin{proof}
 Assumption 1: Algorithm is in while loop for so I can use loop invariant, although I doubt it changes for recursive case
 Assumption 2 (for now): Algorithm requires every outcome in random element array to be unique
 Step 1 of proof(Can't find in the literature as of now):

 Show that $\quant^+$ is always an element of the random variable array $\tilde{x} $. First we recall the definition of $\quant^+$.
 
 The function f(tau) which equals \P${\tilde{x} < \tau}$ . This function has n discrete values, equivalent to the number of random outcomes the function can take on. 
 
 The domain for each output of the function other than 0 and 1 is $(tau_k, tau_{k + 1)}]$, where it is $(-infinity, tau_1]$, and $(tau_n, infinity)$ respectively. 

 Therefore, for all probability values less than 1, the maximum domain for a given value will always be an element of the random variable element array x. 
 
 By extension, the maximum tau such that the function f will have a value is less than or equal to alpha is always going to be an element of the random element array x, except for when alpha is 1.

 Step 2:
 Loop Invariant: At every element the quantile plus will remain in the array x.
 Termination Step: x will always get smaller at each step, except for when x only has one element. When it gets smaller, all the elements that will be removed are not going to be the max quantile. 
 In the case where alpha is less than or equal to tail, every element greater 
 
\end{proof}

\begin{lem}
\Cref{alg:quick_quantile} runs in linear time.
\end{lem}
\begin{proof}
  \mm{Maeve and prove it in Lean}
\end{proof}

\begin{lem}
  If \cref{alg:quick_quantile} correctly computes $\quant^+$ then \cref{alg:quick_cvar} correctly computes CVaR. 
\end{lem}
\begin{proof}
  \mm{TODO}
\end{proof}

\section{Algorithm}

\subsection{Piece-wise Linear Minimization}

\maeve{Incorporate projection.jl }
\begin{algorithm}
\floatname{algorithm}{Piece-wise Linear Minimization}
\end{algorithm}


\subsection{Quick Quantile}

\begin{algorithm}
  \floatname{algorithm}{Quick Quantile}
  \algrenewcommand\algorithmicrequire{\textbf{Input: Random variable $\tilde{X}$ with domain $x$ and probability mass function $p$ and $\alpha \in [0,1]$}}
  \algrenewcommand\algorithmicensure{\textbf{Output: $\alpha$-quantile of $\tilde{X}$}}
  \caption{}\label{alg:quick_quantile}
  \begin{algorithmic}[1]
    \Require 
    \Ensure 
    \If{$n = 1$}
    \State \Return $x$
    \EndIf
    \State $pivot \gets rand(1:n)$
    \State $tail \gets \sum_{i=0}^n \mathds{1}(x[i] \leq x[pivot]) p[i]$
    \If{$\alpha \leq tail$}
    \State \Return $qq(vals[\mathds{1}(x[i] \leq x[pivot])], p[\mathds{1}(x[i] \leq x[pivot])], \alpha)$
    \Else
    \State \Return $qq(vals[\mathds{1}(x[i] > x[pivot])], p[\mathds{1}(x[i] > x[pivot])], \alpha - tail)$
    \EndIf
  \end{algorithmic}
\end{algorithm}

\begin{algorithm}
  \floatname{algorithm}{Quick Quantile Loopy}
  \algrenewcommand\algorithmicrequire{\textbf{Input: Random variable $\tilde{X}$ with domain $x$ and probability mass function $p$ and $\alpha \in [0,1]$}}
  \algrenewcommand\algorithmicensure{\textbf{Output: $\alpha$-quantile of $\tilde{X}$}}
  \caption{}\label{alg:quick_quantile_loop}
  \begin{algorithmic}[1]
    \Require 
    \Ensure 
    \While{$n > 1$}
      \State $pivot \gets rand(1:n)$
      \State $tail \gets \sum_{i=0}^n \mathds{1}(x[i] \leq x[pivot]) p[i]$
      \If{$\alpha \leq tail$}
      \State $n \gets \sum_{i=0}^n \mathds{1}(x[i] \leq x[pivot])$
      \Else
      \State $n \gets \sum_{i=0}^n \mathds{1}(x[i] > x[pivot])$
      \State $\alpha \gets \alpha - tail$
      \EndIf
    \EndWhile
  \end{algorithmic}
\end{algorithm}


\subsection{Quick CVaR}

\begin{algorithm}
  \floatname{algorithm}{Quick CVaR}
  \algrenewcommand\algorithmicrequire{\textbf{Input: Random variable $\tilde{X}$ with probability mass function p and $\alpha \in [0,1]$}}
  \algrenewcommand\algorithmicensure{\textbf{Output: $CVaR_\alpha(\tilde{X})$}}
  \caption{}\label{alg:quick_cvar}
  \begin{algorithmic}[1]
    \Require 
    \Ensure 
    \State $q_\alpha = \text{quick\_quantile}(\tilde{X}, p, \alpha)$ 
    \State \Return $\sum_{i=0}^n \mathds{1}( x[i] \leq q_\alpha ) x[i] p[i]$
  \end{algorithmic}
\end{algorithm}

\section{k-capped simplex}

\begin{equation}
  \label{eq:k_capped_simplex}
  \psi(x, k) = 
  \begin{mprog}
    \minimize{\xi \in \Real^n} \lvert \lvert \xi - x \rvert \rvert_2^2
    \stc \xi \in \Delta_k^n
  \end{mprog}
  =
  \begin{mprog}
    \minimize{\xi \in \Real^n} \lvert \lvert \xi - x \rvert \rvert_2^2
    \stc \xi\tr \mathbf{1} \leq k
    \cs \xi \geq 0   
  \end{mprog}
\end{equation}


\bibliography{fastcvar.bib}

\end{document}

%%% Local Variables:
%%% mode: LaTeX
%%% TeX-master: t
%%% End:
