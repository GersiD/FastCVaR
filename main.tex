\documentclass{article}
% if you need to pass options to natbib, use, e.g.:

\PassOptionsToPackage{numbers, sort&compress}{natbib}
% before loading neurips_2025
% ready for submission
\usepackage{neurips_2025}
% to compile a preprint version, e.g., for submission to arXiv, add add the
% [preprint] option:
%     \usepackage[preprint]{neurips_2025}
% to compile a camera-ready version, add the [final] option, e.g.:
%     \usepackage[final]{neurips_2025}
% to avoid loading the natbib package, add option nonatbib:
%    \usepackage[nonatbib]{neurips_2025}

\usepackage[colorlinks=true,citecolor=BlueViolet]{hyperref} % For hyperlinks in the PDF like citations
\usepackage{mpemath}
\usepackage{graphicx} % Required for inserting images
\usepackage[dvipsnames]{xcolor} 

\SetKwInOut{Input}{Input}
\SetKwInOut{Output}{Output}

\usepackage{booktabs}

\newcommand{\gersi}[1]{\textcolor{red}{#1}}
\newcommand{\palash}[1]{\textcolor{green}{#1}}
\newcommand{\mm}[1]{\textcolor{magenta}{[#1]}}
\newcommand{\maeve}[1]{\textcolor{blue}{[#1]}}

\bibliographystyle{abbrvnat}

\newcommand{\Nats}{\mathbb{N}}

\DeclareMathOperator{\kl}{KL}
\DeclareMathOperator{\ER}{DR}
\DeclareMathOperator{\ess}{ess}
\DeclareMathOperator{\KL}{KL}
\DeclareMathOperator{\ext}{ext}
\DeclareMathOperator{\conv}{conv}
\DeclareMathOperator{\qvar}{QuickVaR}
\newcommand{\quant}{{\mathfrak{q}}}

\renewcommand{\cite}{\citep}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% THEOREMS
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\title{Evaluating Risk Measures in Linear Time}% and $\varphi$-divergence Risk Measures}
%\author{Palash Agrawal, Gersi Doko, Maeve Burwell, Marek Petrik}
%\date{}

\begin{document}

\maketitle

\begin{abstract}
Monetary risk measures have gained popularity in expressing decision-makers' preferences for risk aversion. Value-at-Risk (VaR) and Conditional-Value-at-Risk (CVaR) in particular are used commonly for this purpose. This paper proposes a novel approach for computing these risk measures as well as others for discrete distributions in linear time. We adapt the Quick Select algorithm and combine it with a general polymathic representation of $\varphi$-divergence risk measures. Numerical results show that our new algorithms offer an order of magnitude speed up for large domains, and a library implementation of the algorithms is available at \url{https://github.com/RiskAverseRL/RiskMeasures.jl}.     
\end{abstract}

\section{Introduction}\label{sec:intro}

Monetary measures of risk generalize the expectation operator to be able to represent the risk aversion of decision makers in domains that range from robotics~\cite{akella2024risk,majumdar2020should,benrabah2024review} to finance~\cite{Follmer2016} to infrastructure maintenance~\cite{Inzunza2016}, and disaster response~\cite{Barahona2013}. Value-at-Risk~(VaR) and Conditional-Value-at-Risk~(CVaR) are the most popular risk measures and have made inroads in reinforcement learning, machine learning, and decision making~\cite{Petrik2024}. Machine learning and reinforcement learning algorithms typically must compute the risk measures for discrete random variables. Fast algorithms for computing risk measures are necessary in several machine-learning settings. For example, when solving robust or risk-averse MDPs by value iteration, the risk measure must be evaluated in every iteration for every state of the algorithm and can represent a significant computational bottleneck~\cite{Ho2021,Ho2018,Ho2022c,Hau2023a,Hau2025}. 

In this paper, we propose linear time algorithms for computing risk measures of discrete random variables. First, we develop \emph{QuickVaR}, an algorithm that computes VaR in linear time. The QuickQuantile algorithm builds on the WeightedMedian quickselect algorithm. Second, we develop \emph{QuickDivergence}, an algorithm that computes, in linear time,   $\varphi$-divergence risk measures~\cite{Ahmadi-Javid2012} that satisfy a specific piecewise linearity property. For example, CVaR is a $\varphi$-divergence risk measure that satisfies the required linearity property. The QuickDivergence algorithm leverages the fact that $\varphi$-divergence risk measures can be represented as linear minimization over polymatroids~\cite{Follmer2016}. While we focus on expected linear time algorithms because they work well in practice, but they can be readily adapted to run in deterministic linear time with a careful pivot selection as we describe below. 

Standard algorithms for computing risk measures of discrete random variables have superlinear time complexity. For instance, VaR algorithms sort the values of the random variable which requires $\Omega(n \log n)$ operations where $n$ is the size of the probability space. Coherent risk measures, like CVaR, can be evaluated by solving convex optimization problems~\cite{Follmer2016, rockefeller1996variational}. Generic convex optimization algorithms, such as the interior point linear program solvers, are no faster than $O(n^3)$~\cite{Ho2021} which may be too slow in practice. However, CVaR and other coherent comotonotonic law-invariant risk measures can be solved using greedy algorithms in $O(n \log  n)$ time~\cite{Follmer2016}. The $\Omega(n \log  n)$ time complexity of the sort dominates the time complexity and prevents linear time computation. In this paper, we circumvent the complexity of sorting by developing no-sort algorithms for these risk measures and others. 

Numerous efficient algorithms have been designed for structured optimization problems that resemble VaR and CVaR evaluations. Some example of such structured optimization problems are the projection onto the simplex~\cite{adam2019projectionscanonicalsimplexadditional, Wang2013Simplex}, projection on k-capped simplex~\cite{kcapped_ang,Lim2016}, linear optimization on the intersection of $L_p$ or $\varphi$-divergence balls and the probability simplex as arise in robust optimizations~\cite{Ho2022}, optimization for $\varphi$-divergence risk measures~\cite{Ahmadi-Javid2012}, and on polytopes defined by submodular functions which are known as polymatroids~\cite{Korte2012}. Many of these algorithms require sorting the input variables, which has $O(n \log  n)$ complexity. While these papers often discuss the possibility of linear-time algorithms, they usually do not study them with a few exceptions~\cite{Condat2016,Lim2016}. 

Our algorithms' time complexity improves on the state of the art by a logarithmic factor. Although this improvement may seem unimportant,  it is an acceleration that can be practically meaningful without any serious drawbacks. Our algorithms are simple and easy to implement, with little added complexity compared to the state-of-the-art. Our numerical results show that they can lead to an order of magnitude speed-up for realistic large problems, a significant speed-up for moderately sized problems, and match the performance of existing algorithms for small problems.

The remainder of the paper is organized as follows. \Cref{sec:preliminaries} describes the necessary background on risk measures, $\varphi$-divergence, and polymatroid optimization. Then, \Cref{sec:var-linear} shows the relationship between VaR and the Weighted Median problem, allowing us to trivially adapt algorithms for the Weighted Median Problem to solve VaR. Then, \cref{sec:phi-divergence} builds on this to develop \emph{QuickDivergence}, an algorithm that evaluates certain $\varphi$-divergence risk measures in linear time. We evaluate the algorithms numerically on synthetic and realistic problems in \cref{sec:experiments}. A tested implementation of the algorithms in Julia is available at \url{https://github.com/RiskAverseRL/RiskMeasures.jl}.

\section{Preliminaries}\label{sec:preliminaries}

In this work, we restrict our attention to random variables defined on finite probability spaces $(\Omega, 2^{\Omega}, \bm{p})$ where $\Omega := \left\{ 1, \dots , n \right\}$, $\bm{p} \in \Delta_n$, and $\Delta_n$ is the $n$-dimensional probability simplex. We use a tilde to indicate a random variable, such as $\tilde{x}\colon \Omega \to \Real$. The set of real-valued random variables is denoted by $\mathbb{X}$. Vectors are denoted with a lower-case bold font, such as $\bm{x} \in \Real^n$ and indexed as $\bm{x}_i \text{ for } i = 1, \dots , n$. We use $\tilde{x}$ and $\bm{x}$ interchangeably, such that $\bm{x}_\omega = \tilde{x}(\omega) \text{ for }  \omega \in \Omega$. Sets are denoted with calligraphic letters and $\bar{\Real} := \Real \cup \left\{ -\infty, \infty \right\}$ denotes the extended real line. 

\emph{Monetary risk measures} $\rho\colon \mathbb{X} \to \bar{\Real}$ generalize the expectation operator to account for different notions of uncertainty in the distribution of a random variable~\cite{Follmer2016}. We apply risk measures to rewards and suppose that higher risk values are preferable. 

First, we consider the \emph{Value-at-Risk}~(VaR)---one of the most popular risk measures---that is defined for $\alpha\in [0,1)$ for any $\tilde{x}\in \mathbb{X}$ as~\cite{Follmer2016,Shapiro2014}
\begin{equation}
  \label{eq:var-definition}
  \var{\alpha}{ \tilde{x} }
  \;:=\;
    \max\; \left\{\tau \in \bar{\Real} \mid \P{\tilde{x} < \tau}  \le \alpha\right\} =
    \max\; \left\{\tau \in \bar{\Real} \mid \P{\tilde{x} \ge  \tau}  \ge 1 - \alpha\right\}.
\end{equation}
The first maximum in~\eqref{eq:var-definition} exists because $\tau \mapsto \P{\tilde{x} < \tau}$ is a lower semi-continuous function. One can readily see that $\varo_0[\tilde{x}] = \ess \inf [\tilde{x}]$, and typically the risk measure is extended as $\varo_{1}[ \tilde{x} ] = \infty$. Our analysis will also require the notion of the \emph{quantile} $\quant_{\alpha}$ for $\tilde{x}$ which is defined as the set
\[
  \quant_{\alpha}[\tilde{x}] := \left\{ z\in \Real \mid  \P{ \tilde{x} \le z } \ge \alpha , \P{ \tilde{x} \ge z } \ge 1-\alpha  \right\}.
\]
The lower and upper bounds of the quantile set are denoted by $\quant^+_{\alpha}[\tilde{x}] := \max \quant_{\alpha}[\tilde{x}] = \var{\alpha }{\tilde{x}}$ and $\quant^-_{\alpha}[\tilde{x}] := \min \quant_{\alpha}[\tilde{x}]$.

\begin{table}
    \centering
    \label{tab:divergences}
    \begin{tabular}{l|c|c|c}
         \toprule
         Risk measure & $\varphi(y)$  & $\varphi\opt(x)$ & $\beta$ \\
         \midrule        
         $\mathbb{E}$           & $\begin{cases} 0 &\text{if } y = 1 \\ \infty & \text{otherwise}  \end{cases}$ & $x$ & $0$  \\
      \midrule
      $\cvaro_{\alpha}$     & $\begin{cases} 0 &\text{if } y \in [0, \frac{1}{\alpha}] \\ \infty & \text{otherwise}  \end{cases}$ & $\frac{1}{\alpha} \cdot \max \left\{ 0,  x \right\} $& $0$ \\
      \midrule
      $\evaro_{\alpha}$     & $\begin{cases} y \log y &\text{if } y > 0 \\ 0 &\text{if } y =  0 \\  \infty &\text{if } y < 0 \end{cases}$ & $\exp{ x - 1 }$ & $\log \frac{1}{\alpha}$ \\
      \midrule
         $\tvaro_{\alpha}$     & $|y - 1|$ & $ \begin{cases} x &\text{if } |x| \le 1\\ \infty &\text{otherwise} \end{cases} $& $\sqrt{ 2 \log 2 \cdot \log \frac{1}{\alpha} }$ \\
         \bottomrule
    \end{tabular}
    \caption{Parameters for selected $\varphi$-divergence risk measures for $\alpha\in (0,1)$.} \label{tab:phi-divergence-risk}
\end{table}

Second, we consider \emph{$\varphi$-divergence risk measures}~\cite{Ahmadi-Javid2012}. A divergence risk measure $\ER_{\varphi, \beta }\colon \mathbb{X} \to  \bar{\Real}$  is defined for a closed, convex function $\varphi\colon \bar{\Real} \to \bar{\Real}$ that satisfies $\varphi(1) = 0$ and $\beta\in \Real_{\ge 0}$ as
\begin{align} \label{eq:fdiv-primal}
  \ER_{\varphi, \beta}[ \tilde{x} ]
  &:= \inf \left\{\bm{q}\tr \bm{x} \mid \bm{q}\in \Delta_n, H_{\varphi}(\bm{q} \| \bm{p}) \le \beta
    %, \bm{q} \ll \bm{p}
    \right\} \\
  \label{eq:fdiv-dual}
  &= \sup \left\{  t \cdot v - t\cdot \E { \varphi\opt \left(- \frac{\tilde{x}}{t} + v + \beta \right)} \mid  t,v \in \Real, \,  > 0 \right\}.
\end{align}
% The notation $\bm{q} \ll \bm{p}$ indicates that $\bm{q}$ is absolutely continuous with respect to $\bm{p}$: $p_{\omega} =0 \implies q_{\omega} = 0, \forall  \omega \in \Omega$.
The function $H_{\varphi}$ in~\eqref{eq:fdiv-primal} is a $\varphi$-divergence measure defined as~\cite{Bayraksan2015}
\[
  H_\varphi(\bm{q} \| \bm{p}) := \sum_{\omega \in \Omega} p_{\omega} \cdot \varphi\left(\frac{q_{\omega}}{p_{\omega}}\right),
\]
and we adopt the common convention that $1 / 0 = \infty$ and $0 \cdot \infty = 0$~\cite{rockefeller1996variational}. Note that the absolute continuity restriction is not needed for finite probability spaces. 
The function $\varphi\opt(x)$ is the convex conjugate of $\varphi(y)$.



Finally, it is important to note that $\varphi$-divergence risk measures are coherent, which means they are convex and positively homogeneous. Any coherent risk measure $\rho\colon \mathbb{X}\to \bar{\Real}$ satisfies for some closed convex non-empty set $\mathcal{Q} \subseteq \Delta^n$ \citep[Prop. 2.84]{Follmer2016}. 

One specific $\varphi$-divergence risk measure we consider in this work is \emph{Conditional-Value-at-Risk}~(CVaR), a common risk measure, which is defined for a random variable $\tilde{x} \in \mathbb{X}$ and risk level $\alpha \in (0,1]$ as~\cite{Follmer2016}
\begin{equation} \label{eq:primal_cvar}
    \cvaro_\alpha(\tilde{x})
  := \max_{z \in \Real} \left(z - \frac{1}{\alpha} \cdot \E{[z -\bm{x}]_+}\right) 
  % = \min \left\{\bm{q}\tr \bm{x} \mid \bm{q} \leq \alpha^{-1} \bm{p},\, \bm{q}\in \Delta_n \right\},
\end{equation}
CVaR is usually extended continuously as $\cvaro_0[\tilde{x}] = \ess\inf[\tilde{x}]$.

We also focus on another $\varphi$-divergence risk measure risk measure in which the divergence is the L1 norm. While the risk measure has been studied in prior literature, a proper name has never been given. Thus, we name the risk measure TVaR, where for a given $\alpha$, TVaR$_\alpha$ can be seen as a lower bound on CVaR$_{\alpha}$ and EVaR$_{\alpha}$ (Entropic Value at Risk).   
\begin{proposition}
For each $\alpha\in [0,1]$ and $\tilde{x} \in \mathbb{X}$:
\[
  \var{\alpha}{\tilde{x}}
  \ge
  \cvar{\alpha}{\tilde{x}}
  \ge
  \evar{\alpha}{\tilde{x}}
  \ge
  \tvar{\alpha}{\tilde{x}}.
\]
\end{proposition}
\begin{proof}
The proof follows directly from Chapter 2 of \cite{CoverThomas1991Elements} and \cite{Ahmadi-Javid2012}. Note the proof in \cite{Ahmadi-Javid2011} is in terms of costs while our risk measures are in terms of rewards.
\end{proof}

In the remainder of the paper, we first propose a linear-time algorithm for computing VaR then we build on it to propose a linear time algorithm for certain $\varphi$-divergence risk measures. 

\section{Computing VaR in Linear Time} \label{sec:var-linear}

In this section, we show derive a linear time algorithm for Value at Risk and show its equivalence between a slightly modifed version of the Weighted Median problem, which is well known to be solvable in linear time.\cite{Korte2012,cormen2001introduction}. The algorithms modify the standard quick select algorithm to one where the values may have varying weights.

We first show that $\quant_{\alpha}^+(\tilde{x})$ is an element in the array $x$, which will be necessary to establish correctness of the algorithm. 
\begin{lemma}\label{lem:quantile-in-finite-omega}
Suppose that $\tilde{x}\colon \Omega \to \Real$ is a random variable with finite $\Omega$. Then, there exist $\omega_+, \omega_-\in \Omega$ such that $\quant_{\alpha}^+(\tilde{x}) = \tilde{x}(\omega_+)$ and $\quant_{\alpha}^-(\tilde{x}) = \tilde{x}(\omega_-)$ for each $\alpha\in (0,1)$.
\end{lemma}

We now present our algorithm. While the version here is recursive it can trivially be modified to version with while loops, which is often faster in practice. We refer the readers to the appendix for the non-recursive version, which we also use for our expirimental evaluations.
% We now define our version of the Weighted Median problem.
% \begin{definition} \label{WM_Problem}

% We now show that we can cast VaR as an instance of the Weighted Median problem
% \begin{lemma}
% The weighted median problem defined in \cref{WM_Problem} is equivalent to solving VaR when $W = \alpha$ for a discrete random variable $\tilde{x}\colon \Omega \to \Real$ with finite $\Omega$ such that the outcomes of the random variable $\xi_1, \ldots, \xi_n \in \Real$ and the weights $w_1, \ldots, w_n \in \Real^{\mathbb{C}}$ correspond to the probabilities of each outcome .
% \end{lemma}


%We first restate some well-known properties of the Hoare Partition scheme.
%Our \emph{QuickVaR} algorithm is described in \cref{alg:quick-quantile}. The algorithm works as follows. It first picks a pivot by taking the middle element between front and back and applies the partition scheme to easily compute the probability of all outcomes smaller than or equal to that of the pivot (Lines \ref{line:pivot_comp} - \ref{line:partion_line}). Note that three partitions are create, one where the elements are all less than the pivot, another containing all elements equal to the pivot, and a third pivot containing all elements greater than the pivot. We refer to these partitions as the left, middle, and right partition from here on. QuickVaR then determines whether the quantile can exist in the left, middle, or right pivot (lines \ref{line:determine_left}, \ref{line:determine_right}, and \ref{determine_middle} ) and adjusts the $f_k$ and $b_k$ respectively to reduce the search space to one of these partitions. We note that in the case the quantile is determined to exist in the middle partition, the algorithm can terminate since every value in the middle partition is the same. In the case of $\alpha^0 = 0$, we handle this separately and simply find the minimum element. 

% \begin{algorithm}
%   \algrenewcommand\algorithmicrequire{\textbf{Input: vector $x$, pmf $p$ and $\alpha \in [0,1]$}}
%   \algrenewcommand\algorithmicensure{\textbf{Output: $\alpha$-quantile of $\tilde{X}$}}
%   \caption{QuickVaR: Linear-time algorithm for VaR. $\mathds{1}(bool)$ .}\label{alg:quick-quantile}

%   \begin{algorithmic}[1]
%     \Require{} \Ensure{}
%     \State{$f_0 \gets 1$, $b_0 \gets n$, $x^0 = x$, $p^0 = p$, $k=1$, $\alpha_0 = \alpha$ } \Comment{Init front $f_0$ and back $b_0$}
%     \While{$b_k - f_k \geq 1$}
%       \State{$i_k, x^k, p^k \gets \operatorname{partition}(x^{k-1}, p^{k-1}, f_{k-1}, b_{k-1})$} %\Comment{Hoares partition scheme}
%     \State{$t_k \gets \sum_{j=f_{k-1}}^{i_k} p^k_j$}
%       \If{$\alpha_k < t_k$}
%       \State{$b_k \gets i_k$, $\alpha_k \gets \alpha_{k-1}$}
%       \Else{}
%       \State{$f_k \gets i_k + 1$, $\alpha_k \gets \alpha_{k-1} - t_k$}
%     \EndIf{}
%     \State $k \gets k + 1$
%     \EndWhile{}
%     \State{\Return{$x^k_{b_k}$}}
%   \end{algorithmic}
% \end{algorithm}


% \begin{algorithm}
%   \caption{QuickVaR: Linear-time algorithm for VaR}\label{alg:quick-quantile}
%   \Input{vector $\bm{x}$, pmf $\bm{p}$ and $\alpha \in (0,1)$}
%   \Output{$\alpha$-quantile of $\tilde{x}$}
%   $f_0 \gets 1$, $b_0 \gets n$, $\bm{x}^0 = \bm{x}$, $\bm{p}^0 = \bm{p}$, $\alpha_0 = \alpha$, $k=1$ \;
%   \While{$b_{k-1} - f_{k-1} \geq 1$} {
%      $i' \gets f_{k-1} + \lceil\frac{b_{k-1}- f_{k-1}}{2}\rceil$ \tcp*{Pivot: Median-of-medians for $O(b-f)$ runtime}
%      $l_k, g_k, \bm{x}^k, \bm{p}^k \gets \operatorname{partition}(\bm{x}^{k-1}, \bm{p}^{k-1}, x_{i'}, f_{k-1}, b_{k-1})$
%     \tcp*{Alg.~\ref{alg:partition}, \cref{lem:hoare-partition}}
    
%     $t_k \gets \sum_{j=f_{k-1}}^{g_k} p^k_j$ \;
%     \lIf{$\alpha_{k-1} < t_k$} {$f_k \gets f_{k-1}$, $b_k \gets g_k$, $\alpha_k \gets \alpha_{k-1}$}
%     \lElse {$f_k \gets g_k + 1$, $b_k \gets b_{k-1}$, $\alpha_k \gets \alpha_{k-1} - t_k$}
%     $k \gets k + 1$\; 
%   }
%   \KwRet{$x^k_{b_k}$}\;    
% \end{algorithm}  

\begin{algorithm}
  \caption{Recursive QuickVaR: Linear-time recursive algorithm for VaR}\label{alg:quick-quantile}
  \Input{vector $\bm{x}$, pmf $\bm{p}$, $\alpha \in (0,1)$, indices $f$, $b$}
  \Output{$\varo_{\alpha}[\tilde{x}]$}
  
  \If{$b \le f$}{
    \KwRet{$x_b$}\;
  }

  $i \gets \operatorname{rand}(\left\{ f, f+1, \dots , b \right\})$ \tcp*{Pivot index}
  $v \gets x_i$ \tcp*{Pivot value}
  $l, g, \bm{x}', \bm{p}' \gets \operatorname{partition}(\bm{x}, \bm{p}, v, f, b)$
  \tcp*{Rearrange elements into $<$, $=$, and $>$ groups}
  $t \gets \sum_{j=f}^{l-1} p'_j$ \tcp*{$\P{\tilde{x}' < v}$}
  $e \gets \sum_{j=l}^{g} p'_j$  \tcp*{$\P{\tilde{x}' = v}$}

  \If{$\alpha < t$}{
    \tcp{Quantile lies to the left of the pivot}
    \KwRet{$\qvar(\bm{x}', \bm{p}', \alpha, f, l-1)$}\;
  }
  \ElseIf{$\alpha \ge t + e$}{
    \tcp{Quantile lies to the right of the pivot}
    \KwRet{$\qvar(\bm{x}', \bm{p}', \alpha - t - e, g+1, b)$}\;
  }
  \Else{
    \tcp{Quantile equals pivot value}
    \KwRet{$v$}\;
  }
\end{algorithm}





%The above conditions primarily state that each call to the partition scheme performs a partial sorting such that all elements $\leq v_k$ will have an index $< i_k$ , and elements greater than the pivot will have an index $> i_k$.
We now prove the correctness of our algorithm. We first prove a general condition on the return value of our algorithm which applies for any array $p$, regardless of whether it is a simplex or not. It trivially follows that when $p$ is a valid simplex, the condition simplifies to the \cref{eq:var-definition}. For the partition algorithm and it's proof of correctness, we refer the reader to the appendix.
\begin{lemma} \label{PROOFCORRECTNESS}
  % \Cref{alg:recursive-qvar} returns $\varo_{\alpha}[\tilde{x}]$ for $\alpha\in [0,1)$. 
  % \Cref{alg:recursive-qvar} returns $\varo_{\alpha}[\tilde{x}]$ for $\alpha\in [0,1)$. 
Algorithm \ref{alg:quick-quantile} returns $\tau\opt$ where \[
  \tau\opt  \in \argmax \left\{\tau \in \Real \mid   \sum_{m = f}^b p_m \cdot I(x_m < \tau) \leq \alpha \right\},
\] \label{INDUCTHYPOTH}
$\forall \alpha < \sum_{m = f}^{b}p_m$.
\end{lemma}
\begin{proof}

We proceed by induction on the number of elements in the recursion interval, 
$n = b - f + 1$.

% \paragraph{Inductive hypothesis.} \label{GeneralVarDEF}
% Assume that for all inputs of size less than or equal to $n$, 
% the algorithm correctly returns the
% \[
%   \max \left\{\tau \in \Real \mid  ( \sum_{m = f}^b p_m\right\} \cdot I(x_m < \tau)) \leq \alpha,
% \]
% where $\alpha < \sum_{m = f}^{b}$.

\paragraph{Base case ($n = 1$).}
Since $b = f$, the algorithm returns $x_b$.
Since there is only one possible outcome, we have $\sum_{m = f}^b p_m \cdot I(x_m < x_b)  =  0 \le \alpha \textbf{ } \forall \alpha < p_b $ and $\sum_{m = f}^b p_m \cdot I(x_m \leq x_b) > \alpha \textbf {}\forall \alpha < p_b. $ by the inductive hypothesis.
Hence, by definition, $x_b$ is the optimal $\tau$ for . 
Therefore, the algorithm is correct for $n=1$.



\paragraph{Inductive step.}
Consider a call with interval size $j > 1$  and suppose that the inductive hypothesis holds for all n with $1 \le n < j$:
\[
  \textsc{QuickVaR}(\bm{x}, \bm{p}, \alpha, f, b).
\]

There are three possible cases depending on the pivot value $v$ that is selected:
\begin{enumerate}
  \item[\textbf{(i)}] \textbf{Case 1 $\bm{\alpha < t}$.}
    We can also, show by contradiction that $\tau\opt$ optimal in \cref{INDUCTHYPOTH} must satisfy that
    \[
     \tau\opt < v,
   \]
   because 
    \[
      \alpha  < t = \sum_{m = f}^{l-1} p'_m  = \sum_{m = f}^b p'_m I(x'_m < v),
    \]
  % implying that the optimal $\tau\opt \in f{:}(l-1)$.
    Moreover, $x'_m < v, \forall m \in f{:}(l-1)$ from the partition function. 
  By the inductive hypothesis,
  \begin{align*}
    \qvar(\bm{x}', \bm{p}', \alpha, f, l-1)
    &= \max \left\{\tau \in \Real \mid  \sum_{m = f}^{l - 1} p'_m \cdot I(x'_m < \tau) \leq \alpha \right\} \\
    &= \max \left\{\tau \in (-\infty, v) \mid  \sum_{m = f}^{l - 1} p'_m \cdot I(x'_m < \tau) \leq \alpha \right\} \\
    &\stackrel{\text(a)}{=} \max \left\{\tau \in (-\infty, v)  \mid  \sum_{m = f}^{b} p'_m \cdot I(x'_m < \tau) \leq \alpha \right\} \\
    &= \max \left\{\tau \in (-\infty, v)  \mid  \sum_{m = f}^{b} p_m \cdot I(x_m < \tau) \leq \alpha \right\} \\
    &= \max \left\{\tau \in \Real  \mid  \sum_{m = f}^{b} p_m \cdot I(x_m < \tau) \leq \alpha \right\} \\
    &= \tau\opt.
  \end{align*}
  The equality \emph{(a)} holds because we just add 0.

  \item[\textbf{(ii)}] \textbf{Case 2 $\bm{\alpha > t + e}$.}
  We can also, show by contradiction that $\tau\opt$ optimal in \cref{INDUCTHYPOTH} must satisfy that
    \[
     \tau\opt > v,
   \]
   because 
    \[
      \alpha  > t + e = \sum_{m = f}^{g} p'_m  = \sum_{m = f}^b p'_m I(x'_m \le v),
    \]
  % implying that the optimal $\tau\opt \in f{:}(l-1)$.
    Moreover, $x'_m > v, \forall m \in g + 1{:}(b)$ from the partition function. 
  By the inductive hypothesis,
  \begin{align*}
    \qvar(\bm{x}', \bm{p}', \alpha - (t + e), g + 1, b)
    &= \max \left\{\tau \in \Real \mid  \sum_{m = g + 1}^{b} p'_m \cdot I(x'_m < \tau) \leq \alpha - (t + e) \right\} \\
    &= \max \left\{\tau \in \Real \mid  t + e + \sum_{m = g + 1}^{b} p'_m \cdot I(x'_m < \tau) \leq \alpha \right\} \\
    &\stackrel{\text(a)}{=} \max \left\{\tau \in \Real  \mid  \sum_{m = f}^{b} p'_m \cdot I(x'_m < \tau) \leq \alpha \right\} \\
    &= \tau\opt.
  \end{align*}
  The equality \emph{(a)} holds because we just add 0.
  \item[\textbf{(iii)}] \textbf{Case 3 $\bm{t \le \alpha < t + e}$.}
  Then $\sum_{m = f}^b p_m \cdot I(x_m < v) = t \le \alpha$ and 
  $\sum_{m = f}^b p_m \cdot I(x_m \le v) = t + e > \alpha$, 
  which implies that $v$ itself is the optimal $\tau$ that satisfies $\cref{INDUCTHYPOTH}$
  The algorithm correctly returns $v$ in this case.
\end{enumerate}

\end{proof}

\begin{corollary}
\Cref{alg:quick-quantile} computes $\quant_{\alpha}(\tilde{x})$.
\end{corollary}
\begin{proof}
The proof directly follows from \cref{PROOFCORRECTNESS}.
\end{proof}

The proof of time complexity follows directly from that for the standard quick-select algorithm in \cite{Aho1974}\cite{HorowitzSahni1978}. We note that we have slightly modified the first steps of the proof to allow for the case where multiple elements in the array are equal to the chosen of the pivot. The remainder of the proof is exactly that in \cite{Aho1974} and \cite{HorowitzSahni1978}.
\begin{proposition} \label{prop:time-complexity}
\Cref{alg:recursive-qvar} runs in expected $O(n)$ time when one use uniformly random pivot selection.
\end{proposition}


%\begin{lemma} \label{lem:T-complexity}
%If \(T(1)\le c\) then for every integer \(n\ge 1\),
\[
%T(n)\le 4 c n.
%\]
%\end{lemma}
%\begin{proof}
%We now prove the lemma by induction. 
%\emph{Base case.}
%For \(n=1\) we have \(T(1)\le c \le 4c\), so the claim holds.
%\emph{Inductive step.}
%Assume the claim holds for all positive integers less than \(n\);
%that is, assume \(T(i)\le 4 c i\) for all \(1\le i\le n-1\).
%Then
%\[
%\sum_{i=1}^{n-1} T(i) \le \sum_{i=1}^{n-1} 4 c i
%= 4c \sum_{i=1}^{n-1} i.
%\]
%Compute the arithmetic sum:
%\[
%\sum_{i=1}^{n-1} i = \frac{(n-1)n}{2}.
\]
%Hence
%\[
%\sum_{i=1}^{n-1} T(i) \le 4c\cdot\frac{(n-1)n}{2} = %2c\,n(n-1).
%\]
%Substituting this bound into the recurrence \eqref{eq:recurrence} yields
%\[
%T(n) \le c n + \frac{2}{n}\cdot \bigl(2c\,n(n-1)\bigr)
%= c n + 4c(n-1) = 4cn - 3c.
%\]
%Therefore
%\[
%T(n) \le 4cn - 3c \le 4cn,
%\]
%which establishes the inductive step.
%\end{proof}


\begin{proof}
Let $T(n)$ be the expected running time of \Cref{alg:recursive-qvar} for $n := b - f + 1$. 

Suppose $\qvar_{\alpha}(\tilde{x}, \bm{p}, \alpha, f, b)$ returns the \(k\)-th smallest element in $x_f, \dots, x_b$ (i.e. there are \(k-1\) smaller elements and \(n - k - d\) elements larger, where $d$ is the number of elements equal to $\qvar(\bm{x}, \bm{p}, \alpha, f, b)$.

Furthermore let $v$ be the \(i\)-th smallest element in $x$ (i.e. there are \(i-1\) smaller elements and \(n - i - s\) elements larger, where $s$ is the number of elements equal to $v$

Since the pivot is chosen uniformly, each element with probability \(1/n\).

Thus, the expected cost of the recursion at line 4 or 6 is 
\begin{align*}
  \frac{1}{n} \left( \sum_{i = 1}^{k} T(n - i - s) + \sum_{i = k + 1 + d}^{n} T(i - 1) \right)
  &\leq
  \frac{1}{n} \left( \sum_{i = 1}^{k} T(n - i) + \sum_{i = k + 1}^{n} T(i - 1) \right) \\
  &=
\frac{1}{n} \left( \sum_{i = n - k + 1}^{n - 1} T(i) + \sum_{i = k}^{n - 1} T(i) \right)
\end{align*}
The second equation is an upper bound and focuses on the case when there is a unique element that equals VaR and a unique element that equals the pivot   ($d = 0$ and $s = 0$). We thus focus only on the analysis of this case for the remainder of the proof.

The rest of procedure \textsc{SELECT} requires $cn$ time for some constant $c$,  
so we have the following inequality for $n \ge 2$:
\begin{equation}
T(n) \le cn + \max_{k} 
\left\{\frac{1}{n} (\sum_{i = n - k + 1}^{n - 1} T(i) + \sum_{i = k}^{n - 1} T(i) ) \right\}.
\label{eq:select_recurrence}
\end{equation}

The remaining (non-recursive) work performed by the procedure on an input of size \(n\) is \(\Theta(n)\).
Thus there exists a constant \(c>0\) such that, for all \(n\ge 2\),
\begin{equation}\label{eq:recurrence}
T(n) \le c n + \frac{2}{n}\sum_{i=1}^{n-1} T(i).
\end{equation}
By induction, \(T(n)\le 4cn\) for all integers \(n\ge 1\). This proves that the expected running time of QuickVaR is linear in \(n\).
\end{proof}

Finally, we note that with a very slight modification, the weighted median can be seen as a general
\begin{remark}
Let $n \in \mathbb{N}$, $\xi_1, \ldots, \xi_n \in \Real$, $w_1, \ldots, w_n \in \Real_{\geq 0}$, and $W \in \Real$ with 
\[
0 < W < \sum_{i=1}^n w_i.
\]
Then the $(w_1, \ldots, w_n \mid W)$-weighted median with respect to $(\xi_1, \ldots, \xi_n)$ is defined to be the unique number $\xi^*$ for which
\begin{equation}
\sum_{i : \xi_i < \xi^*} w_i \leq W < \sum_{i : \xi_i \leq \xi^*} w_i.
\label{WeightedMedianEquality}
\end{equation}
% \end{definition}
If $W = \alpha$, then $\xi^* = \quant_{\alpha}[\tilde{x}]$.
We note that our definition differs very slightly from the popular definition in \cite{Korte2012} in that the position of the $\leq$ and $>$ in \cref{WeightedMedianEquality} are swapped.
\end{remark}.

 \begin{proof}
 Let $\xi^*$ be the optimal solution to the weighted median problem and let $W = \alpha$. Then $\P{\tilde{x} < \xi^*} \leq \alpha$ and thus $\xi^*$ is feasible for \eqref{eq:var-definition}. Since $\alpha < \P{\tilde{x} \leq \xi^*}$, $\alpha < \P{\tilde{x} \leq \xi^*}$ $\forall \xi > \xi^*$ .Thus $\xi^*$ is optimal for \eqref{eq:var-definition}. Since the optimal value for VaR must be an outcome of $\tilde{x}$ by \cref{lem:quantile-in-finite-omega}, $\xi^*$ then must equal $\quant_{\alpha}[\tilde{x}]$ and the proof concludes.
 \end{proof}




\section{Computing \texorpdfstring{$\varphi$}{phi}-divergence Risk Measures in Linear Time} \label{sec:phi-divergence}


In this section, we propose new linear-time algorithms for computing $\varphi$-divergence risk measures. We approach this challenge by solving a linear optimization problem over polymatroids . As we show, for certain polymatroids this optimization can be solved in linear time compared to the $O(n \log  n)$ runtime of standard sorting based greedy algorithms. 
First, recall the standard definition of submodular functions and the related concept of polymatroid~\cite{Korte2012}.
\begin{definition} \label{def:polymatroid}
A function $f \colon 2^{\mathcal{E}} \to \Real_{\ge 0}$ for a finite set $\mathcal{E} = \left\{ 1, \dots , E \right\}$ and $E\in \Nats$ is \emph{submodular} if
\[ 
  f(\mathcal{A}) + f(\mathcal{B}) \geq f(\mathcal{A} \cup \mathcal{\mathcal{B}}) + f(\mathcal{A} \cap \mathcal{B}),
  \quad
  \forall \mathcal{A}, \mathcal{B} \subseteq \mathcal{E}.
\]
A \emph{polymatroid} $\mathcal{Q}_f$ for $f$ is a polytope defined as
  \[
    \mathcal{Q}_f := \left\{ q \in \Real_{\ge 0}^E  \mid   q(\mathcal{U}) \leq f(\mathcal{U}), \, \forall \,  \mathcal{U} \subseteq \mathcal{E} \right\},
  \]
  where $q(\mathcal{U}) := \sum_{e \in \mathcal{U}} q(e)$. %and $f(\mathcal{U})\rightarrow[0, 1]$. We note that condition $f(\mathcal{U})\rightarrow[0, 1]$ is not necessary in general, but is needed in the context of probability distributions.
  \mm{Do we need to assume that $f(\emptyset) = 0$?}
\end{definition}

% marek: we already say this in the preliminaries section
% This polytope is of special interest for computing risk measures, as we will see later on that all coherent and comonotonic risk measures can be computed by solving
% \begin{align}\label{eq:polytope}
%   \max_{\bm{q}\in \Real}\left\{\bm{x}\tr \bm{q} \mid  \bm{q} \in \mathcal{Q}_f\right\}
% \end{align}

Polymatroids are important for our purpose because many risk measures can be formulated as linear optimization over polymatroids.  
\begin{proposition} \label{prop:coherent-polymatroid}
  Suppose that $\rho\colon \mathbb{X}\to \Real$ is a comonotonic coherent risk measure (defined as $\rho$ such that $\rho(X) +  \rho(Y)= \rho(X + Y)$ when $X$ and $Y$ are comonotone). Then
\begin{equation}\label{eq:comontonic-optimization}
   \rho[\tilde{x}] = \min_{\bm{q}\in \mathcal{Q}_c} \bm{q}\tr  \bm{x},
\end{equation}
where $\mathcal{Q}_c$ is the polymatroid in \cref{def:polymatroid} for $c(\mathcal{A}) := -\rho(-\one_{\mathcal{A}})$ for each $\mathcal{A} \subseteq \Omega$. \mm{Missing a negative sign?}
\end{proposition}
\begin{proof}
  The result is an immediate consequence of \cite[Corollary 4.89]{Follmer2016}
\end{proof}

Conventional polymatroid optimization algorithms for risk measures and the closely related maximum weighted independent set (such as \cref{alg:greedy-polymatroid} in the appendix) solve the optimization problem in \cref{prop:coherent-polymatroid} using a greedy approach that requires sorting. We now show that for a special class of submodular functions $f$ as defined below, sorting is not necessary.

\begin{definition} \label{asm:submodular-function}

A \emph{submodular} function $f\colon 2^{\Omega} \to \Real_{\ge 0}$ is \emph{Element-Wise-Separable}~(EWS) if there exists $g\colon [0,1] \to  [0,1]$ such that $f(\mathcal{O}) = g(\P {\mathcal{O}}), \forall \mathcal{O} \subseteq \Omega$ and for some $c \in \Real_{\ge 0}$ and $m\in \Real_{>0}$:
\[
    g(x) =
    \begin{cases}        
    \min \{c + m  \cdot x, 1\} &\text{if } x > 0 \\
    0 &\text{otherwise}.
    \end{cases}, \quad \forall x\in [0,1]
\]
The values $m$ and $c$ are such that $c + m \ge 1$ and, therefore, $g(1) = 1$.
\end{definition}



\begin{proposition}
If $f\colon 2^{\Omega} \to  \Real_{\ge 0}$ is EWS then it is submodular.   
\end{proposition}
\begin{proof} 
We first note Theorem~4.5.1 in~\cite{Bilmes18} (proof provided in Appendix B.1) states that if $g(x)$ as defined in \cref{asm:submodular-function} is concave , $f$ is submodular. 
It remains to prove that $g$ is concave.

We first prove concavity for g on the interval $(0, 1]$, where $0$ is not included. On this interval, $g(x)$ is the minimum of two linear functions and is thus also concave.\cite{boyd2004convex}

We then prove concavity for g on the interval $[0, 1]$, where $0$ is included. If we have value $a$, $b$. and $z$ such that $a = 0$ and thus $g(a) = 0$, $b > 0$, and $z$ in $(0, 1)$ we can shot that $(1 - z)g(a) + z\cdot g(b) = z\cdot g(b) = \min(z\cdot c + m\cdot z\cdot b, z)\leq \min(c + m \cdot z \cdot b, 1) =g(z * b) = g(z*b + (1 - z)*a)$. Thus, $g$ is concave on the interval $[0, b]$ for $b > 0$.

Thus $g$ is concave, and the proof concludes.
\end{proof}



We now describe an algorithm for optimizing over polymatroids with this class of submodular function. We then show that both CVaR and TVaR can be computed by solving this optimization problem for particular EWS functions.
\begin{algorithm}
  \caption{Linear-time Algorithm for optimizing linear function on polymatroids} \label{alg:maximum-weight-independent}

  \Output{$q \in \Real^n$}

  $v \gets \operatorname{QuickVaR}(p, x, \alpha = (1 - c)/m) $ \;
  $k_{\min} \gets \min_{i = 1, \dots, n} x_i$  \tcp{Breaking ties arbitrarily}
  $\mathcal{K}_{< v} \gets \{ i = 1, \dots , n \mid x_i < v \}$ \;
  $\mathcal{K}_{= v} \gets \{ i = 1, \dots , n \mid x_i = v \}$ \;
  $\theta \gets \left( 1 - c + \sum_{k = \mathcal{K}_{< v}} m\cdot p_k\right) / (\sum_{k \in \mathcal{K}_{=v}} p_k) $   \;
  \[
    \forall i\in 1, \dots , n:
    \qquad
    q_i \gets
    \begin{cases}
      m\cdot p_i + c &\text{if } i = k_{\min} \\
      m \cdot p_i &\text{if } i \in \mathcal{K}_{< v} \wedge i \ne k_{\min} \\
      \theta \cdot  p_i  &\text{if } i \in \mathcal{K}_{=v} \\
      0 &\text{otherwise}.  \\
    \end{cases}
  \]
 \end{algorithm}

We now prove the optimality of the algorithm and it's expected linear time complexity. The proof of optimality is obtained by showing that every value we compute for $q$ is equivalent to what would be obtained by following the computational procedure defined in \cite[Remark 4.86]{Follmer2016}. The proof of the time complexity follows from the expected linear time complexity, and the fact that remaining operations can be computed in linear time, and thus have no impact on the complexity.

\begin{proposition}
  \cref{alg:maximum-weight-independent} returns a optimal solution to the optimization problem defined in \cref{comontonic optimization}. 
\end{proposition}
\begin{proof}
Assume that $x_1 \le x_2 \le  \dots  \le x_n$ .
  
  The optimal solution will always be $q_{i+1} = f(\mathcal{O}_{i+1}) - f(\mathcal{O}_{i})$ \cite[Remark 4.86]{Follmer2016}, where $\mathcal{O}_i$ is the set of all elements that have only values less than or equal to the i-th largest element:
  \[
   \mathcal{O}_i := \left\{1, \dots ,i  \right\}, \quad \forall  i = 1, \dots, n,
 \]
 and $\mathcal{O}_{0 } := \emptyset$. For $f(\mathcal{O}_{i+1}) \leq 1$ and $f(\mathcal{O}_i) < 1$ and $\mathcal{O}_i \neq \emptyset$ \mm{???}



\begin{align*}
    f(\mathcal{O}_{i + 1}) - f(\mathcal{O}_i) &= c  +m \cdot \sum_{k = 1}^{i + 1}p_{k} - (c  +m \cdot \sum_{k = 1}^{i}p_{i})\\
    &= m \cdot p_{i+1}.
\end{align*}


We know from \eqref{eq:var-definition} that $v$ is the largest $\tau$ such that $P[x < \tau] \leq \frac{1 - c}{m}$. 
Thus, for any $i$ such that $x_i < z$ and $i \neq j$, $(c  +\sum_{j = 1}^{i}m \cdot p_j) \leq 1$
and thus $q_i$ must equal $m*p_i$.

  




For $f(\mathcal{O}_{i+1}) \leq 1$ and $f(\mathcal{O}_i) = 0$


\begin{align*}
    f(\mathcal{O}_{i + 1}) - f(\mathcal{O}_i) &= c  +\sum_{k = 1}^{i + 1}m\cdot p_{k}
\end{align*}

Thus, for any $i$ such that $x_i < z$ and $i = j$, $q_i$ must equal $m*p_i + c$.




For all other $i$ such that $x_i> z$, we note that $P[x < \tau] > \frac{1 - c}{m}$ indicates $f(\mathcal{O}_{i}) - f(\mathcal{O}_{i - 1}) = 1 - 1 = 0$, and thus $q_i = 0$.

For all other $i$ such that $x_i=z$, since all $q_i$ must sum to 1, it can be seen for all such $i$, $\sum_{k : x_k =  v}p_k$ must equal $y$. The solution of the LP is not affected by which $i$ this sum is distributed, so we set $x_z$ to $y$ and everything else to 0. 


\end{proof}

\begin{proposition}
\cref{alg:maximum-weight-independent} run with expected complexity $O(N)$.
\end{proposition}
\begin{proof}
The quick quantile call runs in expected linear time complexity as shown in \cref{prop:time-complexity}. After the call to QuickQuantile, the number of operations is bounded by the number of elements in $x_0$ and $p_0$ and thus is linear time as well. 
\end{proof}

\subsection{CVaR}
We now prove that CVaR can be represented as a polymatroid with a submodular function that satisfies Element-Wise-Seperability as defined above. Therefore we can provably compute CVaR in expected linear time.

\begin{proposition}
Let $f\colon 2^{\Omega} \to \Real_{\ge 0}$ be defined as
\[
  f(\mathcal{O})
  :=
  \min\left\{\frac{1}{\alpha} \cdot \P{\mathcal{O}}, 1\right\},
\quad \forall \mathcal{O} \subseteq \Omega .
\]
Then $f$ is submodular and the polymatroid $\mathcal{Q}_f$ satisfies
\[
\cvar{\alpha }{\tilde{x} } = \max \left\{\bm{x}\tr \bm{q} \mid  \bm{q} \in \mathcal{Q}_f\right\}.
\]
Moreover $f$ also satisfies Element-Wise-Separabaility with $m = 1/\alpha$ and $c = 0$ 
\end{proposition}
\begin{proof}
\[
\text{HO:}
\]

\[
\min\left\{ \frac{1}{\alpha} I_0^{\top} p,\; 1 \right\}
=
\max \left\{ I_0^{\top} q \;\middle|\;
\begin{array}{l}
q \ge 0, \\
I_0^{\top} q = 1, \\
q \le \frac{1}{\alpha} p
\end{array}
\right\}
\]

\bigskip

$\underline{\text{Case 1:}} \quad I_0^{\top} p \ge \alpha$

\[
1 = \max \left\{ I_0^{\top} q \;\middle|\;
\begin{array}{l}
q \ge 0, \\
I_0^{\top} q = 1, \\
q \le \frac{1}{\alpha} p
\end{array}
\right\}
\]

\begin{itemize}
\item[(a)] $\le$: \quad $I_0^{\top} q = I_0^{\top} q \le 1$
\item[(b)] $\ge$: by construction of $q$, \quad
$q = \frac{1}{I_0^{\top} p} \, p \; (\le \frac{1}{\alpha} p)$
\end{itemize}

\bigskip

$\underline{\text{Case 2:}} \quad I_0^{\top} p < \alpha$

\[
\frac{1}{\alpha} I_0^{\top} p
=
\max \left\{ I_0^{\top} q \;\middle|\;
\begin{array}{l}
q \ge 0, \\
I_0^{\top} q = 1, \\
q \le \frac{1}{\alpha} p
\end{array}
\right\}
\]

\begin{itemize}
\item[(a)] $\le$: \quad $I_0^{\top} q \le \frac{1}{\alpha} I_0^{\top} p$
\item[(b)] $\ge$: \quad
\end{itemize}

\bigskip

\[
d = 1 - \frac{1}{2} I_0^{\top} p
\]

\[
\text{(b)} \quad
q = \frac{1}{\alpha} p \otimes \delta
+ \left( 1 - \frac{1}{\alpha} \right) \delta (1 - \delta)
\]

\begin{itemize}
\item[(b.1)] $I_0^{\top} q = \frac{1}{\alpha} I_0^{\top} p$
\item[(b.2)] $q \in \mathcal{Q}$ \quad (check the constraints)
\end{itemize}


% Note: From here on, we will use $\setminus$ to denote the complement of a set.

%We first show that for any $q$ in $\mathcal{Q}_f$, %$q(\mathcal{O})$ $\leq f(\mathcal{O})$. This directly follows %from the constraint set in definition 2 of CVaR where we see that %$q_i \leq ((1/\alpha)\cdot p_i)$ for any element $i$ in the %distributions $p$ and $q$.
  

%We also show below, we can construct $q$ such that %$q(\mathcal{O}) =\min\left\{\frac{1}{\alpha} \cdot %\P{\mathcal{O}}, 1\right\} $ for any $\mathcal{O}$. 
%\item[Case 1]: $\P{\mathcal{O}} \geq \alpha$. 
%In this scenario, we aim to construct $q(\mathcal{O})$ = 1 since %$\frac{1}{\alpha}\cdot \P{\mathcal{O}} \geq 1 $. Thus, every %$q_i$ such that $i$ represents an element in $\mathcal{O}$ should %be set to $p_i \cdot \frac{1}{\P{\mathcal{O}}}$ and all other %$q_i$ should be set to 0 . Since  $\P{\mathcal{O}} \geq \alpha$, %we can imply the reciprocal $\frac{1}{\P{\mathcal{O}}} \leq %\frac{1}{\alpha}$ is also true given $\P{\mathcal{O}} \geq \alpha %> 0$. Therefore, every $q_i$ is feasible given definition 2.

%\item[Case 2]: $\P{\mathcal{O}} < \alpha$. 
%In this scenario, since $\frac{1}{\alpha} \cdot  \P{\mathcal{O}} %< 1$ . Then we construct $q$ such that $q(\mathcal{O}) = \frac{1}%{\alpha} \cdot  \P{\mathcal{O}}$. 
%  Thus, every $q_i$ such that $i$ represents an element in $\mathcal{O}$ should be set to $\frac{1}{\alpha} \cdot p_i$ and $p_i \cdot  d$ for all other $i$ such that $\frac{1}{1 - \alpha} \cdot \P{\mathcal{O}} + d \cdot \P{\mathcal{O}\setminus} = 1$, where $ \mathcal{O}\setminus$ contains all elements not in $\mathcal{O}$. 
  
  %We now show  that $d \leq \frac{1}{\alpha}$, which is required for the constructed $Q$ to be feasible. For all feasible $\alpha \geq 0$, $\frac{1}{\alpha} \geq 1$. Since $\P{\mathcal{O}} + \P{ \mathcal{O}\setminus} = 1$,  $d \leq 1$ in order for $\frac{1}{\alpha} \cdot  \P{\mathcal{O}} + d\cdot\P{\mathcal{O}\setminus} = 1$ to hold. Since $1 \leq \frac{1}{\alpha}$, this implies $d \le \frac{1}{\alpha}$.
  
  
  %We have now shown that $q( \mathcal{O}) \leq f(\mathcal{O}) = \min\left\{\frac{1}{\alpha} \cdot \P{\mathcal{O}}, 1\right\}$ and it is possible to construct $Q(\mathcal{O})$ such that $Q(\mathcal{O}) = \min\left\{\frac{1}{\alpha} \cdot \P{\mathcal{O}}, 1\right\}$. Thus by Remark 4.90 in \cite{Follmer2016}, $f(\mathcal{O}) = \min\left\{\frac{1}{\alpha} \cdot \P{\mathcal{O}}, 1\right\}$
\end{proof}


\subsection{TVaR}

(TVaR)~\cite{Kruse2021}

We remind the reader that in this work we define TVaR as the $\varphi$-divergence risk that uses the $L_1$ norm divergence. This may not seem very interpretable, however we can show that we can set the divergence constraint, or $\beta$, such that it can be a provable lower bound for a given EVaR with $\alpha$ value between 0 and 1.
From previous work , we know that the KL divergece is $\leq \frac{1}{2 \log(2)} \, \|p - q\|_1^2$ \cite{Iyengar2005}. Thus, by algebraic manipulation it can be seen that the coherent risk measure TVaR with the divergence constraint $\|p - q\|_1 \leq \sqrt{2 \log(2) *-\log(\alpha)}$  must be $\leq EVaR_\alpha[\tilde{x}]$. 

We now prove the submodular function for TVaR and show that it also satisfies ElementWiseSeperability as defined above.
\begin{proposition}
    

For $\mathcal{O} \subseteq \Omega$, define $f_{\beta}\colon 2^{\Omega} \to \Real_{\ge 0}$:
  \[
    f_{\beta}(\mathcal{O}) = \min\left\{\P{\mathcal{O}} + \frac{\beta}{2}, 1\right\}.
  \]
If $\mathcal{O} \neq \emptyset$, and $0$ otherwise, where $\beta = \sqrt{ 2 \log 2 \cdot \log \frac{1}{\alpha}}$ for $\alpha\in (0,1)$ represent the divergence constraint. Then
\[
  \tvar{\alpha }{\tilde{x} } = \max \left\{\bm{x}\tr \bm{q} \mid  \bm{q} \in \mathcal{Q}_{f_{\beta}}\right\}.
  \]
$f_{\beta}$ also satisfy ElementWiseSeparability with $m = 1$ and $c = \beta/2$.
\end{proposition}
\begin{proof}
 
  We first note that $\mathcal{O} = \emptyset$, then $Q(\mathcal{O}) = 0$,   Due to the set of constraints, we know that for all $ \mathcal{O} \text{ such that } \mathcal{O} \neq \emptyset,  q$ must be such that $Q({\mathcal{O}}) \leq {\P{\mathcal{O}}} + \frac{\beta}{2}$ as if $Q(\mathcal{O}) \geq \frac{\beta}{2} +  \P{\mathcal{O}}$ , then $Q(\mathcal{O'})$ must be $< \P{\mathcal{O}} - \frac{\beta}{2}$ in order for the sum constraint of the simplex to equal 1, thus violating the l1 Norm constraint.
  
  Furthermore, as we show below, we can construct $q$ such that  for any $\mathcal{O} \neq \emptyset$,   
  \[f(\mathcal{O}) = \min\left\{\P{\mathcal{O}} + \frac{\beta}{2}, 1\right\}
    .
  \].
  
\item[Case 1]: $\P{\mathcal{O}} + \frac{\beta}{2} \geq 1$. In this scenario, we pick an element in $\mathcal{O}$ and add $1 - \P{\mathcal{O}}$ to it. We set all elements not in $\mathcal{O}$ to 0.

\item[Case 2]: $\P{\mathcal{O}} + \frac{\beta}{2}< 1$. In this scenario, we pick an element in $\mathcal{O}$ and add $\frac{\beta}{2}$ to it. We set all elements not in $\mathcal{O}$ to 0.

We have now shown that $Q(\Omega) \leq f(\mathcal{O}) = \min\left\{\frac{1}{\alpha} \cdot \P{\mathcal{O}}, 1\right\}$ and it is possible to construct $Q(\Omega)$ such that $Q(\Omega) = \min\left\{\frac{1}{\alpha} \cdot \P{\mathcal{O}}, 1\right\}$. Thus by Remark 4.90 in \cite{Follmer2016}, $f(\mathcal{O}) = \min\left\{\frac{1}{\alpha} \cdot \P{\mathcal{O}}, 1\right\}$
\end{proof}

\section{Experiments}\label{sec:experiments}

To evaluate the algorithms developed in \cref{alg:quick-quantile} we numerically compare their speed up against their sorting based counterparts. For the remainder of the paper we prefix our implementation of the algorithms with a ``Q'' to denote the quick algorithms.

Our experimental design allows us to verify that our implementation performs well in practice rather than only theoretically. \cref{fig:all_uniform} shows the results of our experiments. The random variables were generated from a uniform distribution with $N$ samples, where $N$ is varied from $10^2$ to $10^6$. The algorithms were run for each value of $N$ 10 times, and the average runtime was recorded. For consistency, we used a risk level of $0.95$, however the results are similar for other risk levels save for $\alpha = 0 \text{ or } 1$.

The results in \cref{fig:all_uniform} represent a 95\% confidence interval band computed over 10 random runs, each with separately generated random variable. The confidence bands are tight due to the little variance in the measured runtimes between runs. Note that the figure shows the timing results in both linear and log scales.

The results confirm that our quick algorithms are significantly faster than their sorting based counterparts. Especially in the case of CVaR and VaR which are extensively used in the industries aforementioned in \cref{sec:intro}. As expected, the speedup is logarithmic in the runtime of the quick algorithms which is consistent with the theoretical complexity of the algorithms, see \cref{sec:appendix_experiments}. This log speed up coincides with a noticeable speed-up in runtime across the tested random variable sizes. 

The computation of risk measures is often done over large random variables where the runtime and sensitivity of the algorithms to the size of the random variable is a concern. The results presented in this section are chiefly concerned with demonstrating our ability to compute risk measures quickly, fulfilling the practical need.

\begin{figure}
  \includegraphics[width=0.5\textwidth]{./code/plots/all_slow_vs_fast_uniform_linear.pdf}
  \includegraphics[width=0.5\textwidth]{./code/plots/all_slow_vs_fast_uniform_log.pdf}
  \caption{Comparison of runtime of the algorithms for samples from a \emph{uniform} random variable with linear (left) and logarithmic (right) view.}
  \label{fig:all_uniform}
\end{figure}

\begin{table}
\centering
\caption{Runtime comparison for the Stock Market scenario.}
\begin{tabular}{lrr}
\toprule
Method & Mean (ms) & St. dev. (ms) \\
\midrule 
CVaR & 0.3912 & 0.0382 \\ 
\textbf{QuickCVaR} & \textbf{0.1426} & \textbf{0.0307} \\ 
\midrule 
VaR & 0.3733 & 0.0321 \\ 
\textbf{QVar} & \textbf{0.1199} & \textbf{0.0287} \\ 
\midrule 
TVaR & 0.3993 & 0.0414 \\ 
\textbf{QuickTVaR} & \textbf{0.1786} & \textbf{0.0356} \\ 
\midrule 
Expectation & 0.0144 & 0.0124 \\ 
\bottomrule
\end{tabular}
\end{table}

\section{Conclusion}

We have formulated and demonstrated the correctness of a new algorithm for computing the maximum weight independent set for polymatroids without sorting which allows us to solve many common risk measures such as CVaR, VaR, and TVaR in $O(N)$ time.
This speed up is significant in the context of risk measures as they are often used in large scale applications such as finance, healthcare and robotics.


\bibliography{fastcvar.bib}

\appendix

\section{Proofs and Details for \cref{sec:var-linear}}

% \begin{algorithm}
% \algrenewcommand\algorithmicrequire{\textbf{Input: Random variable $x \in \Real^n$ with probability mass function $p \in \Delta^n$ and $f, b \in \Real$}}
% \algrenewcommand\algorithmicensure{\textbf{Output: indices of partition $i, j \in \Real$}}
% \caption{Hoare's partition scheme}\label{alg:partition}
%   \begin{algorithmic}[1]
%     \Require{}
%     \Ensure{} 
%     \State{$i \gets f + \lceil\frac{b-f}{2}\rceil$}
%     \State{$\lambda \gets f$} % less than
%     \State{$\xi \gets f$} % equal to
%     \State{$\mu \gets b$} % greater than
%     \While{$\xi \leq \mu$}
%       \If{$x[\xi] < x[i]$}
%         \State{swap $x[\xi]$ with $x[\lambda]$}
%         \State{swap $p[\xi]$ with $p[\lambda]$}
%         \State{$\lambda \gets \mu + 1$}
%         \State{$\xi \gets \xi + 1$}
%       \ElsIf{$x[\xi] > x[i]$}
%         \State{$\mu \gets \mu - 1$}
%       \Else{}
%         \State{$\xi \gets \xi + 1$}
%       \EndIf{}
%     \EndWhile{}
%     \State{\Return{($\lambda - 1$, $\mu$)} \Comment{last element less than the pivot, first element greater than the pivot}}
%   \end{algorithmic}
% \end{algorithm}
%

\subsection{Proof of \cref{lem:quantile-in-finite-omega}}
\begin{proof}
Recall the definition:
 \begin{align} \label{eq:var-definition-close}
  \quant_{\alpha}^+(\tilde{x})
  \;:=\;
    \max\; \left\{\tau \in \Real \mid \P{ \tilde{x} < \tau } \le \alpha\right\},
 \end{align}

Let $x[\omega_1] \leq x[\omega_2] \leq \dots \leq x[\omega_n]$ without loss of generality. Suppose for the sake of contradiction that there is no $\omega\in \Omega $ such that $\quant^+_{\alpha}(\tilde{x}) = \tilde{x}[\omega]$. Then since $\quant^+_{\alpha}(\tilde{x}), x[\omega] \in \Real$ and $\Omega$ is finite, there are only 3 cases to consider.

\begin{enumerate}
\item[Case 1]: $\quant^+_{\alpha}(\tilde{x}) = \tau\opt \in (x[\omega_i], x[\omega_{i + 1}])$.
Since there are no $\omega\in \Omega$ such that $x[\omega] \in  [\tau\opt , x[\omega_{i+1}])$:

  \[
    \P{\tilde{x} < x[\omega_{i+1}]} = \P{\tilde{x} <\tau\opt} \le \alpha.
  \]
Therefore, $x[\omega_{i+1}] > \tau\opt$ is feasible in \eqref{eq:var-definition-close} contradicting the optimality of $\tau\opt$.
\item[Case 2]: $\quant^+_{\alpha}(\tilde{x}) = \tau\opt < x[\omega_1]$. Since there is no $\omega\in \Omega$ such that $x[\omega] \in (-\infty, x[\omega_1])$:
  \[
    \P{\tilde{x} < x[\omega_{1}]} = \P{\tilde{x} <\tau\opt} = 0 \le \alpha.
  \]
  Therefore, $x[\omega_1] > \tau\opt$ is feasible in \eqref{eq:var-definition-close} contradicting the optimality of $\tau\opt$.

\item[Case 3]: $\quant^+_{\alpha}(\tilde{x}) = \tau\opt > x[\omega_n]$.
  For all $\tau \ge \tau\opt$
  \[
    \P{\tilde{x} < \tau } \ge  \P{\tilde{x} < \tau\opt } = 1 > \alpha.
  \]
  Therefore, $\tau\opt$ cannot be optimal in~\eqref{eq:var-definition-close} since $\alpha < 1$. %, which contradicts the hypothesis.
  
\end{enumerate}
By similar reasopning one can show that $\quant_{\alpha}^-(\tilde{x})$ is also in the set of values of $\tilde{x}$, i.e. there exists $\omega\in \Omega$ such that $\quant_{\alpha}^-(\tilde{x}) = \tilde{x}[\omega]$.
\end{proof}


\subsection{Standard VaR Algorithm} \label{sec:stand-var-algor}

\begin{algorithm}
\caption{VaR: Standard VaR algorithm}
\label{alg:slow-quantile}
\Input{vector $\bm{x}$, probability $\bm{p}$, and $\alpha \in [0,1)$}
\Output{$\alpha$-quantile of $\tilde{X}$}
Sort $\bm{x}$ indexes to get $\bm{k}\in \Nats^n$ such that $i > j \implies x_{k_i} > x_{k_j}, \; \forall i, j \in 1, \dots , n$\;
$q_{n+1} \gets 0$ \;
\For{$i=n, \dots , 1$} {
    $q_i \gets q_{i+1} + p_{k_i}$
    \tcp*{$q_i = \P{\tilde{x} \ge x_{k_i}}$ if $x_{k_i}$ unique}
    \lIf{$q_i \ge 1-\alpha$} {\KwRet{$x_{k_i}$}}
}
\end{algorithm}

\begin{proposition}
For a random variable $\tilde{x} \in \mathbb{X}$ and $\alpha\in [0,1)$, \cref{alg:slow-quantile} returns $\var{\alpha}{\tilde{x}}$.
\end{proposition}
\begin{proof}
First, we show by induction on $i$ from $n$ to $1$ that
\begin{equation} \label{eq:proof-q-j}
  q_j = \P{\tilde{x} \ge x_{k_i}},
  \quad\text{where}\quad
  j = \min \left\{ j = 1, \dots , i \mid x_{k_j} = x_{k_i} \right\}.
\end{equation}
The base case is immediate. The inductive case for $i-1$ from $i$ follows by algebraic manipulation from the fact that $x_{k_i}$ is sorted increasingly:
\begin{align*}
\P{\tilde{x} \ge x_{k_{i-1}}} 
 &= \P{\tilde{x} = x_{k_{i-1}}} + \P{\tilde{x} > x_{k_{i-1}}}
 = \P{\tilde{x} = x_{k_{i-1}}} + \P{\tilde{x} \ge x_{k_{i}}} \\
 &= \P{\tilde{x} = x_{k_{i-1}}} + q_i = \sum_{l=j}^{i-1} q_{k_j} + q_{k_i} = q_{k_{i-1}}.
\end{align*}
Now, suppose that \cref{alg:slow-quantile} terminates with value $q\opt$. Then, if the algorithm terminates in iteration $i\opt$ then from \eqref{eq:proof-q-j}, we have that $\P{\tilde{x} \ge q\opt} = q_{j\opt} \ge 1-\alpha$ where $j\opt$ is defined as in \eqref{eq:proof-q-j}. Therefore, $q\opt \le \var{\alpha}{\tilde{x}}$. To prove the converse inequality, suppose that $q\opt > \var{\alpha}{\tilde{x}}$, and therefore $\P{\tilde{x} \ge q\opt} < 1-\alpha$. Then, this is a violation since \eqref{eq:proof-q-j} combined with the termination criterion implies that $\P{\tilde{x} \ge q\opt} < 1-\alpha$.
\end{proof}

\subsection{Partition Algorithm}

\mm{We should describe the intuition for the partition algorithm here somewhere}
 
\begin{algorithm}
  \caption{\texttt{partition}: Dutch national flag algorithm ~\cite[chapter 14]{Dijkstra1976}}
  \label{alg:partition}
  \Input{Random variable $\bm{x} \in \Real^n$ with probability mass function $\bm{p} \in \Delta^n$, pivot value $v\in \Real$, and front and back indices $f, b \in \Nats$}
  \Output{indices $l$, $g$ and $\bm{x}$ and $\bm{p}$ such that
    $x_j < v, \, j\in f{:}(l-1), \quad x_j = v, \, j \in l{:}g, \quad x_j > v, \, j \in (g+1){:}b $}
  % $i \gets f + \lceil\frac{b-f}{2}\rceil$ \tcp*{Pivot: Median-of-medians for $O(b-f)$ runtime}
  $l_0 \gets f$ \tcp*{invariant: less: $x_j < v, \, j\in f{:}(l-1)$}
  $e_0 \gets f$ \tcp*{invariant: equal: $x_j = v, \, j \in l{:}(e-1)$ }
  $g_0 \gets b$ \tcp*{invariant: greater: $x_j > v, \, j \in (g+1){:}b$}
  $k \gets 0$ \;
  
  \While{$e_k \leq g_k$}
  {
  \label{line:partition-while}
    \tcp{invariants hold here}
    \If{$x_{e_k} < v$}{
      swap: $x_{e_k} \leftrightarrow x_{l_k}$ and $p_{e_k} \leftrightarrow p_{l_k}$\; \label{line:swap1}
      $l_{k+1} \gets l_k + 1$ \;
      $e_{k+1} \gets e_k + 1$\; \label{line:partition-e-1-1}
    }
    \ElseIf{$x_{e_k} > v$}{
      swap: $x_{e_k}\leftrightarrow x_{g_k}$ and $p_{e_k} \leftrightarrow p_{g_k}$\; \label{line:swap2}
      $g_{k+1} \gets g_{k} - 1$\; \label{line:parition-g-1}
    }
    \lElse {$e_{k+1} \gets e_{k} + 1$} \label{line:partition-e-1-2}
    $k \gets k + 1$
  }
  \KwRet{$(l_k, g_k, \bm{x}, \bm{p})$}\; 
  %\tcp{last element less than the pivot, first element greater than the pivot}
\end{algorithm}


\begin{lemma} \label{lem:hoare-partition}
Suppose that for $\bm{x}'\in \Real^n$, $\bm{p}'\in \Delta^n$ and $f, b\in \Nats$ such that $f < b$ $(l, g, \bm{x}, \bm{p}) \gets \operatorname{partition}(\bm{x'}, \bm{p'}, i, f, b)$ is computed using \cref{alg:partition}. Then:
\begin{align}
   j \in f{:}(l-1) &\quad\implies \quad x_j < x_i', \label{eq:hoare-less-than}\\
   j \in l{:}g &\quad\implies \quad x_j = x_i', \label{eq:hoare-equal-to}\\
   j \in (g+1){:}b &\quad\implies \quad x_j > x_i', \label{eq:hoare-greater-than}\\
   \forall \; j, j' \in [1\dots n]&\quad x_j = x'_{j'} \iff p_j = p'_{j'}. \label{eq:hoare-correspond}
\end{align}
Moreover, the algorithm runs in $O(b - f)$ time.
\end{lemma}
\begin{proof}
Let $v$ be an input to \cref{alg:partition} and consider the following invariants.
\begin{align}
\label{eq:ho-inv-less}   x_j &< v, & j\in f_k{:}(l_k-1), \\
\label{eq:ho-inv-equal}  x_j &= v, & j \in l_k{:}(e_k-1), \\
\label{eq:ho-inv-more}   x_j &> v, & j \in (g_k+1){:}b_k.
\end{align}
  
\emph{Initialization}. For $k=0$, conditions \eqref{eq:ho-inv-less}, \eqref{eq:ho-inv-equal}, \eqref{eq:ho-inv-more} are true because the intervals are empty. 
  
\emph{Maintenance}. At each iteration of the loop that starts on line \ref{line:partition-while} of \cref{alg:partition} there are 3 possible branches determined by the conditional logic. $x_{e_k} < v$, $x_{e_k} > v$ and $x_{e_k} = v$. Since $v, x_{e_k} \in \Real$ one of these branches must be executed for each iterate $k$.

  Each time the $x_{e_k} < v$ or $x_{e_k} > v$ branches are executed conditions \eqref{eq:ho-inv-less} and \eqref{eq:ho-inv-more} remain true respectively since in each case we are swapping neccecary elements to preserve each condition. Notice also that $e_k$ is incremented alongside $l_k$ to maintain $e_k - l_k = e_{k+1} - l_{k+1}$ as is necessary for condition \eqref{eq:ho-inv-equal}.

  When $x_{e_k} = v$, $e_k$ is incremented, since $e_0 = l_0$ this creates a difference between $e_{k+1}$ and $l_{k+1}$, the algorithm terminates when $g_t = e_t$, $g_t - l_t = e_t - l_t > 0$ therefore \eqref{eq:ho-inv-equal} remains true.   

  Condition \eqref{eq:hoare-correspond} remains true for each mutation that may occur as both the $x$ values and $p$ values are swapped at the same indices, see lines \ref{line:swap1} and \ref{line:swap2}.
  
  \emph{Termination}. During the loop on line \ref{line:partition-while} there are 3 possible paths of execution determined by the conditional logic $x_{e_k} < v$, $x_{e_k} > v$ and $x_{e_k} = v$. Each of these paths of execution decreases $g_k - e_k$ by 1. This occurs on lines \ref{line:partition-e-1-1} and \ref{line:partition-e-1-2} by increasing $e_k$ by 1 and keeping $g_k$ the same. Or on line \ref{line:parition-g-1} by decreasing $g_k$ by 1 and keeping $e_k$ the same.
  
  $g_0 - e_0 = b - f$ is positive since $f<b$ by assumption. $g_k-e_k$ decreases by 1 at each iteration, and each iteration requires constant time to complete. The function returns when $g_t - e_t = 0$, therefore this algorithm completes in $O(b-f)$ time. 

  Notice on the last iteration $g_{t} = e_{t}$, therefore by the same reasoning as above $g_{t+1} - e_{t+1} = -1$ and 
  condition \eqref{eq:ho-inv-equal} holds for $g_{t+1} = e_{t+1} - 1$.
  
\end{proof}
\section{Proofs and Details for \cref{sec:phi-divergence}}
\begin{lemma}
This lemma is Theorem 4.5.1 from \cite{Bilmes18}: Given a ground set $V$. The following two are equivalent:
\begin{enumerate}
    \item For all modular functions $m \colon  2^V \rightarrow \Real_{\ge 0}$, the function $f\colon  2^V \rightarrow \Real$ defined as
    \[
    f(A) = g(m(A))
    \]
    is submodular.
    \item $g \colon  \Real_{\ge 0} \rightarrow \Real$ is concave.
\end{enumerate}

\end{lemma}

\begin{proof}
Note the proof is not the same provided in \cite{Bilmes18}, but we feel this proof is easier. It is well known that concave functions are subadditive - that is for any concave function $g$ and two values $y$ and $z$, $g(y) + g(z) \geq g(y + z)$ on the interval $[0, \infty)$. Given $A \subseteq B \subseteq E $, it follows $0 \leq a = m(A) \leq b = m(B)$. Therefore it follows that $f(A) + f(B) = g(a) + g(b) \geq g(a + b) = f(A \cup B)$. Thus it follows from the definition of sub-modularity that $f$ is submodular.

\end{proof}


% \section{No-sort Polymatroid}

% Our first contribution new algorithm for solving the maximum weight Independent Set without sorting for polymatroids. We note that certain additional assumptions have to be satisfied on the submodular function as follows.
% \begin{assumption}
% Assume for $P_f$ that $f(U)$ is such that for all $S_i, S_{ i+1 } \cup E$ where $S_i \cup (i + 1) = S_{ i+1 } $ and $f(S_{ i+1 }) < f(E)$, $f(S_i) + f({i + 1}) = f(S_{ i+1 })$.
% \end{assumption}
% We note this assumption essentially state that the submodular function must be a piecewise linear function. 
% \begin{algorithm}
%   \todo{fix typos}
%     \algrenewcommand\algorithmicrequire{\textbf{Input: }Finite $N = [1 \ldots n]$, non-decreasing submodular function $f : 2^N \to \Real$ satisfying $f(\emptyset) = 0$ and $f(N) = 1$} 
%     \algrenewcommand\algorithmicensure{\textbf{Output: $\max \{f(S) : S\subseteq N\}$}}
%   \caption{algorithm}{New algorithm}
%  \begin{algorithmic}[1]
%      \Require{}
%      \Ensure{}
  
%     \State{$left \gets 1$}
%     \State{$right \gets n$}
%     \While{$right - left \geq 1$}
    
%     \State{$mid \gets partition(left, right, c)$}
%     \If{$f(S_{mid}) = f(N)$}
%        \State{Let $x_i :=  \begin{cases}0 & \text{if  } f(S_{mid}) = f(N) \text{ for } i \geq mid \\ f(i) \end{cases}$}
%     \Else
%           \State{Let $x_i :=  \begin{cases}f(i) & \text{for } i \leq mid \end{cases}$}
%     \EndIf{}      
    
    
    
%     \EndWhile{}
%     \State{$x_{right} \gets f(N) - f(S_{right - 1})$}
% \end{algorithmic}
% \end{algorithm}
% The proof for this algorithm is simple and shows that output is equal to that of Algorithm 4 (the standard greedy algorithm), which has already proved to be both feasible and optimal.
% \begin{lemma}
% Algorithm 1 returns a feasible and optimal solution to the maximum weight independent set for polymatroids.
% \end{lemma}
% \begin{proof}
% The optimal solution will always be $f(S_i) - f(S_{i-1})$ assuming the weight $c_i \geq 0$. Our solution is $f(i)$ for $f(S_i) < f(N)$, and by assumption 1 this equals $f(S_i) - f(S_{i - 1})$. If $f(S_i) = f(N)$ and $f(S_{i - 1}) = f(N)$, then $x_i = 0$ by the algorithm.
% \end{proof}

% \section{Comonotonicity of Risk}
%
% \begin{definition}
% Two random variables $\tilde{x}$ and $\tilde{y}$ are said to be comonotonic if
%   \begin{align*}
%     (\tilde{x}(\omega) - \tilde{x}(\omega'))(\tilde{y}(\omega) - \tilde{y}(\omega')) \geq 0 \quad \forall \; \omega, \omega' \in \Omega.
%   \end{align*}
% \end{definition}
% \begin{definition}
%   A risk measure is said to be comonotonic if given comonotonic random variables $\tilde{x}$ and $\tilde{y}$
%   \begin{align*}
%     \rho(\tilde{x} + \tilde{y}) = \rho(\tilde{x}) + \rho(\tilde{y}).
%   \end{align*}
% \end{definition}
%
% \begin{table}
% \begin{tabular}{lll}
%   \textbf{Risk Measure} &  \textbf{Comonotone?} & \\
%   CVaR &  Yes & \\
%   VaR &   Yes & \\
%   TVaR &  Yes & \\
%   EVaR &  No & \\
%   Expectile & No &
% \end{tabular}
% \caption{\mm{This table is out of place; probably better to just include the information in the text}}
% \end{table}

\section{Experiments}\label{sec:appendix_experiments}
\begin{figure}
  \includegraphics[width=0.5\textwidth]{./code/plots/all_slow_vs_fast_sparse_linear.pdf}
  \includegraphics[width=0.5\textwidth]{./code/plots/all_slow_vs_fast_sparse_log.pdf}
  \caption{Comparison of runtime of the algorithms for samples from a \emph{sparse} random variable with linear (left) and logarithmic (right) view.}
  \label{fig:all_sparse}
\end{figure}
Code and results for these experiments as well as the implementations of all contained risk measures can be found at\todo{REDACTED}.


\begin{table} 
\begin{tabular}{lccccccc}
\toprule
 Size of Prob. & CVaR & QuickCVaR & VaR & QuickVaR & TVaR & QuickTVaR & Expectation \\
Space (n) &  &  &  &  &  &  &  \\
\midrule
1000 & 0.03 ± 0.01 & \textbf{0.01 ± 0.01} & 0.03 ± 0.01 & \textbf{0.01 ± 0.01} & 0.03 ± 0.01 & \textbf{0.02 ± 0.01} & 0.00 ± 0.00 \\
2000 & 0.07 ± 0.01 & \textbf{0.03 ± 0.02} & 0.06 ± 0.01 & \textbf{0.02 ± 0.01} & 0.06 ± 0.01 & \textbf{0.03 ± 0.01} & 0.00 ± 0.00 \\
3000 & 0.10 ± 0.01 & \textbf{0.04 ± 0.02} & 0.09 ± 0.02 & \textbf{0.03 ± 0.02} & 0.10 ± 0.01 & \textbf{0.05 ± 0.01} & 0.00 ± 0.00 \\
4000 & 0.14 ± 0.01 & \textbf{0.05 ± 0.03} & 0.13 ± 0.03 & \textbf{0.04 ± 0.03} & 0.13 ± 0.01 & \textbf{0.07 ± 0.02} & 0.00 ± 0.00 \\
5000 & 0.18 ± 0.02 & \textbf{0.06 ± 0.03} & 0.17 ± 0.01 & \textbf{0.04 ± 0.03} & 0.17 ± 0.02 & \textbf{0.08 ± 0.02} & 0.00 ± 0.00 \\
6000 & 0.21 ± 0.02 & \textbf{0.07 ± 0.04} & 0.20 ± 0.02 & \textbf{0.06 ± 0.04} & 0.21 ± 0.02 & \textbf{0.10 ± 0.03} & 0.00 ± 0.01 \\
7000 & 0.25 ± 0.01 & \textbf{0.09 ± 0.05} & 0.24 ± 0.02 & \textbf{0.06 ± 0.04} & 0.24 ± 0.02 & \textbf{0.12 ± 0.03} & 0.01 ± 0.00 \\
8000 & 0.29 ± 0.02 & \textbf{0.09 ± 0.05} & 0.27 ± 0.02 & \textbf{0.07 ± 0.05} & 0.28 ± 0.02 & \textbf{0.13 ± 0.04} & 0.01 ± 0.00 \\
9000 & 0.33 ± 0.02 & \textbf{0.11 ± 0.06} & 0.31 ± 0.02 & \textbf{0.08 ± 0.06} & 0.32 ± 0.02 & \textbf{0.14 ± 0.04} & 0.01 ± 0.00 \\
10000 & 0.37 ± 0.02 & \textbf{0.11 ± 0.06} & 0.34 ± 0.02 & \textbf{0.09 ± 0.06} & 0.35 ± 0.03 & \textbf{0.16 ± 0.05} & 0.01 ± 0.00 \\
\bottomrule
\end{tabular}
\caption{Mean time (ms) ± 95\% confidence interval of each method.}
\label{tab:small_results}
\end{table}

To assure the reader that our algorithm is indeed faster than the sorting based methods, we present the results of our experiments on small scale problems in \cref{tab:small_results}. 
We test our algorithm on small scale problems where the difference in runtimes is not as significant,
however these problems occur frequently in practice. \cref{tab:small_results} is consistent with the experimental
results obtained in \cref{sec:experiments}, namely our no-sort methods outperform the sorting based methods in runtime.'
This assures that the algorithms developed here provide improvements in runtime for inputs of varying sizes.

The results obtained in \cref{tab:small_results} were generated using a similar process to that of \cref{sec:experiments} 
where instead of 10 trails per experiment, we used 50 trails per experiment. \cref{tab:small_results} shows the mean
time in ms $\pm$ the 95\% confidence interval for each method.

\cref{tab:small_results} shows that even for smaller settings our method is faster than the sorting based methods. The results are consistent with the theoretical complexity of the algorithms, which is $O(n \log n)$ for the sorting based methods and $O(n)$ for the QuickSelect based methods.

\subsection{Computer Specifications}

The results obtained in this work were all generated on a computer with the following Specifications.
\begin{itemize}
  \item AMD Ryzen Thread ripper 3970X 32-Core (64) @ 4.55 GHz
  \item 256 GB RAM
  \item Julia 1.11.5
\end{itemize}

The activity of other users on the system is minimized to account for scheduling conflicts.
The presence of multithreading in our language of choice (Julia) is not used in this work, as while
the runtime of the experiments would be shorter, we observed individual threads ``thrashing'', where 
they would suddenly take twice or three times as long on one particular iteration. 
We conjecture that this is due to the garbage collector. For this reason, we manually disable the 
Julia garbage collector, even when running synchronously.

\section{Algorithms}

\subsection{Non-Recursive QVaR}

\begin{algorithm}
  \caption{QuickVaR: Linear-time algorithm for VaR}
  \Input{vector $\bm{x}^0$, pmf $\bm{p}^0$ and $\alpha^0 \in (0,1)$}
  \Output{$\alpha$-quantile of $\tilde{x}$}
  $f_0 \gets 1$;~~~~$b_0 \gets n$;~~~~$k=1$ \;
  \While{$b_{k-1} > f_{k-1}$} {
    $i_k \gets f_{k-1} + \lceil\frac{b_{k-1}-f_{k-1}}{2}\rceil$ \label{line:pivot_comp} \tcp*{Pivot index}
    $v_k \gets x^{k-1}_{i_k}$ \tcp*{Pivot value}
    %Median-of-medians for $O(b-f)$ runtime}
     $l_k, g_k, \bm{x}^k, \bm{p}^k \gets \operatorname{partition}(\bm{x}^{k-1}, \bm{p}^{k-1}, v_k, f_{k-1}, b_{k-1})$
    \tcp*{Alg.~\ref{alg:partition}, \cref{lem:hoare-partition}} \label{line:partion_line}    
    $t_k \gets \sum_{j=f_{k-1}}^{l_k-1} p^k_j$  \tcp*{$\P{\tilde{x}^k < v_k}$}
    $e_k \gets \sum_{j = l_k}^{g_k} p^{k}_j $   \tcp*{$\P{\tilde{x}^k = v_k}$}
    \If{$\alpha_{k-1} < t_k$\label{line:determine_left}}
    {
      \tcp{$\alpha_{k-1} < \P{ \tilde{x}^k < v_k }
        \implies \alpha_{k-1}< 1 - \P{ \tilde{x}^k \ge v_k}$}
      \tcp{$\implies \P{ \tilde{x}^k \ge v_k } < 1 - \alpha_{k-1}
        \implies \quant^{+}_{\alpha_{k-1}} < v_k$}
      $f_k \gets f_{k-1}$;~~~$b_k \gets l_k-1$\;
      $\alpha_k \gets \alpha_{k-1}$\;
    }
    \ElseIf{$t_k + e_k \le  \alpha_{k-1} \label{line:determine_right}$} 
    {
      % \tcp{$\P{\tilde{x}^k \le v_k} < \alpha_{k-1} \implies \quant_{\alpha_{k-1}} > v_k$}
      \tcp{$\P{\tilde{x}^k \le v_k} \le \alpha_{k-1}
        \implies \P{\tilde{x}^k > v_k} \ge 1 - \alpha_{k-1}$} 
        \tcp{$\implies \P{\tilde{x}^k \ge v_k+\epsilon} \ge 1 - \alpha_{k-1}
        \implies \quant_{\alpha_{k-1}}^+ > v_k$}
      $f_k \gets g_k+1$;~~~$b_k \gets b_{k-1}$\;
      $\alpha_k \gets \alpha_{k-1} - t_k - e_k$\;
    }
    \Else{
      \tcp{$\alpha_{k-1} \ge \P{ \tilde{x}^k < v_k } \, \wedge\, \P{ \tilde{x}^k \le v_k } > \alpha_{k-1}$} \label{determine_middle}
      \tcp{$\implies \P{ \tilde{x}^k \ge v_k } \ge 1-\alpha_{k-1}
        \, \wedge \, \P{ \tilde{x}^k \le v_k } \ge \alpha_{k-1}
        \, \wedge \, \P{ \tilde{x}^k > v_k } < 1 - \alpha_{k-1}$}
      \tcp{$\implies \quant^+_{\alpha_{k-1} } = v_k$}
      $f_k \gets g_k$;~~~$b_k \gets g_k$\;
    }
    $k \gets k + 1$\; 
  }
  \KwRet{$x^k_{b_k}$}\;    
\end{algorithm}  


\subsection{Quick CVaR}

% \begin{algorithm}
%   %\floatname{algorithm}{Quick CVaR}
%   \algrenewcommand\algorithmicrequire{\textbf{Input: Random variable $\tilde{X}$ as vector $x$ with probability mass function p and $\alpha \in [0,1]$}}
%   \algrenewcommand\algorithmicensure{\textbf{Output: $CVaR_\alpha(\tilde{X})$}}
%   \caption{QuickCVaR}\label{alg:quick_cvar}
%   \begin{algorithmic}[1]
%     \Require{}
%     \Ensure{} 
%     \todo{Make absolutely sure that you tell the reader that we use 1 or 0 based indexing}
%     \State{$q^+_\alpha \gets \text{quick\_quantile}(x, p, \alpha)$}
%     \State{$val \gets 0$}
%     \State{$p_{left} \gets 1$}
%     \For{$i \in 0 \dots n$}
%       \State{$inc \gets \min(\frac{p[i]}{\alpha}, p_{left})$}
%       \State{$val += x[i] * inc$}
%       \State{$p_{left} -= inc$}
%       \If{$p_{left} \leq 0$}
%         \State{Break}
%       \EndIf{}
%     \EndFor{}
%     \State{\Return{$val$}}
%   \end{algorithmic}
% \end{algorithm}


\section{Greedy Algorithm}

\todo{what weights?}
\mm{Why would we need the weights to be non-negative?}

% \begin{algorithm} \label{alg:greedy-polymatroid}
%     \algrenewcommand\algorithmicrequire{\textbf{Input: }Finite $N = [1 \ldots n]$, non-decreasing submodular function $f : 2^N \to \Real$ satisfying $f(\emptyset) = 0$ and $f(N) = 1$} 
%     \algrenewcommand\algorithmicensure{\textbf{Output: }$\max \{f(S) : S\subseteq N\}$}
%   \caption{Greedy algorithm from Submodular Functions, Polymatroids, TDI}
%  \begin{algorithmic}[1]
%     \Require{}
%     \Ensure{}
%     \State{Order the elements such that $c_1 \geq c_2 \cdots \geq c_n$.}
%     \State{Let $S_i := \{1, \dots, i\}$ and $S_0 := \emptyset$}
%     \State{Let $x_i :=  \begin{cases}f(S_i) - f(S_{i-1}) & \text{for } i = 1,\dots, r \\ 0 & \text{otherwise} \end{cases}$}
% \end{algorithmic}
% \end{algorithm}

\begin{algorithm}
    \caption{Greedy algorithm from Submodular Functions, Polymatroids, TDI, \mm{rewrite for random variables (and law-invariant risk measure?) Needs more care in terms of bounding $c$ and $f$'s properties.} }\label{alg:greedy-polymatroid}

    \Input{Non-decreasing submodular function $f \colon  2^{\left\{ 1, \dots , n \right\}} \to \Real$ satisfying $f(\emptyset) = 0$ and $f(\left\{ 1, \dots , n \right\}) = 1$, and an objective $\bm{c}\in \Real^n$}
    \Output{$\min \{f(S) : S\subseteq \left\{ 1, \dots , n \right\}\}$}

    Assume that $c_1 \le c_2 \cdots \le c_n$\;
    Let $S_{i+1} := \{1, \dots, i+1\}$ and $S_0 := \emptyset$\;
    Let $x_i :=  \begin{cases}f(S_i) - f(S_{i-1}) & \text{for } i = 1,\dots, r \\ 0 & \text{otherwise} \end{cases}$\;
\end{algorithm}

% Graveyard
% \begin{figure}[htbp]
%   \centering
%   \includegraphics[width=0.70\textwidth]{./code/plots/cvar_div_qcvar.pdf}
%   \caption{Linear regression fit of a logarithmic function to the ratio of the time taken to compute $\cvar{0.5}{\tilde{x}}$ by sorting (CVaR) and QuickSelect (QuickCVaR). \todo{dont like this graph, can we get rid of it?}}
%   \label{fig:cvar_div_qcvar}
% \end{figure}
% \begin{algorithm}
%   \algrenewcommand\algorithmicrequire{\textbf{Input: vector $x$, pmf $p$ and maximum divergence $\beta$}}
%   \algrenewcommand\algorithmicensure{\textbf{Output: new distribution $q$}}
%   \caption{Linear Time Algorithm for TVaR}\label{alg:quick_tvar}
%   \begin{algorithmic}[1]
%     \Require{}
%     \Ensure{}
%     \State{$m \gets \arg\min \left\{ x_i \mid  i = 1, \dots, n \right\}$}
%     \State{$\delta \gets \min\left\{\frac{\beta}{2}, 1 - p_m\right\} $}
%     \State{$\quant^+ \gets \operatorname{QuickQuantile}(x, q, \delta)$ }
%     \State{$q_m \gets q_m + \delta$}
%     \State{$\lambda \gets \delta$}
%     \For{$i \in 1 \dots n$}
%       \If{$q[i] > \quant^+$}
%         \State{$\lambda \gets \lambda - q[i]$}
%         \State{$q[i] \gets 0$} \todo{Wouldn't it be best to return $\lambda$ here?}
%       \EndIf{}
%     \EndFor{}
%     \State{\Return{$\lambda$}} \todo{I dont think this is right}
%   \end{algorithmic}
% \end{algorithm}

% \subsection{Piece-wise Linear Minimization}
% \maeve{Incorporate projection.jl }

% \subsection{Quick CVaR}

% \begin{algorithm}
%   %\floatname{algorithm}{Quick CVaR}
%   \algrenewcommand\algorithmicrequire{\textbf{Input: Random variable $\tilde{X}$ as vector $x$ with probability mass function p and $\alpha \in [0,1]$}}
%   \algrenewcommand\algorithmicensure{\textbf{Output: $CVaR_\alpha(\tilde{X})$}}
%   \caption{QuickCVaR}\label{alg:quick_cvar}
%   \begin{algorithmic}[1]
%     \Require{}
%     \Ensure{} 
%     \todo{Make absolutely sure that you tell the reader that we use 1 or 0 based indexing}
%     \State{$q^+_\alpha \gets \text{quick\_quantile}(x, p, \alpha)$}
%     \State{$val \gets 0$}
%     \State{$p_{left} \gets 1$}
%     \For{$i \in 0 \dots n$}
%       \State{$inc \gets \min(\frac{p[i]}{\alpha}, p_{left})$}
%       \State{$val += x[i] * inc$}
%       \State{$p_{left} -= inc$}
%       \If{$p_{left} \leq 0$}
%         \State{Break}
%       \EndIf{}
%     \EndFor{}
%     \State{\Return{$val$}}
%   \end{algorithmic}
% \end{algorithm}

% \begin{algorithm}
% \algrenewcommand\algorithmicrequire{\textbf{Input: Random variable $x \in \Real^n$ with probability mass function $p \in \Delta^n$ and $f, b \in \Real$}}
% \algrenewcommand\algorithmicensure{\textbf{Output: indices of partion $i, j \in \Real$}}
% \caption{Hoare's partition scheme}\label{alg:partition}
%   \begin{algorithmic}[1]
%     \Require{}
%     \Ensure{} 
%     \State{$i \gets f + \lceil\frac{b-f}{2}\rceil$}
%     \State{$\lambda \gets f$} % less than
%     \State{$\xi \gets f$} % equal to
%     \State{$\mu \gets b$} % greater than
%     \While{$\xi \leq \mu$}
%       \If{$x[\xi] < x[i]$}
%         \State{swap $x[\xi]$ with $x[\lambda]$}
%         \State{swap $p[\xi]$ with $p[\lambda]$}
%         \State{$\lambda \gets \mu + 1$}
%         \State{$\xi \gets \xi + 1$}
%       \ElsIf{$x[\xi] > x[i]$}
%         \State{$\mu \gets \mu - 1$}
%       \Else{}
%         \State{$\xi \gets \xi + 1$}
%       \EndIf{}
%     \EndWhile{}
%     \State{\Return{($\lambda - 1$, $\mu$)} \Comment{last element less than the pivot, first element greater than the pivot}}
%   \end{algorithmic}
% \end{algorithm}



% \section{Greedy Algorithm}

% \mm{Why would we need the weights to be non-negative?}

% \begin{algorithm} \label{alg:greedy-polymatroid}
%     \algrenewcommand\algorithmicrequire{\textbf{Input: }Finite $N = \{1 \ldots n\}$, non-decreasing submodular function $f : 2^N \to \Real$ satisfying $f(\emptyset) = 0$ and $f(N) = 1$} 
%     \algrenewcommand\algorithmicensure{\textbf{Output: }$\max \{f(S) : S\subseteq N\}$}
%   \caption{Greedy algorithm from Submodular Functions, Polymatroids, TDI}
%  \begin{algorithmic}[1]
%     \Require{}
%     \Ensure{}
%     \State{Order the elements such that $c_1 \geq c_2 \cdots \geq c_n$.}
%     \State{Let $S_i := \{1, \dots, i\}$ and $S_0 := \emptyset$}
%     \State{Let $x_i :=  \begin{cases}f(S_i) - f(S_{i-1}) & \text{for } i = 1,\dots, r \\ 0 & \text{otherwise} \end{cases}$}
% \end{algorithmic}
% \end{algorithm}
% \section{k-capped simplex}

% \begin{equation}
%   \label{eq:k_capped_simplex}
%   \psi(x, k) = 
%   \begin{mprog}
%     \minimize{\xi \in \Real^n} \lvert \lvert \xi - x \rvert \rvert_2^2
%     \stc \xi \in \Delta_k^n
%   \end{mprog}
%   =
%   \begin{mprog}
%     \minimize{\bm\xi \in \Real^n} \lvert \lvert \bm\xi - \bm{x} \rvert \rvert_2^2
%     \stc \bm\xi\tr \bm{1} \leq k
%     \cs \bm{0} \le \bm\xi \le \bm{1} 
%   \end{mprog}
% \end{equation}

% \mm{define k-capped simplex}

%     \todo{Make absolutely sure that you tell the reader that we use 1based indexing. Make sure that we always use 1-based indices in all algorithms}

% \begin{algorithm}
%   \algrenewcommand\algorithmicrequire{\textbf{Input:   $\tilde{X}$ as vector $x$ to project and sum constraint k}}
%   \algrenewcommand\algorithmicensure{\textbf{Output: $\alpha$-quantile of $\tilde{X}$}}
%   \caption{algorithm}{K-capped}\label{alg:quick-quantile}
%   \begin{algorithmic}[1]
%     \Require{}
%     \Ensure{}
%     \State{$frontA \gets 1$}
%     \State{ $backA \gets n$ }
%     \State{$frontB \gets 1$}
%     \State{ $backB \gets n$ }
%     \State{ $midSum \gets 0$ }
%     \State{ $minMid \gets inf$ }
%     \State{ $midSize \gets 0$ }
%     \State{ $maxMid \gets -inf$ }
%     \While{$backB - frontB \geq 1$ and $backA - frontA \geq 1$}
      
%      \Comment{Lomuto partition scheme}
    
%       \If{$backB - frontB >= 1$}
%       \State{$ind,leftsum,maxLeft,minLeft \gets modifiedPartition(x, l, f, b) - 1$}
%       \State{$\gamma \gets -2x[ind] + 2$}
%        \If{$maxMid + \gamma*2 >= 1$}
%          \State{$front \gets ind + 1$}
%        \ElsIf{$minMid + \gamma*2 <= 0$}
%          \State{$back \gets ind$} 
%        \Else{}
%        \State{$sum = \sum_{n=frontA}^{backA} (max(0, min(1, x[i])))  + midSum + \gamma*midSize + \sum_{i=frontB}^{backB} (max(0, min(1, x[i])))$}
%        \If{$sum > k$}
%            \State{$back \gets ind - 1$}
%        \ElsIf {$sum < k$}
%            \State{$front \gets ind$}
%            \If {$frontB - backA > midsize$}
%            \State{$midSize \gets frontB - backA$}
%            \State{$midSum \gets leftSum + midSum$}
%             \EndIf{}
            
           
%        \Else{}
%          return solution with computed $\gamma$
        
%       \EndIf{}
      
%       \EndIf{}
%       \EndIf{}
%     \If{$backA - backB \ge 1$} 
%        \State{ $backA \gets backB$ }
%      \EndIf{}
%     \If{$backA - frontA >= 1$}
%       \State{$ind,rightsum,maxRight,minRight \gets modifiedPartition(x, r, f, b) - 1$}
%       \State{$\gamma \gets -2x[ind]$}
%        \If{$maxMid + \gamma*2 >= 1$}
%          \State{$front \gets ind + 1$}
%        \ElsIf{$minMid + \gamma*2 <= 0$}
%          \State{$back \gets ind$} 
%        \Else{}
%        \State{$sum = \sum_{n=frontA}^{backA} (max(0, min(1, x[i])))  + midSum + \gamma*midSize + \sum_{i=frontB}^{backB} (max(0, min(1, x[i])))$}
%        \If{$sum > k$}
%            \State{$back \gets ind - 1$}
%        \ElsIf {$sum < k$}
%            \State{$front \gets ind$}
%            \If {$frontB - backA > midSize$}
%            \State{$midSize \gets frontB - backA$}
%            \State{$midSum \gets rightSum + midSum$}
%             \EndIf{}
            
           
%        \Else{}
         
%           \EndIf{}   
%      \EndIf{}
%       \EndIf{}  
%   \EndWhile{}
%     \State{\Return{$x[front]$}}

%   \end{algorithmic}
% \end{algorithm}
% \section*{NeurIPS Paper Checklist}
%
% %%% BEGIN INSTRUCTIONS %%%
%
% \textcolor{blue}{IMPORTANT}, please:
% \begin{itemize}
%     \item {\bf Delete this instruction block, but keep the section heading ``NeurIPS Paper Checklist"},
%     \item  {\bf Keep the checklist subsection headings, questions/answers and guidelines below.}
%     \item {\bf Do not modify the questions and only use the provided macros for your answers}.
% \end{itemize} 
%
% %%% END INSTRUCTIONS %%%
%
%
% \begin{enumerate}
%
% \item {\bf Claims}
%     \item[] Question: Do the main claims made in the abstract and introduction accurately reflect the paper's contributions and scope?
%     \item[] Answer: \answerYes{
%       } % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: \cref{sec:var-linear} describes the linear time VaR agorithm which is a central contribution
%       of our work, and \cref{sec:experiments} demonstrates empirical results that support the claims made in the introduction.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The answer NA means that the abstract and introduction do not include the claims made in the paper.
%         \item The abstract and/or introduction should clearly state the claims made, including the contributions made in the paper and important assumptions and limitations. A No or NA answer to this question will not be perceived well by the reviewers. 
%         \item The claims made should match theoretical and experimental results, and reflect how much the results can be expected to generalize to other settings. 
%         \item It is fine to include aspirational goals as motivation as long as it is clear that these goals are not attained by the paper. 
%     \end{itemize}
%
% \item {\bf Limitations}
%     \item[] Question: Does the paper discuss the limitations of the work performed by the authors?
%     \item[] Answer: \answerTODO{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: \justificationTODO{}
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The answer NA means that the paper has no limitation while the answer No means that the paper has limitations, but those are not discussed in the paper. 
%         \item The authors are encouraged to create a separate "Limitations" section in their paper.
%         \item The paper should point out any strong assumptions and how robust the results are to violations of these assumptions (e.g., independence assumptions, noiseless settings, model well-specification, asymptotic approximations only holding locally). The authors should reflect on how these assumptions might be violated in practice and what the implications would be.
%         \item The authors should reflect on the scope of the claims made, e.g., if the approach was only tested on a few datasets or with a few runs. In general, empirical results often depend on implicit assumptions, which should be articulated.
%         \item The authors should reflect on the factors that influence the performance of the approach. For example, a facial recognition algorithm may perform poorly when image resolution is low or images are taken in low lighting. Or a speech-to-text system might not be used reliably to provide closed captions for online lectures because it fails to handle technical jargon.
%         \item The authors should discuss the computational efficiency of the proposed algorithms and how they scale with dataset size.
%         \item If applicable, the authors should discuss possible limitations of their approach to address problems of privacy and fairness.
%         \item While the authors might fear that complete honesty about limitations might be used by reviewers as grounds for rejection, a worse outcome might be that reviewers discover limitations that aren't acknowledged in the paper. The authors should use their best judgment and recognize that individual actions in favor of transparency play an important role in developing norms that preserve the integrity of the community. Reviewers will be specifically instructed to not penalize honesty concerning limitations.
%     \end{itemize}
%
% \item {\bf Theory assumptions and proofs}
%     \item[] Question: For each theoretical result, does the paper provide the full set of assumptions and a complete (and correct) proof?
%     \item[] Answer: \answerYes{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: Each of our algorithms have proofs accompanying them, and the assumptions are clearly stated in the paper. The proofs can be found in the apppendix.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The answer NA means that the paper does not include theoretical results. 
%         \item All the theorems, formulas, and proofs in the paper should be numbered and cross-referenced.
%         \item All assumptions should be clearly stated or referenced in the statement of any theorems.
%         \item The proofs can either appear in the main paper or the supplemental material, but if they appear in the supplemental material, the authors are encouraged to provide a short proof sketch to provide intuition. 
%         \item Inversely, any informal proof provided in the core of the paper should be complemented by formal proofs provided in appendix or supplemental material.
%         \item Theorems and Lemmas that the proof relies upon should be properly referenced. 
%     \end{itemize}
%
%     \item {\bf Experimental result reproducibility}
%     \item[] Question: Does the paper fully disclose all the information needed to reproduce the main experimental results of the paper to the extent that it affects the main claims and/or conclusions of the paper (regardless of whether the code and data are provided or not)?
%     \item[] Answer: \answerYes{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: Our experimental design and implementation details are given in \cref{sec:experiments} and \cref{sec:appendix_experiments}. The code will also be available after the review process.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item If the paper includes experiments, a No answer to this question will not be perceived well by the reviewers: Making the paper reproducible is important, regardless of whether the code and data are provided or not.
%         \item If the contribution is a dataset and/or model, the authors should describe the steps taken to make their results reproducible or verifiable. 
%         \item Depending on the contribution, reproducibility can be accomplished in various ways. For example, if the contribution is a novel architecture, describing the architecture fully might suffice, or if the contribution is a specific model and empirical evaluation, it may be necessary to either make it possible for others to replicate the model with the same dataset, or provide access to the model. In general. releasing code and data is often one good way to accomplish this, but reproducibility can also be provided via detailed instructions for how to replicate the results, access to a hosted model (e.g., in the case of a large language model), releasing of a model checkpoint, or other means that are appropriate to the research performed.
%         \item While NeurIPS does not require releasing code, the conference does require all submissions to provide some reasonable avenue for reproducibility, which may depend on the nature of the contribution. For example
%         \begin{enumerate}
%             \item If the contribution is primarily a new algorithm, the paper should make it clear how to reproduce that algorithm.
%             \item If the contribution is primarily a new model architecture, the paper should describe the architecture clearly and fully.
%             \item If the contribution is a new model (e.g., a large language model), then there should either be a way to access this model for reproducing the results or a way to reproduce the model (e.g., with an open-source dataset or instructions for how to construct the dataset).
%             \item We recognize that reproducibility may be tricky in some cases, in which case authors are welcome to describe the particular way they provide for reproducibility. In the case of closed-source models, it may be that access to the model is limited in some way (e.g., to registered users), but it should be possible for other researchers to have some path to reproducing or verifying the results.
%         \end{enumerate}
%     \end{itemize}
%
%
% \item {\bf Open access to data and code}
%     \item[] Question: Does the paper provide open access to the data and code, with sufficient instructions to faithfully reproduce the main experimental results, as described in supplemental material?
%     \item[] Answer: \answerYes{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: Code will be released via GitHub after the review process.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item Please see the NeurIPS code and data submission guidelines (\url{https://nips.cc/public/guides/CodeSubmissionPolicy}) for more details.
%         \item The instructions should contain the exact command and environment needed to run to reproduce the results. See the NeurIPS code and data submission guidelines (\url{https://nips.cc/public/guides/CodeSubmissionPolicy}) for more details.
%         \item The authors should provide instructions on data access and preparation, including how to access the raw data, preprocessed data, intermediate data, and generated data, etc.
%         \item The authors should provide scripts to reproduce all experimental results for the new proposed method and baselines. If only a subset of experiments are reproducible, they should state which ones are omitted from the script and why.
%         \item At submission time, to preserve anonymity, the authors should release anonymized versions (if applicable).
%         \item Providing as much information as possible in supplemental material (appended to the paper) is recommended, but including URLs to data and code is permitted.
%     \end{itemize}
%
%
% \item {\bf Experimental setting/details}
%     \item[] Question: Does the paper specify all the training and test details (e.g., data splits, hyperparameters, how they were chosen, type of optimizer, etc.) necessary to understand the results?
%     \item[] Answer: \answerYes{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: Where neccecary we disclose our random variable and risk parameters in \cref{sec:experiments} and \cref{sec:appendix_experiments}
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The experimental setting should be presented in the core of the paper to a level of detail that is necessary to appreciate the results and make sense of them.
%         \item The full details can be provided either with the code, in appendix, or as supplemental material.
%     \end{itemize}
%
% \item {\bf Experiment statistical significance}
%     \item[] Question: Does the paper report error bars suitably and correctly defined or other appropriate information about the statistical significance of the experiments?
%     \item[] Answer: \answerYes{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: As explained in \cref{sec:experiments} we run our experiments for 10 trials and report the mean and 95\% confidence interval of the results. We assume that runtimes on a machine are normally distributed.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The authors should answer "Yes" if the results are accompanied by error bars, confidence intervals, or statistical significance tests, at least for the experiments that support the main claims of the paper.
%         \item The factors of variability that the error bars are capturing should be clearly stated (for example, train/test split, initialization, random drawing of some parameter, or overall run with given experimental conditions).
%         \item The method for calculating the error bars should be explained (closed form formula, call to a library function, bootstrap, etc.)
%         \item The assumptions made should be given (e.g., Normally distributed errors).
%         \item It should be clear whether the error bar is the standard deviation or the standard error of the mean.
%         \item It is OK to report 1-sigma error bars, but one should state it. The authors should preferably report a 2-sigma error bar than state that they have a 96\% CI, if the hypothesis of Normality of errors is not verified.
%         \item For asymmetric distributions, the authors should be careful not to show in tables or figures symmetric error bars that would yield results that are out of range (e.g. negative error rates).
%         \item If error bars are reported in tables or plots, The authors should explain in the text how they were calculated and reference the corresponding figures or tables in the text.
%     \end{itemize}
%
% \item {\bf Experiments compute resources}
%     \item[] Question: For each experiment, does the paper provide sufficient information on the computer resources (type of compute workers, memory, time of execution) needed to reproduce the experiments?
%     \item[] Answer: \answerYes{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: See \cref{sec:appendix_experiments}.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The paper should indicate the type of compute workers CPU or GPU, internal cluster, or cloud provider, including relevant memory and storage.
%         \item The paper should provide the amount of compute required for each of the individual experimental runs as well as estimate the total compute. 
%         \item The paper should disclose whether the full research project required more compute than the experiments reported in the paper (e.g., preliminary or failed experiments that didn't make it into the paper). 
%     \end{itemize}
%     
% \item {\bf Code of ethics}
%     \item[] Question: Does the research conducted in the paper conform, in every respect, with the NeurIPS Code of Ethics \url{https://neurips.cc/public/EthicsGuidelines}?
%     \item[] Answer: \answerYes{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: The authors of this work have reviewed the NeurIPS Code of Ethics and confirm that the research conducted in this paper conforms with it.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The answer NA means that the authors have not reviewed the NeurIPS Code of Ethics.
%         \item If the authors answer No, they should explain the special circumstances that require a deviation from the Code of Ethics.
%         \item The authors should make sure to preserve anonymity (e.g., if there is a special consideration due to laws or regulations in their jurisdiction).
%     \end{itemize}
%
%
% \item {\bf Broader impacts}
%     \item[] Question: Does the paper discuss both potential positive societal impacts and negative societal impacts of the work performed?
%     \item[] Answer: \answerNA{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: There is little to no societal impact of our work. The authors do not have reason to believe that the work performed has any propensity for negative societal impact.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The answer NA means that there is no societal impact of the work performed.
%         \item If the authors answer NA or No, they should explain why their work has no societal impact or why the paper does not address societal impact.
%     \end{itemize}
%     
% \item {\bf Safeguards}
%     \item[] Question: Does the paper describe safeguards that have been put in place for responsible release of data or models that have a high risk for misuse (e.g., pretrained language models, image generators, or scraped datasets)?
%     \item[] Answer: \answerNA{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: Our work does not involve any data or models that have a high risk for misuse.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The answer NA means that the paper poses no such risks.
%     \end{itemize}
%
% \item {\bf Licenses for existing assets}
%     \item[] Question: Are the creators or original owners of assets (e.g., code, data, models), used in the paper, properly credited and are the license and terms of use explicitly mentioned and properly respected?
%     \item[] Answer: \answerNA{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: We do not use any existing assets. 
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The answer NA means that the paper does not use existing assets.
%     \end{itemize}
%
% \item {\bf New assets}
%     \item[] Question: Are new assets introduced in the paper well documented and is the documentation provided alongside the assets?
%     \item[] Answer: \answerNA{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: We do not release new assets.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The answer NA means that the paper does not release new assets.
%     \end{itemize}
%
% \item {\bf Crowdsourcing and research with human subjects}
%     \item[] Question: For crowdsourcing experiments and research with human subjects, does the paper include the full text of instructions given to participants and screenshots, if applicable, as well as details about compensation (if any)? 
%     \item[] Answer: \answerNA{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: Our work does not involve crowdsourcing nor research with human subjects.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The answer NA means that the paper does not involve crowdsourcing nor research with human subjects.
%     \end{itemize}
%
% \item {\bf Institutional review board (IRB) approvals or equivalent for research with human subjects}
%     \item[] Question: Does the paper describe potential risks incurred by study participants, whether such risks were disclosed to the subjects, and whether Institutional Review Board (IRB) approvals (or an equivalent approval/review based on the requirements of your country or institution) were obtained?
%     \item[] Answer: \answerNA{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: Our work does not involve crowdsourcing nor research with human subjects.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The answer NA means that the paper does not involve crowdsourcing nor research with human subjects.
%     \end{itemize}
%
% \item {\bf Declaration of LLM usage}
%     \item[] Question: Does the paper describe the usage of LLMs if it is an important, original, or non-standard component of the core methods in this research? Note that if the LLM is used only for writing, editing, or formatting purposes and does not impact the core methodology, scientific rigorousness, or originality of the research, declaration is not required.
%     %this research? 
%     \item[] Answer: \answerNA{} % Replace by \answerYes{}, \answerNo{}, or \answerNA{}.
%     \item[] Justification: We do not discuss nor is our core contribution contingent on LLMs.
%     \item[] Guidelines:
%     \begin{itemize}
%         \item The answer NA means that the core method development in this research does not involve LLMs as any important, original, or non-standard components.
%     \end{itemize}
%
% \end{enumerate}

\end{document}

%%% Local Variables:
%%% mode: LaTeX
%%% TeX-master: t
%%% End:
